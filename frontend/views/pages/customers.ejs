<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> - <%= appName %></title>
  <link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600;700;800&family=DM+Mono:wght@400;500&family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/app.css">
  <link rel="stylesheet" href="/vendor/leaflet/leaflet.css">
  <script src="/vendor/leaflet/leaflet.js"></script>
  <style>
    :root {
      --d-bg:#eef2f9; --d-card:#fff; --d-border:#e0e7f3;
      --d-text:#0d1b3e; --d-muted:#6b7fa8;
      --d-blue1:#1a6ef5; --d-blue2:#0047cc;
      --d-green:#00c896; --d-red:#f5365c; --d-orange:#fb8c00;
    }
    .pay-page { font-family:'DM Sans',sans-serif; background:var(--d-bg) !important; }

    /* ── Fin-card ── */
    /* ── STAT CARDS — single strip ── */
    .fin-grid, .fin-grid-4 {
      display: grid; grid-template-columns: repeat(4, 1fr);
      background: #fff; border-radius: 16px;
      border: 1px solid #e8eaed; overflow: hidden; margin-bottom: 20px;
    }
    .fin-card {
      padding: 20px 22px 18px; border-right: 1px solid #e8eaed;
      min-height: 130px;
      cursor: pointer; display: flex; flex-direction: column; gap: 0;
      position: relative; overflow: hidden; transition: background .15s;
    }
    .fin-card::before, .fin-card::after { display:none !important; content:none !important; }
    .fin-card .b1,.fin-card .b2,.fin-card .b3,
    .fin-card .orb,.fin-card .orb1,.fin-card .orb2 { display:none !important; }
    .fin-card:last-child { border-right: none; }
    .fin-card:hover { background: #fafbff; }
    .fin-card.fc-active { background: #2563eb; border-color: #2563eb; }
    .fin-card.fc-active:hover { background: #1d4ed8; }
    .fin-card.fc-active .xc-lbl  { color: rgba(255,255,255,.70); }
    .fin-card.fc-active .xc-val  { color: #fff; }
    .fin-card.fc-active .xc-sub  { font-size:11px; color:#94a3b8; white-space:nowrap; overflow:hidden; text-overflow:ellipsis; transition:color .15s; }
    .fin-card.fc-active .xc-pill { display:inline-flex; align-items:center; padding:1px 6px; border-radius:100px; font-size:10.5px; font-weight:700; white-space:nowrap; align-self:flex-start; transition:all .15s; }
    .fin-card.fc-active .sp-line { stroke: rgba(255,255,255,.70) !important; }
    .fin-card.fc-active .sp-fill { fill: rgba(255,255,255,.12) !important; }
    .fin-card.fc-active .sp-dot  { fill: #fff !important; }
    .xc-lbl  { font-size:12px; font-weight:500; color:#64748b; margin-bottom:10px; transition:color .15s; }
    .xc-body { display:flex; align-items:flex-end; justify-content:space-between; gap:8px; }
    .xc-left { display:flex; flex-direction:column; min-width:0; }
    .xc-val  { font-size:30px; font-weight:800; color:#0f172a; letter-spacing:-1.2px; line-height:1; transition:color .15s; }
    @media(max-width:1400px){ .xc-val { font-size:26px; } }
    .xc-meta { display:flex; flex-direction:column; gap:3px; margin-top:6px; }
    .xc-pill { display:inline-flex; align-items:center; padding:1px 6px; border-radius:100px; font-size:10.5px; font-weight:700; white-space:nowrap; align-self:flex-start; transition:all .15s; }
    .xc-blue   { background:#dbeafe; color:#1d4ed8; }
    .xc-green  { background:#dcfce7; color:#16a34a; }
    .xc-red    { background:#fee2e2; color:#dc2626; }
    .xc-amber  { background:#fef3c7; color:#b45309; }
    .xc-teal   { background:#cffafe; color:#0e7490; }
    .xc-orange { background:#ffedd5; color:#c2410c; }
    .xc-sub  { font-size:11px; color:#94a3b8; white-space:nowrap; overflow:hidden; text-overflow:ellipsis; transition:color .15s; }
    .xc-spark { flex-shrink:0; width:72px; height:36px; }
    .xc-spark svg { width:72px; height:36px; }
    .sp-fill { transition:fill .3s; }
    .sp-line { stroke-width:1.8; fill:none; stroke-linecap:round; stroke-linejoin:round; }
    .sp-dot  { }
    @media(max-width:700px) {
      .fin-grid, .fin-grid-4 { grid-template-columns:repeat(2,1fr); }
      .fin-card { border-right:none; border-bottom:1px solid #e8eaed; }
      .fin-card:nth-child(odd)  { border-right:1px solid #e8eaed; }
      .fin-card:nth-child(even) { border-right:none; }
      .fin-card:nth-last-child(-n+2) { border-bottom:none; }
    }
    @media(max-width:400px) {
      .fin-grid, .fin-grid-4 { grid-template-columns:1fr; }
      .fin-card { border-right:none !important; border-bottom:1px solid #e8eaed; }
      .fin-card:last-child { border-bottom:none; }
    }
        /* ── White card ── */
    .w-card { background:var(--d-card);border:1px solid var(--d-border);border-radius:22px;box-shadow:0 2px 12px rgba(26,110,245,.05); }



    /* ── Buttons ── */
    .btn-fin { padding:9px 18px;border-radius:11px;border:none;font-family:'DM Sans',sans-serif;font-size:13px;font-weight:600;cursor:pointer;transition:all .15s;display:inline-flex;align-items:center;gap:6px; }
    .btn-primary-fin { background:var(--d-blue1);color:#fff;box-shadow:0 4px 14px rgba(26,110,245,.3); }
    .btn-primary-fin:hover { background:var(--d-blue2);transform:translateY(-1px);box-shadow:0 6px 18px rgba(26,110,245,.4); }
    .btn-ghost { background:var(--d-card);color:var(--d-text);border:1.5px solid var(--d-border); }
    .btn-ghost:hover { border-color:var(--d-blue1);color:var(--d-blue1);background:#f0f5ff; }
    .btn-green-fin { background:#e6fff7;color:#00a07a;border:1.5px solid #a7f3d0; }
    .btn-green-fin:hover { background:#ccfff0;border-color:#00c896; }
    .btn-sync { background:#eef3ff;color:var(--d-blue1);border:1.5px solid #bfdbfe; }
    .btn-sync:hover { background:#dbeafe; }

    /* ── Search/filter ── */
    .search-wrap { position:relative; }
    .search-wrap input { width:100%;padding:10px 14px 10px 40px;box-sizing:border-box;border:1.5px solid var(--d-border);border-radius:12px;font-family:'DM Sans',sans-serif;font-size:13.5px;color:var(--d-text);background:#fff;transition:border-color .15s,box-shadow .15s;outline:none; }
    .search-wrap input:focus { border-color:var(--d-blue1);box-shadow:0 0 0 3px rgba(26,110,245,.1); }
    .search-wrap .si { position:absolute;left:12px;top:50%;transform:translateY(-50%);color:var(--d-muted);pointer-events:none; }
    .filter-select { padding:10px 14px;border:1.5px solid var(--d-border);border-radius:12px;font-family:'DM Sans',sans-serif;font-size:13px;color:var(--d-text);background:#fff;cursor:pointer;outline:none;transition:border-color .15s; }
    .filter-select:focus { border-color:var(--d-blue1); }

    /* ── Table ── */
    .cust-table { width:100%;border-collapse:collapse;min-width:860px;font-family:'Plus Jakarta Sans',sans-serif; }
    .cust-table thead th { text-align:left;padding:10px 16px;font-size:11px;font-weight:600;letter-spacing:.04em;color:var(--d-muted);border-bottom:1px solid var(--d-border);background:#f8faff;white-space:nowrap; }
    .cust-table tbody tr { border-bottom:1px solid #f0f4fc;transition:background .1s; }
    .cust-table tbody tr:hover { background:#f4f8ff; }
    .cust-table tbody tr:last-child { border-bottom:none; }
    .cust-table td { padding:13px 16px;vertical-align:middle;font-size:13px;color:var(--d-text); }

    .av-circle { width:36px;height:36px;border-radius:50%;display:flex;align-items:center;justify-content:center;font-size:13px;font-weight:700;color:#fff;flex-shrink:0; }
    .cid-badge { font-family:'DM Mono',monospace;font-size:12px;color:var(--d-blue1);background:#eef3ff;padding:3px 8px;border-radius:6px;display:inline-block; }
    .cust-name-link { color:var(--d-text);font-size:13.5px;font-weight:600;text-decoration:none;transition:color .15s; }
    .cust-name-link:hover { color:var(--d-blue1); }

    /* Status badges */
    .sb { font-size:11.5px;font-weight:600;padding:4px 11px;border-radius:20px;display:inline-flex;align-items:center;gap:5px;border:1px solid transparent; }
    .sb-active    { background:#f0fdf9;color:#059669;border-color:#c6f6d5; }
    .sb-inactive  { background:#f7f8fc;color:var(--d-muted);border-color:#e2e8f0; }
    .sb-suspended { background:#fff4f5;color:var(--d-red);border-color:#fecdd3; }
    .sb-overdue   { background:#fff4f5;color:var(--d-red);border-color:#fecdd3; }
    .sb-due-soon  { background:#fffbf0;color:var(--d-orange);border-color:#fde68a; }
    .sb-dot { width:6px;height:6px;border-radius:50%;display:inline-block;flex-shrink:0; }

    /* Due date colors */
    .due-ok      { color:var(--d-muted); }
    .due-soon-c  { color:var(--d-orange); }
    .due-overdue { color:var(--d-red); }

    /* Row action buttons */
    .rb { padding:4px 13px;border-radius:7px;font-family:'Plus Jakarta Sans',sans-serif;font-size:12px;font-weight:600;cursor:pointer;transition:all .15s;display:inline-flex;align-items:center;gap:4px;text-decoration:none;white-space:nowrap;border:1px solid; }
    .rb-wa   { background:#fff;color:#059669;border-color:#a7f3d0; }
    .rb-wa:hover { background:#f0fdf9; }
    .rb-edit { background:#fff;color:var(--d-blue1);border-color:#bfdbfe; }
    .rb-edit:hover { background:#eef3ff; }
    .rb-del  { background:#fff;color:#cbd5e0;border-color:#e2e8f0; }
    .rb-del:hover { color:var(--d-red);border-color:#fecdd3;background:#fff4f5; }

    /* Pagination */
    .pg-btn { padding:6px 12px;border-radius:9px;font-size:13px;font-weight:600;text-decoration:none;transition:all .15s;border:1.5px solid var(--d-border);color:var(--d-muted);background:var(--d-card); }
    .pg-btn:hover  { border-color:var(--d-blue1);color:var(--d-blue1); }
    .pg-btn.active { background:var(--d-blue1);color:#fff;border-color:var(--d-blue1);box-shadow:0 3px 10px rgba(26,110,245,.3); }

    /* Toast */
    .toast-ok  { background:#e6fff7;border:1px solid #a7f3d0;color:#00875a;padding:12px 16px;border-radius:12px;margin-bottom:16px;font-size:13.5px; }
    .toast-err { background:#fff0f2;border:1px solid #fecdd3;color:var(--d-red);padding:12px 16px;border-radius:12px;margin-bottom:16px;font-size:13.5px; }

    /* ══ MODAL — Bottom-sheet on mobile, centered card on desktop ══ */
    .modal-ov {
      display: none; position: fixed; inset: 0;
      background: rgba(13, 27, 62, .55); backdrop-filter: blur(6px);
      z-index: 9999;
      align-items: flex-end; justify-content: center;  /* mobile: dock bottom */
      padding: 0;
    }
    .modal-ov.active { display: flex; animation: modalOvIn .2s ease; }
    @keyframes modalOvIn { from { opacity: 0; } to { opacity: 1; } }

    .modal-bx {
      background: #fff;
      width: 100%; max-width: 600px;
      border-radius: 22px 22px 0 0;             /* mobile: only top corners */
      max-height: 92vh;
      display: flex; flex-direction: column;     /* sticky header & footer */
      overflow: hidden;
      box-shadow: 0 -8px 32px rgba(0, 0, 0, .15);
      animation: modalSheetUp .26s cubic-bezier(.32, 1.24, .5, 1);
    }
    @keyframes modalSheetUp { from { transform: translateY(100%); } to { transform: translateY(0); } }

    /* Desktop ≥ 600px → centered modal */
    @media (min-width: 600px) {
      .modal-ov { align-items: center; padding: 16px; }
      .modal-bx {
        border-radius: 22px;
        max-height: 88vh;
        box-shadow: 0 24px 60px rgba(0, 0, 0, .18);
        animation: modalScaleIn .22s cubic-bezier(.32, 1.24, .5, 1);
      }
      @keyframes modalScaleIn { from { transform: scale(.94); opacity: 0; } to { transform: scale(1); opacity: 1; } }
    }

    /* Drag handle bar (mobile only) */
    .modal-handle {
      width: 38px; height: 4px; border-radius: 2px;
      background: #d4dae4;
      margin: 10px auto 0;
      flex-shrink: 0;
    }
    @media (min-width: 600px) { .modal-handle { display: none; } }

    /* Header — sticky inside flex column */
    .modal-hdr {
      display: flex; align-items: center; justify-content: space-between;
      padding: 18px 22px 14px;
      border-bottom: 1px solid #f0f0f5;
      flex-shrink: 0;
    }
    .modal-ttl {
      font-size: 16px; font-weight: 800; color: var(--d-text);
      margin: 0; letter-spacing: -.2px;
    }
    .modal-cls {
      width: 30px; height: 30px; border-radius: 50%;
      background: var(--d-bg); border: none;
      display: flex; align-items: center; justify-content: center;
      cursor: pointer; color: var(--d-muted); font-size: 14px;
      transition: background .15s, color .15s;
      flex-shrink: 0;
      /* Reset old position */
      position: static; top: auto; right: auto;
    }
    .modal-cls:hover { background: var(--d-border); color: var(--d-text); }

    /* Body — scrollable area */
    .modal-body {
      padding: 18px 22px;
      overflow-y: auto;
      -webkit-overflow-scrolling: touch;
      flex: 1; min-height: 0;
      /* Reset old inline rules supaya tidak duplikat scrollbar */
    }

    /* Footer — sticky inside flex column */
    .modal-foot {
      padding: 14px 22px;
      border-top: 1px solid #f0f0f5;
      display: flex; gap: 10px; justify-content: flex-end;
      background: #fff;
      flex-shrink: 0;
    }
    @media (max-width: 480px) {
      .modal-foot { gap: 8px; }
      .modal-foot .btn-fin { padding: 12px 16px; font-size: 13.5px; }
    }

    /* Form labels & inputs */
    .flbl {
      display: block; font-size: 11.5px; font-weight: 700;
      color: var(--d-muted); margin-bottom: 5px;
      letter-spacing: .04em; text-transform: uppercase;
    }
    .finput {
      width: 100%; box-sizing: border-box;
      padding: 10px 13px;
      border: 1.5px solid var(--d-border); border-radius: 10px;
      font-family: 'DM Sans', sans-serif; font-size: 13.5px;
      color: var(--d-text); outline: none; background: #fff;
      transition: border-color .15s, box-shadow .15s;
    }
    .finput:focus {
      border-color: var(--d-blue1);
      box-shadow: 0 0 0 3px rgba(26, 110, 245, .1);
    }
    .finput::placeholder { color: #b0bcdc; }
    textarea.finput { resize: vertical; }

    /* Form grid — 2 cols desktop, 1 col mobile (auto via media query) */
    .form-grid-2 {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 12px;
    }
    @media (max-width: 520px) {
      .form-grid-2 { grid-template-columns: 1fr; gap: 10px; }
    }
    .form-grid-2 > .span-all { grid-column: 1 / -1; }
    .form-field { min-width: 0; }
    .form-hint  { font-size: 10.5px; color: var(--d-muted); margin-top: 3px; line-height: 1.4; }
    .form-badge { font-size: 10px; color: #0891b2; font-weight: 600; }
    .form-meta  { font-size: 10px; font-weight: 400; text-transform: none; color: var(--d-muted); }

    /* ── Section header dalam modal (gaya seperti Registrasi Sales) ── */
    .form-sec {
      grid-column: 1 / -1;
      display: flex; align-items: baseline; gap: 10px; flex-wrap: wrap;
      margin: 8px 0 2px; padding-bottom: 7px;
      border-bottom: 1.5px solid var(--d-border);
    }
    .form-sec:first-child { margin-top: 0; }
    .form-sec-t {
      font-size: 12px; font-weight: 800; letter-spacing: .04em; text-transform: uppercase;
      color: var(--d-blue1);
    }
    .form-sec-hint { font-size: 11px; font-weight: 400; color: var(--d-muted); }

    /* ── Peta titik lokasi ── */
    .cust-map { width: 100%; height: 220px; border-radius: 12px; border: 1.5px solid var(--d-border); overflow: hidden; z-index: 0; }
    .cust-loc-actions { display: flex; align-items: center; gap: 10px; flex-wrap: wrap; margin: 2px 0 8px; }
    .cust-loc-btn {
      padding: 7px 12px; font-size: 12px; font-weight: 600; cursor: pointer;
      border: 1px solid #bfdbfe; background: #eff6ff; color: #1d4ed8; border-radius: 8px;
      display: inline-flex; align-items: center; gap: 6px;
    }
    .cust-loc-btn:hover { background: #dbeafe; }

    /* ── Toggle Peta / Satelit (gaya seperti Sales) ── */
    .map-toggle { display:flex; gap:0; background:#fff; border:1px solid var(--d-border); border-radius:10px; overflow:hidden; box-shadow:0 2px 8px rgba(15,40,90,.12); }
    .map-toggle button { border:none; background:#fff; font-family:'DM Sans',sans-serif; font-size:12px; font-weight:600; color:var(--d-muted); padding:7px 14px; cursor:pointer; transition:background .18s ease,color .18s ease; }
    .map-toggle button:hover { background:#f1f5fb; color:var(--d-text); }
    .map-toggle button.on { background:#1a6ef5; color:#fff; }

    /* Animations */
    @keyframes fadeUp { from{opacity:0;transform:translateY(16px)} to{opacity:1;transform:translateY(0)} }
    .a1{animation:fadeUp .45s ease both}
    .a2{animation:fadeUp .45s .07s ease both}
    .a3{animation:fadeUp .45s .14s ease both}
    .a4{animation:fadeUp .45s .21s ease both}
    .a5{animation:fadeUp .45s .28s ease both}
    @keyframes spin{to{transform:translateY(-50%) rotate(360deg)}}

    /* Responsive */
    @media(max-width:1100px) { .fin-grid-4 { grid-template-columns:repeat(2,1fr) !important; } }
    @media(max-width:640px) {
      .col-hide-mob { display:none !important; }
      .btn-fin span { display:none; }
    }
  
    /* ── Standardize card height (match queue.ejs) ── */
    .xc-val   { font-size: 30px; font-weight: 800; color: #0f172a; letter-spacing: -1.2px; line-height: 1; transition: color .15s; }
    .xc-lbl   { font-size: 12px; font-weight: 500; color: #64748b; margin-bottom: 10px; letter-spacing: .1px; transition: color .15s; }
    .xc-sub  { font-size:11px; color:#94a3b8; white-space:nowrap; overflow:hidden; text-overflow:ellipsis; transition:color .15s; }
    .xc-meta { display:flex; flex-direction:column; gap:3px; margin-top:6px; }
    .xc-spark { flex-shrink: 0; width: 72px; height: 36px; }
    .xc-spark svg { width: 72px; height: 36px; }
    @media(max-width:1400px){ .xc-val { font-size: 26px; } }
  </style>
</head>
<body>
<div class="app-layout pay-page">
  <%- include('../partials/sidebar') %>
  <div class="main-content">
    <%- include('../partials/topbar') %>
    <main class="page-content">

      <!-- Toast -->
      <div id="toastOk"  class="toast-ok  a1" style="display:none"></div>
      <div id="toastErr" class="toast-err a1" style="display:none"></div>

      <!-- ══ PAGE HEADER ══ -->
      <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:20px;flex-wrap:wrap;gap:12px" class="a1">
        <div>
          <div style="font-size:22px;font-weight:800;color:var(--d-text)">Customer Data</div>
          <div style="font-size:13px;color:var(--d-muted);margin-top:2px" id="headerSubtitle">Manajemen pelanggan ISP</div>
        </div>
        <div style="display:flex;gap:8px;flex-wrap:wrap">
          <button onclick="syncDueDates()" class="btn-fin btn-ghost" id="btnSyncDue">
            <svg width="14" height="14" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"/></svg>
            <span>Auto Due Date</span>
          </button>
          <button id="importExportToggleBtn" type="button" onclick="toggleImpExpMenu(event)" class="btn-fin" style="background:#fff;color:var(--d-text);border:1px solid var(--d-border);">
            <svg width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-4l-4 4m0 0l-4-4m4 4V4"/></svg>
            <span>Import / Export</span>
            <svg width="11" height="11" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24" style="margin-left:2px;opacity:.6;"><path stroke-linecap="round" stroke-linejoin="round" d="M6 9l6 6 6-6"/></svg>
          </button>
          <button onclick="openAddCustomer()" class="btn-fin btn-primary-fin">
            <svg width="14" height="14" fill="none" stroke="white" stroke-width="2.5" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M12 4v16m8-8H4"/></svg>
            <span>Tambah Customer</span>
          </button>
        </div>
      </div>

      <!-- ══ ROW 1: 4 Fin-cards ══ -->
     
      <div class="fin-grid">
        <div class="fin-card fc-active" id="xc-total" data-filter="total" onclick="custCardClick(this)">
          <div class="xc-lbl">Total Customer</div>
          <div class="xc-body">
            <div class="xc-left">
              <span class="xc-val" id="scTotal">0</span>
              <div class="xc-meta">
                <span class="xc-pill xc-blue" id="xp-total">—</span>
                <span class="xc-sub" id="scTotalSub">—</span>
              </div>
            </div>
            <div class="xc-spark">
              <svg id="sp-total" viewBox="0 0 72 36" preserveAspectRatio="none">
                <path class="sp-fill" id="sp-total-fill" d="" fill="#2563eb" opacity=".12"/>
                <path class="sp-line" id="sp-total-line" d="" stroke="#2563eb"/>
                <circle class="sp-dot" id="sp-total-dot" r="2.5" fill="#2563eb"/>
              </svg>
            </div>
          </div>
        </div>
        <div class="fin-card" id="xc-overdue" data-filter="overdue" onclick="custCardClick(this)">
          <div class="xc-lbl">Overdue</div>
          <div class="xc-body">
            <div class="xc-left">
              <span class="xc-val" id="scOverdue">0</span>
              <div class="xc-meta">
                <span class="xc-pill xc-orange" id="xp-overdue">—</span>
                <span class="xc-sub">Jatuh tempo terlewat</span>
              </div>
            </div>
            <div class="xc-spark">
              <svg id="sp-overdue" viewBox="0 0 72 36" preserveAspectRatio="none">
                <path class="sp-fill" id="sp-overdue-fill" d="" fill="#c2410c" opacity=".12"/>
                <path class="sp-line" id="sp-overdue-line" d="" stroke="#c2410c"/>
                <circle class="sp-dot" id="sp-overdue-dot" r="2.5" fill="#c2410c"/>
              </svg>
            </div>
          </div>
        </div>
        <div class="fin-card" id="xc-duesoon" data-filter="duesoon" onclick="custCardClick(this)">
          <div class="xc-lbl">Jatuh Tempo</div>
          <div class="xc-body">
            <div class="xc-left">
              <span class="xc-val" id="scDueSoon">0</span>
              <div class="xc-meta">
                <span class="xc-pill xc-amber" id="xp-duesoon">—</span>
                <span class="xc-sub">Segera jatuh tempo</span>
              </div>
            </div>
            <div class="xc-spark">
              <svg id="sp-duesoon" viewBox="0 0 72 36" preserveAspectRatio="none">
                <path class="sp-fill" id="sp-duesoon-fill" d="" fill="#d97706" opacity=".12"/>
                <path class="sp-line" id="sp-duesoon-line" d="" stroke="#d97706"/>
                <circle class="sp-dot" id="sp-duesoon-dot" r="2.5" fill="#d97706"/>
              </svg>
            </div>
          </div>
        </div>
        <div class="fin-card" id="xc-revenue" data-filter="revenue" onclick="custCardClick(this)">
          <div class="xc-lbl">Revenue</div>
          <div class="xc-body">
            <div class="xc-left">
              <span class="xc-val" id="scRevenue">0</span>
              <div class="xc-meta">
                <span class="xc-pill xc-green" id="xp-revenue">—</span>
                <span class="xc-sub">Estimasi bulan ini</span>
              </div>
            </div>
            <div class="xc-spark">
              <svg id="sp-revenue" viewBox="0 0 72 36" preserveAspectRatio="none">
                <path class="sp-fill" id="sp-revenue-fill" d="" fill="#16a34a" opacity=".12"/>
                <path class="sp-line" id="sp-revenue-line" d="" stroke="#16a34a"/>
                <circle class="sp-dot" id="sp-revenue-dot" r="2.5" fill="#16a34a"/>
              </svg>
            </div>
          </div>
        </div>
      </div>

      <!-- ══ TABLE CARD ══ -->
      <div class="w-card a5">
        <div style="display:flex;align-items:center;gap:10px;padding:18px 20px 0;flex-wrap:wrap">
          <div class="search-wrap" style="flex:1;max-width:300px">
            <svg class="si" width="15" height="15" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><circle cx="11" cy="11" r="8"/><path d="m21 21-4.35-4.35"/></svg>
            <input type="text" id="searchCustomer" placeholder="Cari nama, CID, HP, paket…">
            <div id="srchSpin" style="display:none;position:absolute;right:10px;top:50%;transform:translateY(-50%)">
              <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="var(--d-blue1)" stroke-width="2.5" style="animation:spin .6s linear infinite"><path d="M12 2v4M12 18v4M4.93 4.93l2.83 2.83M16.24 16.24l2.83 2.83M2 12h4M18 12h4M4.93 19.07l2.83-2.83M16.24 7.76l2.83-2.83"/></svg>
            </div>
          </div>
          <select id="filterStatus" class="filter-select">
            <option value="">Semua Status</option>
            <option value="active">Aktif</option>
            <option value="inactive">Nonaktif</option>
            <option value="suspended">Suspended</option>
            <option value="overdue">Overdue</option>
            <option value="due_soon">Jatuh Tempo 3 Hari</option>
          </select>
          <select id="filterDue" class="filter-select">
            <option value="">Semua Due</option>
            <option value="overdue">Overdue</option>
            <option value="soon">Due 3 Hari</option>
            <option value="none">Tanpa Due Date</option>
          </select>
          <!-- ── Filter area berjenjang ── -->
          <select id="filterProvince" class="filter-select" onchange="onFilterProvinceChange()" title="Filter Provinsi">
            <option value="">Semua Provinsi</option>
          </select>
          <select id="filterRegency" class="filter-select" onchange="onFilterRegencyChange()" title="Filter Kabupaten/Kota" disabled>
            <option value="">Semua Kab/Kota</option>
          </select>
          <select id="filterDistrict" class="filter-select" onchange="loadCustomers()" title="Filter Kecamatan" disabled>
            <option value="">Semua Kecamatan</option>
          </select>
          <button type="button" onclick="backfillAreas()" id="backfillAreaBtn" class="filter-select" title="Isi data area customer lama dari teks alamat"
            style="cursor:pointer;background:#eff6ff;border-color:#bfdbfe;color:#1d4ed8;display:inline-flex;align-items:center;gap:6px;white-space:nowrap">
            <svg width="14" height="14" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"/></svg>
            Isi Area Lama
          </button>
          <div style="font-size:12.5px;color:var(--d-muted);margin-left:auto" id="customerCount">–</div>
        </div>

        <!-- Table -->
        <div style="overflow-x:auto;margin-top:14px">
          <table class="cust-table">
            <thead>
              <tr>
                <th>CID</th>
                <th>Nama</th>
                <th class="col-hide-mob">No HP</th>
                <th>Paket</th>
                <th class="col-hide-mob">Biaya/Bln</th>
                <th class="col-hide-mob">Aktivasi</th>
                <th>Jatuh Tempo</th>
                <th>Status</th>
                <th style="text-align:right;padding-right:20px">Aksi</th>
              </tr>
            </thead>
            <tbody id="customerTable">
              <tr><td colspan="9" style="text-align:center;padding:52px;color:var(--d-muted)">
                <div style="font-size:34px;margin-bottom:10px">📋</div>
                <div style="font-size:14px;font-weight:600;color:var(--d-text)">Memuat data…</div>
              </td></tr>
            </tbody>
          </table>
        </div>

        <!-- Pagination -->
        <div id="customerPagination" style="display:flex;align-items:center;justify-content:space-between;padding:16px 20px;border-top:1px solid var(--d-border)"></div>
      </div>

    </main>
  </div>
</div>

<!-- ════ MODAL Add/Edit ════ -->
<div id="customerModal" class="modal-ov">
  <div class="modal-bx">
    <div class="modal-handle"></div>

    <!-- Sticky header -->
    <div class="modal-hdr">
      <div id="customerModalTitle" class="modal-ttl">Tambah Customer</div>
      <button onclick="closeModal()" class="modal-cls" aria-label="Tutup">✕</button>
    </div>

    <!-- Scrollable body -->
    <div class="modal-body" id="custModalBody">
      <div class="form-grid-2">
        <div class="form-sec"><span class="form-sec-t">Data Pelanggan</span></div>
        <div class="form-field">
          <label class="flbl">CID *</label>
          <input type="text" class="finput" id="custId" placeholder="KOSONGKAN → OTOMATIS" style="font-family:'DM Mono',monospace;text-transform:uppercase" oninput="onCustIdInput(this.value)" autocomplete="off">
        </div>
        <div class="form-field">
          <label class="flbl">Status</label>
          <select class="finput" id="custStatus">
            <option value="active">Aktif</option>
            <option value="inactive">Nonaktif</option>
            <option value="suspended">Suspended</option>
          </select>
        </div>

        <div class="form-field span-all">
          <label class="flbl">Nama Lengkap *</label>
          <input type="text" class="finput" id="custName" placeholder="Nama customer">
        </div>

        <div class="form-field">
          <label class="flbl">No HP / WA *</label>
          <input type="text" class="finput" id="custPhone" placeholder="08xx / 628xx">
        </div>
        <div class="form-field">
          <label class="flbl">Email</label>
          <input type="email" class="finput" id="custEmail" placeholder="opsional">
        </div>

        <div class="form-sec"><span class="form-sec-t">Layanan &amp; Isolir</span><span class="form-sec-hint">Paket, jatuh tempo, dan konfigurasi isolir</span></div>
        <div class="form-field">
          <label class="flbl">Paket</label>
          <select class="finput" id="custPackage"><option value="">Pilih paket</option></select>
        </div>
        <div class="form-field">
          <label class="flbl">Jatuh Tempo <span class="form-meta">(tanggal)</span></label>
          <input type="date" class="finput" id="custDueDate">
          <div class="form-hint">Sumber data reminder otomatis</div>
          <div class="form-hint" id="dueDateHint">Contoh: 15 → jatuh tempo tiap tgl 15</div>
        </div>

        <div class="form-field">
          <label class="flbl">Tanggal Aktivasi</label>
          <input type="date" class="finput" id="custInstallDate" onchange="onActivationDateChange()">
        </div>
        <div class="form-field">
          <label class="flbl">ONT Serial Number</label>
          <input type="text" class="finput" id="custOntSn" placeholder="HWTC12345678">
        </div>

        <div class="form-field">
          <label class="flbl">Static IP <span class="form-badge">(isolir)</span></label>
          <input type="text" class="finput" id="custStaticIP" placeholder="192.168.1.x">
          <div class="form-hint">IP statis pelanggan di MikroTik</div>
        </div>
        <div class="form-field">
          <label class="flbl">Router MikroTik <span class="form-badge">(isolir)</span></label>
          <select class="finput" id="custMikrotikId"><option value="">— Pilih router —</option></select>
        </div>

        <div class="form-field">
          <label class="flbl">Tipe Koneksi <span class="form-badge">(isolir)</span></label>
          <select class="finput" id="custConnType" onchange="onConnTypeChange()">
            <option value="">— Auto-detect —</option>
            <option value="pppoe">PPPoE</option>
            <option value="static">Static IP (Firewall)</option>
            <option value="hotspot">Hotspot (IP Binding)</option>
          </select>
          <div class="form-hint">Menentukan metode isolir. "Hotspot" pakai disable/enable IP Binding.</div>
        </div>
        <div class="form-field" id="custMacField" style="display:none">
          <label class="flbl">MAC Address <span class="form-badge">(hotspot)</span></label>
          <input type="text" class="finput" id="custMacAddress" placeholder="AA:BB:CC:DD:EE:FF" style="font-family:'DM Mono',monospace;text-transform:uppercase">
          <div class="form-hint">MAC perangkat pelanggan untuk IP Binding hotspot</div>
        </div>

        <!-- Slot khusus PPPoE Username di mode EDIT (JS akan move field ke sini saat edit) -->
        <div id="custPPPoEEditSlot" style="display:none" class="span-all">
          <label class="flbl">PPPoE Username
            <span class="form-meta">— mendukung format user@realm</span>
          </label>
        </div>
        <div id="custPppoeEditExtras" style="display:none" class="span-all">
          <label class="flbl">Password PPPoE</label>
          <div style="position:relative">
            <input type="text" class="finput" id="custPppoePasswordEdit" placeholder="Password PPPoE" autocomplete="off" style="padding-right:84px">
            <button type="button" onclick="generatePppoePassword('custPppoePasswordEdit')" style="position:absolute;right:6px;top:50%;transform:translateY(-50%);background:#2563eb;color:#fff;border:none;border-radius:6px;padding:6px 10px;font-size:11px;font-weight:600;cursor:pointer;">Generate</button>
          </div>
          <div class="form-hint">Isi username + password lalu Simpan, atau klik Aktifkan — akun dibuat di RADIUS (atau router jika RADIUS belum diset).</div>
          <button type="button" id="custActivatePppoeBtn" onclick="activateCustomerPppoe()" style="margin-top:8px;background:#065f46;color:#fff;border:none;border-radius:8px;padding:8px 12px;font-size:12px;font-weight:700;cursor:pointer;">Aktifkan akun PPPoE sekarang</button>
        </div>

        <div class="form-sec"><span class="form-sec-t">Alamat</span><span class="form-sec-hint">Wilayah dari data resmi — alamat lengkap tersusun otomatis</span></div>
        <div class="form-field span-all">
          <label class="flbl">No. KTP / NIK</label>
          <input type="text" class="finput" id="custNik" placeholder="16 digit" maxlength="16" inputmode="numeric" style="font-family:'DM Mono',monospace">
          <div class="form-hint">Nomor KTP pelanggan (opsional)</div>
        </div>

        <!-- ═══ Alamat berjenjang (Provinsi → Kab → Kec → Desa, dari API wilayah) ═══ -->
        <div class="form-field">
          <label class="flbl">Provinsi</label>
          <select class="finput" id="custProv" onchange="custOnProvChange()"><option value="">— pilih provinsi —</option></select>
        </div>
        <div class="form-field">
          <label class="flbl">Kabupaten / Kota</label>
          <select class="finput" id="custKab" onchange="custOnKabChange()" disabled><option value="">— pilih —</option></select>
        </div>
        <div class="form-field">
          <label class="flbl">Kecamatan</label>
          <select class="finput" id="custKec" onchange="custOnKecChange()" disabled><option value="">— pilih —</option></select>
        </div>
        <div class="form-field">
          <label class="flbl">Desa / Kelurahan</label>
          <select class="finput" id="custDesa" onchange="custComposeAddr()" disabled><option value="">— pilih —</option></select>
        </div>
        <div class="form-field">
          <label class="flbl">Nama Jalan</label>
          <input type="text" class="finput" id="custJalan" placeholder="Jl. ..." oninput="custComposeAddr()">
        </div>
        <div class="form-field">
          <label class="flbl">No. Rumah</label>
          <input type="text" class="finput" id="custNoRumah" placeholder="No." oninput="custComposeAddr()">
        </div>
        <div class="form-field">
          <label class="flbl">RT</label>
          <input type="text" class="finput" id="custRt" placeholder="001" inputmode="numeric" oninput="custComposeAddr()">
        </div>
        <div class="form-field">
          <label class="flbl">RW</label>
          <input type="text" class="finput" id="custRw" placeholder="002" inputmode="numeric" oninput="custComposeAddr()">
        </div>

        <div class="form-field span-all">
          <label class="flbl">Alamat Lengkap <span class="form-meta">— otomatis tersusun, bisa diedit</span></label>
          <textarea class="finput" id="custAddress" rows="2" placeholder="Alamat lengkap"></textarea>
        </div>

        <div class="form-sec"><span class="form-sec-t">Titik Lokasi</span><span class="form-sec-hint">Klik / geser pin di peta, atau isi koordinat manual</span></div>
        <div class="form-field">
          <label class="flbl">Latitude <span class="form-meta">— koordinat peta</span></label>
          <input type="text" class="finput" id="custLatitude" placeholder="-6.12345678" style="font-family:'DM Mono',monospace" oninput="custOnCoordInput()">
        </div>
        <div class="form-field">
          <label class="flbl">Longitude <span class="form-meta">— koordinat peta</span></label>
          <input type="text" class="finput" id="custLongitude" placeholder="106.12345678" style="font-family:'DM Mono',monospace" oninput="custOnCoordInput()">
        </div>
        <div class="form-field span-all" style="margin-top:-2px">
          <div class="cust-loc-actions">
            <button type="button" onclick="custUseMyLocation()" class="cust-loc-btn">
              <svg width="14" height="14" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><circle cx="12" cy="12" r="3"/><path d="M12 2v3M12 19v3M2 12h3M19 12h3"/><circle cx="12" cy="12" r="8"/></svg>
              Lokasi Saya
            </button>
            <button type="button" onclick="custGeocodeFromAddress(true)" class="cust-loc-btn" id="custGeoBtn" style="border-color:#c7d2fe;background:#eef2ff;color:#4338ca">
              <svg width="14" height="14" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M17.657 16.657L13.414 20.9a2 2 0 01-2.828 0l-4.243-4.243a8 8 0 1111.314 0z"/><circle cx="12" cy="11" r="3"/></svg>
              Cari dari alamat
            </button>
            <span id="custGeoHint" style="font-size:11.5px;color:#64748b"></span>
          </div>
          <div id="custMap" class="cust-map"></div>
          <div class="form-hint" style="margin-top:5px;font-size:11px;color:#94a3b8;line-height:1.45">
            Koordinat terisi otomatis saat memilih wilayah. Klik atau geser pin di peta untuk titik presisi rumah pelanggan.
          </div>
        </div>

        <div class="form-sec"><span class="form-sec-t">Infrastruktur</span><span class="form-sec-hint">Hubungkan ke ODP/ODC induk (opsional)</span></div>
        <div class="form-field span-all">
          <label class="flbl">Parent ODP <span class="form-meta">— ODP/ODC/POP induk (opsional)</span></label>
          <select class="finput" id="custInfraParentId">
            <option value="">— Tanpa parent —</option>
          </select>
          <div class="form-hint" style="margin-top:4px;font-size:11px;color:#64748b;line-height:1.45;">
            Pilih ODP/ODC/POP induk customer ini. Saat customer disimpan dengan koordinat,
            sistem otomatis menambahkan titik di halaman <strong>Infrastruktur</strong> dan menghubungkannya ke parent ini.
          </div>
        </div>

        <!-- Info banner: auto-sync ke peta infrastruktur -->
        <div class="form-field span-all" id="custAutoInfraInfo" style="margin-top:2px;padding:10px 12px;background:#ecfdf5;border:1px solid #a7f3d0;border-radius:10px;display:none;">
          <div style="display:flex;align-items:flex-start;gap:9px;">
            <svg width="16" height="16" fill="none" stroke="#059669" stroke-width="2.2" viewBox="0 0 24 24" style="flex-shrink:0;margin-top:1px;"><path stroke-linecap="round" stroke-linejoin="round" d="M9 20l-5.447-2.724A1 1 0 013 16.382V5.618a1 1 0 011.447-.894L9 7m0 13l6-3m-6 3V7m6 10l4.553 2.276A1 1 0 0021 18.382V7.618a1 1 0 00-.553-.894L15 4m0 13V4m0 0L9 7"/></svg>
            <div style="font-size:11.5px;color:#065f46;line-height:1.5;">
              <strong>Auto-tandai di peta:</strong> Customer dengan koordinat akan otomatis muncul sebagai titik di halaman <strong>Infrastruktur</strong> — tidak perlu menambahkannya manual.
            </div>
          </div>
        </div>
      </div>

      <!-- ═══ PPPoE Account Creation Panel ═══ -->
      <div id="pppoeCreateBox" style="margin-top:18px;padding:14px;background:#eff6ff;border:1.5px solid #bfdbfe;border-radius:12px;">
        <label style="display:flex;align-items:flex-start;gap:10px;cursor:pointer;margin:0;">
          <input type="checkbox" id="custCreatePppoe" style="width:18px;height:18px;accent-color:#2563eb;cursor:pointer;flex-shrink:0;margin-top:2px;">
          <div style="flex:1;min-width:0;">
            <div style="font-size:13px;font-weight:700;color:#1e40af;display:flex;align-items:center;gap:6px;">
              <svg width="14" height="14" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M5 12h14M12 5l7 7-7 7"/></svg>
              Buat akun PPPoE (RADIUS, seperti BillingRadius)
            </div>
            <div style="font-size:11px;color:#1d4ed8;margin-top:2px;line-height:1.4;">
              Satu simpan = data pelanggan + user dial. Kalau server RADIUS aktif, akun masuk <code>radcheck</code> (bukan /ppp/secret). Tanpa RADIUS, fallback ke secret MikroTik.
            </div>
          </div>
        </label>

        <!-- Form PPPoE — muncul saat checkbox aktif -->
        <div id="pppoeFormBox" style="display:none;margin-top:14px;padding-top:14px;border-top:1px dashed #93c5fd;">
          <div class="form-grid-2">
            <div class="form-field span-all">
              <label class="flbl" style="display:flex;align-items:center;gap:6px;">
                Router MikroTik untuk PPPoE *
                <span id="pppoeRouterSpinner" style="display:none;font-size:10px;color:#64748b;font-weight:400;text-transform:none">⏳ memuat...</span>
              </label>
              <select class="finput" id="custPppoeRouter">
                <option value="">— Memuat daftar router —</option>
              </select>
              <div class="form-hint">Daftar router diambil dari halaman <strong>/devices</strong> (tipe Router, status aktif)</div>
            </div>
            <div class="form-field span-all">
              <label class="flbl" style="display:flex;align-items:center;gap:6px;">
                Profile PPPoE *
                <span id="pppoeProfileSpinner" style="display:none;font-size:10px;color:#64748b;font-weight:400;text-transform:none">⏳ memuat...</span>
              </label>
              <select class="finput" id="custPppoeProfile">
                <option value="">— Pilih router dulu —</option>
              </select>
              <div class="form-hint">Daftar profile diambil langsung dari router yang dipilih</div>
            </div>
            <div id="custPPPoEAddSlot" class="form-field span-all">
              <label class="flbl" style="display:flex;align-items:center;gap:6px;">
                Username PPPoE *
                <span class="form-meta">auto dari Nama, bisa diubah</span>
              </label>
              <input type="text" class="finput" id="custPPPoE" placeholder="pppoe-username" autocomplete="off">
              <div class="form-hint">Otomatis terisi dari Nama Customer. Bisa di-edit manual (mendukung format <strong>user@realm</strong>).</div>
            </div>
            <div class="form-field">
              <label class="flbl">Password PPPoE *</label>
              <div style="position:relative">
                <input type="text" class="finput" id="custPppoePassword" placeholder="Password" autocomplete="off" style="padding-right:84px">
                <button type="button" onclick="generatePppoePassword()" style="position:absolute;right:6px;top:50%;transform:translateY(-50%);background:#2563eb;color:#fff;border:none;border-radius:6px;padding:6px 10px;font-size:11px;font-weight:600;cursor:pointer;">Generate</button>
              </div>
            </div>
            <div class="form-field">
              <label class="flbl">Service</label>
              <select class="finput" id="custPppoeService">
                <option value="pppoe" selected>pppoe</option>
                <option value="any">any</option>
                <option value="l2tp">l2tp</option>
                <option value="pptp">pptp</option>
                <option value="sstp">sstp</option>
                <option value="ovpn">ovpn</option>
              </select>
            </div>
            <div class="form-field span-all">
              <label class="flbl">Local Address <span class="form-meta">(opsional)</span></label>
              <input type="text" class="finput" id="custPppoeLocalAddr" placeholder="Misal: 10.10.10.1" style="font-family:'DM Mono',monospace;font-size:12px">
            </div>
            <div class="form-field span-all">
              <label class="flbl">Remote Address <span class="form-meta">(opsional, kosongkan = pakai pool dari profile)</span></label>
              <input type="text" class="finput" id="custPppoeRemoteAddr" placeholder="Misal: 10.10.10.100" style="font-family:'DM Mono',monospace;font-size:12px">
            </div>
          </div>
          <div id="pppoeFormHint" style="margin-top:10px;padding:8px 10px;background:#fff;border-radius:6px;font-size:11px;color:#475569;line-height:1.5;display:none;"></div>
        </div>
      </div>

      <!-- ═══ Akses Portal Pelanggan (mode EDIT saja) ═══ -->
      <div id="portalPanelBox" style="display:none;margin-top:18px;padding:14px;background:#f8fafd;border:1.5px solid #e4ecf7;border-radius:12px;">
        <div style="display:flex;align-items:center;gap:8px;margin-bottom:3px;">
          <svg width="15" height="15" fill="none" stroke="#1a6ef5" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M15 7a2 2 0 012 2m4 0a6 6 0 01-7.743 5.743L11 17H9v2H7v2H4a1 1 0 01-1-1v-2.586a1 1 0 01.293-.707l5.964-5.964A6 6 0 1121 9z"/></svg>
          <div style="font-size:13px;font-weight:800;color:#0d1b3e;">Akses Portal Pelanggan</div>
        </div>
        <div style="font-size:11.5px;color:#64748b;margin-bottom:12px;">
          Kredensial login Customer Portal (terpisah dari PPPoE).
        </div>

        <div style="display:grid;grid-template-columns:1fr;gap:10px;">
          <!-- Login ID -->
          <div>
            <label class="flbl">Customer ID (Login ID Portal)</label>
            <div style="display:flex;gap:8px;align-items:stretch;">
              <input class="finput" id="ppCid" placeholder="CST00001" readonly
                     style="flex:1;font-family:'DM Mono',ui-monospace,monospace;letter-spacing:.02em;background:#eef2f7;cursor:not-allowed;">
              <button type="button" id="ppCidToggle" onclick="ppToggleCidEdit()"
                      style="padding:0 14px;border:1.5px solid #e4ecf7;border-radius:8px;background:#fff;color:#1a6ef5;font-size:12px;font-weight:700;cursor:pointer;font-family:'DM Sans',sans-serif;white-space:nowrap;">
                Ubah
              </button>
            </div>
            <div id="ppCidWarn" style="display:none;font-size:11px;color:#b45309;background:#fffbeb;border:1px solid #fde68a;padding:6px 10px;border-radius:8px;margin-top:6px;">
              ⚠ Mengubah Customer ID akan mengubah login ID pelanggan. Data historis tetap aman (invoice, dll terhubung via internal ID).
            </div>
          </div>

          <!-- Password -->
          <div>
            <label class="flbl">Password Baru <span style="font-size:10px;font-weight:400;text-transform:none;color:var(--d-muted)">(kosongkan jika tidak diubah)</span></label>
            <div style="display:flex;gap:8px;align-items:stretch;">
              <div style="flex:1;position:relative;">
                <input class="finput" id="ppPw" type="password" placeholder="Min. 6 karakter"
                       style="font-family:'DM Mono',ui-monospace,monospace;letter-spacing:.04em;padding-right:42px;">
                <button type="button" onclick="ppTogglePwVisibility()" id="ppPwEye"
                        style="position:absolute;right:8px;top:50%;transform:translateY(-50%);background:none;border:none;cursor:pointer;color:#94a3b8;padding:4px;display:flex;align-items:center;justify-content:center;"
                        title="Tampilkan/sembunyikan">
                  <svg width="16" height="16" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"/><path stroke-linecap="round" stroke-linejoin="round" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"/></svg>
                </button>
              </div>
              <button type="button" onclick="ppGeneratePassword()"
                      style="padding:0 14px;border:1.5px solid #e4ecf7;border-radius:8px;background:#fff;color:#1a6ef5;font-size:12px;font-weight:700;cursor:pointer;font-family:'DM Sans',sans-serif;white-space:nowrap;display:flex;align-items:center;gap:5px;">
                <svg width="12" height="12" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"/></svg>
                Generate
              </button>
            </div>
            <div id="ppPwHint" style="font-size:11px;color:#64748b;margin-top:5px;">
              <!-- diisi runtime -->
            </div>
          </div>

          <!-- Toggle aktif + Reset -->
          <div style="display:flex;align-items:center;justify-content:space-between;gap:10px;flex-wrap:wrap;padding-top:6px;border-top:1px dashed #e4ecf7;">
            <label style="display:flex;align-items:center;gap:8px;cursor:pointer;user-select:none;margin:0;">
              <input type="checkbox" id="ppEnabled" style="width:16px;height:16px;accent-color:#1a6ef5;cursor:pointer;">
              <span style="font-size:12.5px;font-weight:600;color:#0d1b3e;">Akses portal aktif</span>
              <span style="font-size:11px;color:#64748b;">— Pelanggan bisa login</span>
            </label>
            <button type="button" onclick="ppResetPasswordToPhone()"
                    style="padding:7px 14px;border:1.5px solid #fecaca;border-radius:8px;background:#fff;color:#dc2626;font-size:11.5px;font-weight:700;cursor:pointer;font-family:'DM Sans',sans-serif;">
              Reset ke No. HP
            </button>
          </div>

          <div id="ppLastLogin" style="font-size:11px;color:#94a3b8;">
            <!-- diisi runtime -->
          </div>
        </div>
      </div>

      <div id="waWelcomeBox" style="margin-top:18px;padding:12px 14px;background:#f0fdf4;border:1.5px solid #bbf7d0;border-radius:10px;display:flex;align-items:center;gap:12px;">
        <label style="display:flex;align-items:center;gap:10px;cursor:pointer;flex:1;margin:0;">
          <input type="checkbox" id="custSendWaWelcome" checked style="width:18px;height:18px;accent-color:#16a34a;cursor:pointer;flex-shrink:0;">
          <div style="flex:1;min-width:0;">
            <div style="font-size:13px;font-weight:700;color:#15803d;display:flex;align-items:center;gap:6px;">
              <svg width="14" height="14" fill="currentColor" viewBox="0 0 24 24"><path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z"/></svg>
              Kirim Notifikasi WhatsApp Selamat Datang
            </div>
            <div style="font-size:11px;color:#65a30d;margin-top:2px;line-height:1.4;">
              Pesan welcome akan dikirim otomatis ke nomor WA pelanggan setelah disimpan.
              Edit format pesan di <a href="/wa/templates" target="_blank" style="color:#15803d;font-weight:600;text-decoration:underline;">Template Pesan</a>.
            </div>
          </div>
        </label>
      </div>

      <div id="emailWelcomeBox" style="margin-top:12px;padding:12px 14px;background:#eff6ff;border:1.5px solid #bfdbfe;border-radius:10px;display:flex;align-items:center;gap:12px;">
        <label style="display:flex;align-items:center;gap:10px;cursor:pointer;flex:1;margin:0;">
          <input type="checkbox" id="custSendEmailWelcome" checked style="width:18px;height:18px;accent-color:#2563eb;cursor:pointer;flex-shrink:0;">
          <div style="flex:1;min-width:0;">
            <div style="font-size:13px;font-weight:700;color:#1d4ed8;display:flex;align-items:center;gap:6px;">
              <svg width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M4 4h16a2 2 0 012 2v12a2 2 0 01-2 2H4a2 2 0 01-2-2V6a2 2 0 012-2z"/><path stroke-linecap="round" stroke-linejoin="round" d="M22 6l-10 7L2 6"/></svg>
              Kirim Notifikasi Email Selamat Datang
            </div>
            <div style="font-size:11px;color:#3b82f6;margin-top:2px;line-height:1.4;">
              Email welcome akan dikirim otomatis ke alamat email pelanggan setelah disimpan.
              Edit isi email di <a href="/email-template" target="_blank" style="color:#1d4ed8;font-weight:600;text-decoration:underline;">Email Template</a>.
            </div>
          </div>
        </label>
      </div>

    </div><!-- /modal-body -->

    <!-- Sticky footer with actions -->
    <div class="modal-foot">
      <button onclick="closeModal()" class="btn-fin btn-ghost" style="padding:11px 20px">Batal</button>
      <button onclick="saveCustomerBtn()" class="btn-fin btn-primary-fin" id="saveCustomerBtn" style="flex:1;max-width:240px;justify-content:center;padding:11px 18px">
        <svg width="14" height="14" fill="none" stroke="white" stroke-width="2.5" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7"/></svg>
        Simpan Customer
      </button>
    </div>
  </div>
</div>

<!-- hidden stubs -->
<div style="display:none">
  <span id="scActive"></span><span id="scActiveSub"></span><span id="scInactive"></span><span id="scNoDue"></span>
  <div id="scTotalBar"></div>
</div>

<script src="https://cdn.jsdelivr.net/npm/socket.io-client@4.7.2/dist/socket.io.min.js"></script>
<script src="/js/app.js"></script>
<script src="/js/customers.js?v=1779700000"></script>
<script>
/* ── Extra stat update from loadCustomerStats ── */
const _origStats = window.loadCustomerStats;

/* applyFilter — set dropdown + reload */
window.applyFilter = function(status) {
  const sel = document.getElementById('filterStatus');
  if (sel) sel.value = status;
  window._custPage = 1;
  if (typeof loadCustomers === 'function') loadCustomers();
};

/* custCardClick — active state + filter sync */
window.custCardClick = function(el) {
  const COLORS = { total:'#2563eb', overdue:'#c2410c', duesoon:'#d97706', revenue:'#16a34a' };
  const FILTER  = { total:'', overdue:'overdue', duesoon:'due_soon', revenue:'' };
  const key = el.id.replace('xc-','');
  const was = el.classList.contains('fc-active');

  document.querySelectorAll('.fin-card[data-filter]').forEach(card => {
    card.classList.remove('fc-active');
    const k   = card.id.replace('xc-','');
    const col = COLORS[k] || '#64748b';
    const l = card.querySelector('.sp-line'), f = card.querySelector('.sp-fill'), d = card.querySelector('.sp-dot');
    if(l) l.style.stroke = col;
    if(f) f.style.fill   = col;
    if(d) d.style.fill   = col;
  });

  if (!was) {
    el.classList.add('fc-active');
    const l = el.querySelector('.sp-line'), f = el.querySelector('.sp-fill'), d = el.querySelector('.sp-dot');
    if(l) l.style.stroke = 'rgba(255,255,255,.70)';
    if(f) f.style.fill   = 'rgba(255,255,255,.12)';
    if(d) d.style.fill   = '#fff';
  }

  const sel = document.getElementById('filterStatus');
  if (sel) sel.value = was ? '' : (FILTER[key] || '');
  window._custPage = 1;
  if (typeof loadCustomers === 'function') loadCustomers();
};

/* Modal open/close shim — customers.js already handles openAddCustomer/closeModal */
window.saveCustomerBtn = function() {
  const btn = document.getElementById('saveCustomerBtn');
  if (typeof saveCustomer === 'function') saveCustomer();
};

/* ── Customer name link → profile page ── */
(function patchRender() {
  const orig = window.loadCustomers;
  if (typeof orig !== 'function') { setTimeout(patchRender, 100); return; }
  window.loadCustomers = async function() {
    await orig.apply(this, arguments);
    // Patch name links to go to customer_profile page
    document.querySelectorAll('#customerTable tr[data-id]').forEach(tr => {
      const id = tr.dataset.id;
      const nameDiv = tr.querySelector('td:nth-child(2) [data-name]');
      // Already patched by customers.js new template — handled below
    });
  };
})();

/* Search auto-submit */
(function(){
  const s  = document.getElementById('searchCustomer');
  const fs = document.getElementById('filterStatus');
  const fd = document.getElementById('filterDue');
  let t = null;
  function reload() { window._custPage = 1; if(typeof loadCustomers==='function') loadCustomers(); }
  if(s)  s.addEventListener('input', function(){ clearTimeout(t); document.getElementById('srchSpin').style.display='block'; t=setTimeout(()=>{ document.getElementById('srchSpin').style.display='none'; reload(); }, 450); });
  if(fs) fs.addEventListener('change', reload);
  if(fd) fd.addEventListener('change', reload);
})();
</script>

<script>
(function(){
  const W=72,H=36,MAX=20,hist={},C={'total':'#2563eb','overdue':'#c2410c','duesoon':'#d97706','revenue':'#16a34a'},IDS={'total':'scTotal','overdue':'scOverdue','duesoon':'scDueSoon','revenue':'scRevenue'};
  Object.keys(C).forEach(k=>hist[k]=[]);
  function sp(v){if(!v||v.length<2)return null;const mn=Math.min(...v),mx=Math.max(...v),r=mx-mn||1;const p=v.map((n,i)=>[(i/(v.length-1))*W,H-((n-mn)/r)*(H-6)-3]);const d=p.reduce((a,[x,y],i)=>{if(!i)return'M'+x.toFixed(1)+','+y.toFixed(1);const[px,py]=p[i-1],cx=(px+x)/2;return a+' C'+cx.toFixed(1)+','+py.toFixed(1)+' '+cx.toFixed(1)+','+y.toFixed(1)+' '+x.toFixed(1)+','+y.toFixed(1);},'');const lp=p[p.length-1],fp=p[0];return{d,fill:d+' L'+lp[0].toFixed(1)+','+H+' L'+fp[0].toFixed(1)+','+H+' Z',dot:lp};}
  function draw(id,v){const s=sp(v);if(!s)return;const f=document.getElementById(id+'-fill'),l=document.getElementById(id+'-line'),d=document.getElementById(id+'-dot');if(f)f.setAttribute('d',s.fill);if(l)l.setAttribute('d',s.d);if(d){d.setAttribute('cx',s.dot[0].toFixed(1));d.setAttribute('cy',s.dot[1].toFixed(1));}}
  function rn(id){const e=document.getElementById(id);return e?parseFloat(e.textContent.replace(/[^\d.]/g,''))||0:0;}
  function upd(){Object.keys(C).forEach(k=>{const v=rn(IDS[k]);hist[k].push(v);if(hist[k].length>MAX)hist[k].shift();draw('sp-'+k,hist[k]);const p=document.getElementById('xp-'+k);if(p)p.textContent=v;});}
  window._custUpd=upd;
  Object.values(IDS).forEach(id=>{const e=document.getElementById(id);if(e)new MutationObserver(()=>setTimeout(upd,50)).observe(e,{childList:true,characterData:true,subtree:true});});
  document.addEventListener('DOMContentLoaded',upd);
})();
</script>

<!-- ── Import/Export Dropdown — di body level untuk hindari stacking context conflict ── -->
<style>
  #importExportMenu {
    display: none;
    position: fixed;
    background: #ffffff;
    border: 1px solid #e2e8f0;
    border-radius: 12px;
    box-shadow: 0 12px 32px rgba(15, 23, 42, 0.18), 0 2px 6px rgba(15, 23, 42, 0.08);
    min-width: 280px;
    z-index: 100000;
    padding: 6px;
    font-family: 'DM Sans', sans-serif;
  }
  #importExportMenu.show {
    display: block;
    animation: impexpFadeDown .14s ease;
  }
  @keyframes impexpFadeDown {
    from { opacity: 0; transform: translateY(-6px); }
    to   { opacity: 1; transform: translateY(0); }
  }
  #importExportMenu .impexp-item {
    display: flex;
    align-items: center;
    gap: 11px;
    width: 100%;
    padding: 10px 12px;
    border: 0;
    background: transparent;
    cursor: pointer;
    font-family: 'DM Sans', sans-serif;
    text-align: left;
    border-radius: 8px;
    transition: background .12s;
  }
  #importExportMenu .impexp-item:hover { background: #f8fafc; }
  #importExportMenu .impexp-item:active { background: #f1f5f9; }
  #importExportMenu .impexp-icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 32px;
    height: 32px;
    border-radius: 8px;
    flex-shrink: 0;
  }
  #importExportMenu .impexp-text {
    display: flex;
    flex-direction: column;
    gap: 1px;
    flex: 1;
    min-width: 0;
  }
  #importExportMenu .impexp-title {
    font-size: 13px;
    font-weight: 600;
    color: #0f172a;
    line-height: 1.3;
  }
  #importExportMenu .impexp-sub {
    font-size: 11.5px;
    color: #64748b;
    line-height: 1.3;
  }
  #importExportMenu .impexp-divider {
    height: 1px;
    background: #f1f5f9;
    margin: 4px 8px;
  }
</style>
<div id="importExportMenu" role="menu">
  <button type="button" class="impexp-item" onclick="exportCustomers()">
    <span class="impexp-icon" style="background:#dbeafe;">
      <svg width="14" height="14" fill="none" stroke="#1e40af" stroke-width="2.2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4M7 10l5 5 5-5M12 15V3"/></svg>
    </span>
    <span class="impexp-text">
      <span class="impexp-title">Export ke Excel</span>
      <span class="impexp-sub">Download semua data customer</span>
    </span>
  </button>
  <button type="button" class="impexp-item" onclick="openImportModal()">
    <span class="impexp-icon" style="background:#dcfce7;">
      <svg width="14" height="14" fill="none" stroke="#15803d" stroke-width="2.2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M3 15v4a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-4M17 8l-5-5-5 5M12 3v12"/></svg>
    </span>
    <span class="impexp-text">
      <span class="impexp-title">Import dari Excel</span>
      <span class="impexp-sub">Upload file .xlsx</span>
    </span>
  </button>
  <div class="impexp-divider"></div>
  <button type="button" class="impexp-item" onclick="downloadTemplate()">
    <span class="impexp-icon" style="background:#f1f5f9;">
      <svg width="14" height="14" fill="none" stroke="#475569" stroke-width="2.2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><path d="M14 2v6h6M16 13H8M16 17H8M10 9H8"/></svg>
    </span>
    <span class="impexp-text">
      <span class="impexp-title">Download Template</span>
      <span class="impexp-sub">Format kosong .xlsx</span>
    </span>
  </button>
  <div class="impexp-divider"></div>
  <button type="button" class="impexp-item" onclick="generateAllPaymentLinks()">
    <span class="impexp-icon" style="background:#ccfbf1;">
      <svg width="14" height="14" fill="none" stroke="#0f766e" stroke-width="2.2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M13.828 10.172a4 4 0 010 5.656l-3 3a4 4 0 01-5.656-5.656l1.5-1.5m6.656-1.828l3-3a4 4 0 00-5.656-5.656l-1.5 1.5"/></svg>
    </span>
    <span class="impexp-text">
      <span class="impexp-title">Buat Link Pembayaran Massal</span>
      <span class="impexp-sub">Untuk pelanggan yang belum punya link</span>
    </span>
  </button>
</div>

<!-- ── Import / Export Modal ─────────────────────────────────────────── -->
<div id="importModal" style="display:none;position:fixed;inset:0;background:rgba(15,23,42,.6);z-index:9999;align-items:center;justify-content:center;padding:20px;">
  <div style="background:#fff;border-radius:14px;max-width:920px;width:100%;max-height:92vh;overflow:hidden;display:flex;flex-direction:column;box-shadow:0 24px 64px rgba(15,23,42,.22), 0 4px 12px rgba(15,23,42,.08);">
    <div style="padding:18px 22px;border-bottom:1px solid #f1f5f9;display:flex;align-items:flex-start;justify-content:space-between;gap:12px;">
      <div style="display:flex;align-items:center;gap:12px;">
        <div style="width:36px;height:36px;border-radius:9px;background:#eff6ff;display:flex;align-items:center;justify-content:center;flex-shrink:0;">
          <svg width="18" height="18" fill="none" stroke="#1a6ef5" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M3 15v4a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-4M17 8l-5-5-5 5M12 3v12"/></svg>
        </div>
        <div>
          <h2 style="margin:0;font-size:16px;font-weight:600;color:#0f172a;letter-spacing:-.01em;">Import customer dari Excel</h2>
          <p style="margin:2px 0 0;font-size:12.5px;color:#64748b;" id="importModalSubtitle">Upload file .xlsx — preview dulu sebelum import.</p>
        </div>
      </div>
      <button onclick="closeImportModal()" style="background:none;border:0;cursor:pointer;color:#94a3b8;padding:6px;border-radius:8px;display:flex;align-items:center;justify-content:center;transition:background .12s;" onmouseover="this.style.background='#f1f5f9'" onmouseout="this.style.background='none'">
        <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2.2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12"/></svg>
      </button>
    </div>

    <div id="importStep1" style="padding:22px;overflow:auto;">
      <!-- Step 1: Upload File -->
      <div id="importDropArea"
        ondragover="event.preventDefault();this.style.borderColor='#1a6ef5';this.style.background='#f0f7ff';"
        ondragleave="this.style.borderColor='#cbd5e1';this.style.background='#fafbfc';"
        ondrop="handleImportDrop(event)"
        onclick="document.getElementById('importFileInput').click()"
        style="border:2px dashed #cbd5e1;border-radius:12px;padding:36px 20px;text-align:center;cursor:pointer;transition:all .15s;background:#fafbfc;">
        <div style="width:48px;height:48px;border-radius:12px;background:#eff6ff;display:flex;align-items:center;justify-content:center;margin:0 auto 12px;">
          <svg width="22" height="22" fill="none" stroke="#1a6ef5" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M3 15v4a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-4M17 8l-5-5-5 5M12 3v12"/></svg>
        </div>
        <div style="font-size:14px;font-weight:600;color:#0f172a;margin-bottom:3px;">Klik atau seret file ke sini</div>
        <div style="font-size:12px;color:#64748b;">Format .xlsx atau .xls — maksimal 5 MB</div>
        <input type="file" id="importFileInput" accept=".xlsx,.xls" style="display:none;" onchange="handleImportFile(this.files[0])">
      </div>

      <!-- Info: Template -->
      <div style="margin-top:14px;padding:12px 14px;background:#fafbfc;border:1px solid #e2e8f0;border-radius:10px;display:flex;gap:10px;align-items:flex-start;">
        <svg width="16" height="16" fill="none" stroke="#64748b" stroke-width="2" viewBox="0 0 24 24" style="flex-shrink:0;margin-top:1px;"><path stroke-linecap="round" stroke-linejoin="round" d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><path stroke-linecap="round" stroke-linejoin="round" d="M14 2v6h6M16 13H8M16 17H8M10 9H8"/></svg>
        <div style="flex:1;font-size:12.5px;color:#475569;line-height:1.5;">
          <strong style="color:#0f172a;">Belum punya template?</strong>
          <a href="javascript:void(0)" onclick="downloadTemplate()" style="color:#1a6ef5;text-decoration:none;font-weight:600;margin-left:4px;">Download template Excel</a>
          yang sudah berisi header dan contoh data.
        </div>
      </div>

      <!-- Info: Auto-generate ID -->
      <div style="margin-top:8px;padding:12px 14px;background:#fafbfc;border:1px solid #e2e8f0;border-radius:10px;display:flex;gap:10px;align-items:flex-start;">
        <svg width="16" height="16" fill="none" stroke="#64748b" stroke-width="2" viewBox="0 0 24 24" style="flex-shrink:0;margin-top:1px;"><circle cx="12" cy="12" r="10"/><path stroke-linecap="round" stroke-linejoin="round" d="M12 8v4M12 16h.01"/></svg>
        <div style="flex:1;font-size:12.5px;color:#475569;line-height:1.5;">
          <strong style="color:#0f172a;">ID Pelanggan boleh dikosongkan</strong> — sistem akan auto-generate (CID001, CID002, ...) untuk pelanggan baru. Hanya kolom <strong>Nama</strong> yang wajib diisi.
        </div>
      </div>
    </div>

    <div id="importStep2" style="display:none;padding:22px;overflow:auto;flex:1;">
      <!-- Step 2: Preview & Confirm -->
      <div id="importSummary" style="display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin-bottom:16px;"></div>

      <!-- Router secret check banner — di-isi oleh renderImportPreview() saat preview render -->
      <div id="importRouterCheckBanner"></div>

      <div style="display:flex;align-items:center;gap:10px;margin-bottom:12px;padding:10px 12px;background:#fafbfc;border-radius:10px;">
        <svg width="14" height="14" fill="none" stroke="#64748b" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M4 6h16M4 12h16M4 18h7"/></svg>
        <label style="font-size:12.5px;font-weight:600;color:#0f172a;">Customer yang sudah ada:</label>
        <select id="importMode" style="padding:6px 10px;border-radius:7px;border:1px solid #e2e8f0;font-size:12.5px;background:#fff;color:#0f172a;font-family:inherit;cursor:pointer;">
          <option value="skip_duplicate">Lewati (skip)</option>
          <option value="update">Update data lama</option>
        </select>
      </div>

      <div style="border:1px solid #e2e8f0;border-radius:10px;overflow:hidden;max-height:400px;overflow-y:auto;">
        <table style="width:100%;border-collapse:collapse;font-size:12.5px;">
          <thead style="background:#fafbfc;position:sticky;top:0;z-index:1;">
            <tr>
              <th style="padding:10px 12px;text-align:left;border-bottom:1px solid #e2e8f0;font-weight:600;color:#64748b;font-size:11.5px;text-transform:uppercase;letter-spacing:.3px;">#</th>
              <th style="padding:10px 12px;text-align:left;border-bottom:1px solid #e2e8f0;font-weight:600;color:#64748b;font-size:11.5px;text-transform:uppercase;letter-spacing:.3px;">Status</th>
              <th style="padding:10px 12px;text-align:left;border-bottom:1px solid #e2e8f0;font-weight:600;color:#64748b;font-size:11.5px;text-transform:uppercase;letter-spacing:.3px;">ID</th>
              <th style="padding:10px 12px;text-align:left;border-bottom:1px solid #e2e8f0;font-weight:600;color:#64748b;font-size:11.5px;text-transform:uppercase;letter-spacing:.3px;">Nama</th>
              <th style="padding:10px 12px;text-align:left;border-bottom:1px solid #e2e8f0;font-weight:600;color:#64748b;font-size:11.5px;text-transform:uppercase;letter-spacing:.3px;">PPPoE</th>
              <th style="padding:10px 12px;text-align:left;border-bottom:1px solid #e2e8f0;font-weight:600;color:#64748b;font-size:11.5px;text-transform:uppercase;letter-spacing:.3px;">Static IP</th>
              <th style="padding:10px 12px;text-align:left;border-bottom:1px solid #e2e8f0;font-weight:600;color:#64748b;font-size:11.5px;text-transform:uppercase;letter-spacing:.3px;">Router</th>
              <th style="padding:10px 12px;text-align:left;border-bottom:1px solid #e2e8f0;font-weight:600;color:#64748b;font-size:11.5px;text-transform:uppercase;letter-spacing:.3px;">Phone</th>
              <th style="padding:10px 12px;text-align:left;border-bottom:1px solid #e2e8f0;font-weight:600;color:#64748b;font-size:11.5px;text-transform:uppercase;letter-spacing:.3px;">Paket</th>
              <th style="padding:10px 12px;text-align:left;border-bottom:1px solid #e2e8f0;font-weight:600;color:#64748b;font-size:11.5px;text-transform:uppercase;letter-spacing:.3px;">Parent ODP</th>
              <th style="padding:10px 12px;text-align:left;border-bottom:1px solid #e2e8f0;font-weight:600;color:#64748b;font-size:11.5px;text-transform:uppercase;letter-spacing:.3px;">Aksi</th>
              <th style="padding:10px 12px;text-align:left;border-bottom:1px solid #e2e8f0;font-weight:600;color:#64748b;font-size:11.5px;text-transform:uppercase;letter-spacing:.3px;">Catatan</th>
            </tr>
          </thead>
          <tbody id="importPreviewRows"></tbody>
        </table>
      </div>
    </div>

    <div id="importStep3" style="display:none;padding:28px;text-align:center;">
      <!-- Step 3: Result -->
      <div id="importResultContent"></div>
    </div>

    <div style="padding:14px 22px;border-top:1px solid #f1f5f9;display:flex;justify-content:flex-end;gap:8px;background:#fafbfc;">
      <button onclick="closeImportModal()" id="importCancelBtn" class="btn-fin" style="background:#fff;color:#475569;border:1px solid #e2e8f0;">Batal</button>
      <button onclick="importBackToStep1()" id="importBackBtn" class="btn-fin" style="background:#fff;color:#475569;border:1px solid #e2e8f0;display:none;align-items:center;gap:6px;">
        <svg width="13" height="13" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M15 18l-6-6 6-6"/></svg>
        Kembali
      </button>
      <button onclick="confirmImport()" id="importConfirmBtn" class="btn-fin btn-primary-fin" style="display:none;">Import sekarang</button>
    </div>
  </div>
</div>

<script>
// ── Import / Export Customer Logic ─────────────────────────────────
let _importToken = null;
let __importRouterIdToName = new Map(); // populated by renderImportPreview from routerCheck.routerIdToName
let _importValidCount = 0;

// Pindahkan dropdown ke body root saat halaman siap, supaya 100% lepas
// dari stacking context parent manapun.
(function ensureMenuAtBodyRoot() {
  const move = () => {
    const menu = document.getElementById('importExportMenu');
    if (menu && menu.parentElement !== document.body) {
      document.body.appendChild(menu);
    }
  };
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', move);
  } else {
    move();
  }
})();

// Toggle dropdown — posisi dynamic relative ke tombol
function toggleImpExpMenu(e) {
  if (e) { e.stopPropagation(); e.preventDefault(); }
  const menu = document.getElementById('importExportMenu');
  const btn = document.getElementById('importExportToggleBtn');
  if (!menu || !btn) return;

  if (menu.classList.contains('show')) {
    menu.classList.remove('show');
    return;
  }

  // Hitung posisi: di bawah tombol, align kanan
  const rect = btn.getBoundingClientRect();
  const menuWidth = 280; // sesuai min-width
  let left = rect.right - menuWidth; // align kanan tombol
  let top = rect.bottom + 8;

  // Pastikan tidak keluar viewport kiri
  if (left < 8) left = 8;
  // Kalau menu tidak muat ke bawah, tampilkan ke atas
  if (top + 280 > window.innerHeight) {
    top = rect.top - 280 - 8;
    if (top < 8) top = 8;
  }

  menu.style.top  = top + 'px';
  menu.style.left = left + 'px';
  menu.classList.add('show');
}

// Tutup saat klik di luar
document.addEventListener('click', (e) => {
  const menu = document.getElementById('importExportMenu');
  const btn = document.getElementById('importExportToggleBtn');
  if (!menu || !menu.classList.contains('show')) return;
  // Klik di dalam menu atau tombol → biarkan
  if (menu.contains(e.target) || (btn && btn.contains(e.target))) return;
  menu.classList.remove('show');
});

// Tutup saat scroll halaman atau resize (supaya tidak nyangkut di posisi lama)
let _impExpClosing;
window.addEventListener('scroll', () => {
  const menu = document.getElementById('importExportMenu');
  if (menu && menu.classList.contains('show')) menu.classList.remove('show');
}, true);
window.addEventListener('resize', () => {
  const menu = document.getElementById('importExportMenu');
  if (menu && menu.classList.contains('show')) menu.classList.remove('show');
});

// Tutup dengan Escape
document.addEventListener('keydown', (e) => {
  if (e.key === 'Escape') {
    const menu = document.getElementById('importExportMenu');
    if (menu) menu.classList.remove('show');
  }
});

function generateAllPaymentLinks() {
  document.getElementById('importExportMenu').classList.remove('show');
  showConfirmModal(
    'Buat Link Pembayaran Massal',
    'Buat link pembayaran permanen untuk semua pelanggan yang <strong>belum punya link</strong>?<br><small style="color:#64748b">Pelanggan yang sudah punya link akan dilewati — link lama tetap berlaku.</small>',
    'link',
    '#0f766e',
    async function () {
      App.showToast('Sedang membuat link...', 'info');
      const res = await App.api('/customers/payment-link/generate-all', { method: 'POST' });
      if (res && res.success) {
        const d = res.data || {};
        App.showToast('✓ ' + (res.message || ('Link dibuat: ' + (d.created || 0))), 'success');
      } else {
        App.showToast('Gagal: ' + ((res && res.message) || 'Error'), 'error');
      }
    }
  );
}

function exportCustomers() {
  document.getElementById('importExportMenu').classList.remove('show');
  // Trigger download via iframe (supaya tidak meninggalkan halaman)
  const token = document.cookie.match(/token=([^;]+)/)?.[1] || localStorage.getItem('token') || '';
  // Pakai window.open dengan auth — tapi browser tidak set Authorization untuk navigation.
  // Solusi: pakai fetch + blob download.
  App.showToast('Sedang menyiapkan file...', 'info');
  fetch('/api/customers/export', {
    headers: token ? { 'Authorization': 'Bearer ' + token } : {},
    credentials: 'include',
  }).then(res => {
    if (!res.ok) throw new Error('HTTP ' + res.status);
    return res.blob();
  }).then(blob => {
    const url = URL.createObjectURL(blob);
    const a = document.createElement('a');
    a.href = url;
    a.download = `customers-export-${new Date().toISOString().slice(0,10)}.xlsx`;
    document.body.appendChild(a);
    a.click();
    a.remove();
    URL.revokeObjectURL(url);
    App.showToast('Export berhasil!', 'success');
  }).catch(err => {
    App.showToast('Export gagal: ' + err.message, 'error');
  });
}

function downloadTemplate() {
  document.getElementById('importExportMenu').classList.remove('show');
  const token = document.cookie.match(/token=([^;]+)/)?.[1] || localStorage.getItem('token') || '';
  App.showToast('Generating template...', 'info');
  fetch('/api/customers/import-template', {
    headers: token ? { 'Authorization': 'Bearer ' + token } : {},
    credentials: 'include',
  }).then(res => {
    if (!res.ok) throw new Error('HTTP ' + res.status);
    return res.blob();
  }).then(blob => {
    const url = URL.createObjectURL(blob);
    const a = document.createElement('a');
    a.href = url;
    a.download = 'customers-import-template.xlsx';
    document.body.appendChild(a);
    a.click();
    a.remove();
    URL.revokeObjectURL(url);
  }).catch(err => {
    App.showToast('Gagal download template: ' + err.message, 'error');
  });
}

function openImportModal() {
  document.getElementById('importExportMenu').classList.remove('show');
  document.getElementById('importModal').style.display = 'flex';
  importBackToStep1();
}

function closeImportModal() {
  document.getElementById('importModal').style.display = 'none';
  _importToken = null;
}

function importBackToStep1() {
  document.getElementById('importStep1').style.display = 'block';
  document.getElementById('importStep2').style.display = 'none';
  document.getElementById('importStep3').style.display = 'none';
  document.getElementById('importBackBtn').style.display = 'none';
  document.getElementById('importConfirmBtn').style.display = 'none';
  document.getElementById('importCancelBtn').textContent = 'Batal';
  document.getElementById('importModalSubtitle').textContent = 'Upload file .xlsx — preview dulu sebelum import.';
  document.getElementById('importFileInput').value = '';
  _importToken = null;
}

function handleImportDrop(e) {
  e.preventDefault();
  e.currentTarget.style.borderColor = '';
  e.currentTarget.style.background = '#f8faff';
  const file = e.dataTransfer.files[0];
  if (file) handleImportFile(file);
}

async function handleImportFile(file) {
  if (!file) return;
  const ext = (file.name.split('.').pop() || '').toLowerCase();
  if (!['xlsx', 'xls'].includes(ext)) {
    App.showToast('Hanya format .xlsx atau .xls yang didukung', 'error');
    return;
  }
  if (file.size > 5 * 1024 * 1024) {
    App.showToast('File terlalu besar. Maksimal 5 MB', 'error');
    return;
  }

  // Show loading
  document.getElementById('importDropArea').innerHTML = `
    <div style="padding:20px;">
      <svg class="spinner" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="#1a6ef5" stroke-width="2">
        <circle cx="12" cy="12" r="10" stroke-opacity=".25"/>
        <path d="M22 12a10 10 0 0 1-10 10" stroke-linecap="round">
          <animateTransform attributeName="transform" type="rotate" from="0 12 12" to="360 12 12" dur="1s" repeatCount="indefinite"/>
        </path>
      </svg>
      <div style="font-size:14px;font-weight:600;color:var(--d-text);margin-top:10px;">Membaca file...</div>
      <div style="font-size:12px;color:var(--d-muted);">${file.name}</div>
    </div>
  `;

  const form = new FormData();
  form.append('file', file);

  try {
    const token = document.cookie.match(/token=([^;]+)/)?.[1] || localStorage.getItem('token') || '';
    const res = await fetch('/api/customers/import-preview', {
      method: 'POST',
      headers: token ? { 'Authorization': 'Bearer ' + token } : {},
      body: form,
    });
    const d = await res.json();
    if (!d.success) {
      App.showToast(d.message || 'Gagal parse file', 'error');
      importBackToStep1();
      return;
    }
    showImportPreview(d);
  } catch (err) {
    App.showToast('Error: ' + err.message, 'error');
    importBackToStep1();
  }
}

function showImportPreview(data) {
  _importToken = data.importToken;
  _importValidCount = data.summary.valid;

  // Pindah ke step 2
  document.getElementById('importStep1').style.display = 'none';
  document.getElementById('importStep2').style.display = 'block';
  document.getElementById('importBackBtn').style.display = 'inline-flex';
  document.getElementById('importConfirmBtn').style.display = 'inline-flex';
  document.getElementById('importConfirmBtn').textContent = `Import ${_importValidCount} Baris`;
  document.getElementById('importConfirmBtn').disabled = _importValidCount === 0;
  document.getElementById('importModalSubtitle').textContent = 'Periksa preview di bawah, lalu klik "Import Sekarang".';

  // Build map mikrotik_id → router name (untuk display di kolom Router preview).
  // Backend kirim sebagai object {id: name} di data.routerCheck.routerIdToName.
  // Convert ke Map untuk lookup yang gampang.
  __importRouterIdToName = new Map();
  const idToName = data.routerCheck?.routerIdToName || {};
  Object.keys(idToName).forEach(idStr => {
    __importRouterIdToName.set(parseInt(idStr, 10), idToName[idStr]);
  });

  // Summary cards
  const s = data.summary;
  const autoSubtitle = s.toCreateAuto > 0
    ? `<div style="font-size:10.5px;color:#475569;font-weight:500;margin-top:3px;display:flex;align-items:center;gap:4px;">
         <svg width="10" height="10" fill="none" stroke="currentColor" stroke-width="2.2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M13 10V3L4 14h7v7l9-11h-7z"/></svg>
         <span>${s.toCreateAuto} dengan ID otomatis</span>
       </div>`
    : '';
  document.getElementById('importSummary').innerHTML = `
    <div style="padding:14px 16px;background:#fff;border:1px solid #e2e8f0;border-radius:10px;">
      <div style="font-size:10.5px;font-weight:600;color:#64748b;text-transform:uppercase;letter-spacing:.4px;margin-bottom:4px;">Total baris</div>
      <div style="font-size:22px;font-weight:600;color:#0f172a;line-height:1;">${s.total}</div>
    </div>
    <div style="padding:14px 16px;background:#fff;border:1px solid #d1fae5;border-radius:10px;">
      <div style="font-size:10.5px;font-weight:600;color:#059669;text-transform:uppercase;letter-spacing:.4px;margin-bottom:4px;">Valid</div>
      <div style="font-size:22px;font-weight:600;color:#065f46;line-height:1;">${s.valid}</div>
    </div>
    <div style="padding:14px 16px;background:#fff;border:1px solid #fecaca;border-radius:10px;">
      <div style="font-size:10.5px;font-weight:600;color:#dc2626;text-transform:uppercase;letter-spacing:.4px;margin-bottom:4px;">Error</div>
      <div style="font-size:22px;font-weight:600;color:#991b1b;line-height:1;">${s.invalid}</div>
    </div>
    <div style="padding:14px 16px;background:#fff;border:1px solid #bfdbfe;border-radius:10px;">
      <div style="font-size:10.5px;font-weight:600;color:#1d4ed8;text-transform:uppercase;letter-spacing:.4px;margin-bottom:4px;">Buat baru / update</div>
      <div style="font-size:18px;font-weight:600;color:#1e3a8a;line-height:1;">${s.toCreate} <span style="color:#94a3b8;font-weight:400;">/</span> ${s.toUpdate}</div>
      ${autoSubtitle}
    </div>
  `;

  // ── Router secret check info banner ──────────────────────────────
  // Tampilkan ringkasan hasil validasi PPPoE username terhadap secret di router.
  // Hitung berapa row dengan warning "TIDAK ditemukan di router" untuk give
  // overall visibility. Render sebagai banner kuning/info di atas tabel preview.
  const banner = document.getElementById('importRouterCheckBanner');
  if (banner) {
    const rc = data.routerCheck || {};
    // Hitung row dengan warning "tidak ditemukan di router"
    const missingCount = data.rows.filter(r =>
      (r.warnings || []).some(w => w.includes('TIDAK ditemukan di'))
    ).length;

    let bannerHTML = '';
    if (rc.checkedRouters && rc.checkedRouters.length > 0) {
      // Ada router yang berhasil di-cek
      if (missingCount > 0) {
        // Ada row yang username-nya tidak ditemukan di router
        bannerHTML = `
          <div style="display:flex;gap:10px;align-items:flex-start;padding:10px 14px;background:#fef3c7;border:1px solid #fde68a;border-radius:10px;margin-bottom:12px;">
            <svg width="16" height="16" fill="none" stroke="#d97706" stroke-width="2" viewBox="0 0 24 24" style="flex-shrink:0;margin-top:2px;">
              <path stroke-linecap="round" stroke-linejoin="round" d="M12 9v2m0 4h.01M10.29 3.86L1.82 18a2 2 0 001.71 3h16.94a2 2 0 001.71-3L13.71 3.86a2 2 0 00-3.42 0z"/>
            </svg>
            <div style="flex:1;font-size:12px;color:#92400e;line-height:1.5;">
              <strong>${missingCount} customer</strong> punya username PPPoE yang <strong>tidak ditemukan</strong> di router (${rc.checkedRouters.length} router di-cek: ${rc.checkedRouters.map(n => `<code style="background:#fbbf24;color:#451a03;padding:1px 5px;border-radius:3px;font-size:11px;">${n}</code>`).join(' ')}, total ${rc.totalSecrets} secret).
              Cek kolom <strong>Catatan</strong> di tabel — row tetap valid &amp; bisa di-import, tapi pastikan buat secret di router sebelum customer aktif, atau via halaman PPPoE Manager setelah import.
            </div>
          </div>`;
      } else {
        // Semua username sudah ada di router — OK
        bannerHTML = `
          <div style="display:flex;gap:10px;align-items:center;padding:10px 14px;background:#ecfdf5;border:1px solid #a7f3d0;border-radius:10px;margin-bottom:12px;">
            <svg width="16" height="16" fill="none" stroke="#059669" stroke-width="2" viewBox="0 0 24 24" style="flex-shrink:0;">
              <path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7"/>
            </svg>
            <div style="font-size:12px;color:#065f46;line-height:1.5;">
              Semua username PPPoE sudah cocok dengan secret yang ada di router (${rc.checkedRouters.length} router: ${rc.checkedRouters.map(n => `<code style="background:#a7f3d0;color:#064e3b;padding:1px 5px;border-radius:3px;font-size:11px;">${n}</code>`).join(' ')}).
            </div>
          </div>`;
      }
    }
    // Tambahkan warning kalau ada router yang gagal di-cek
    if (rc.warnings && rc.warnings.length > 0) {
      bannerHTML += `
        <div style="display:flex;gap:10px;align-items:flex-start;padding:10px 14px;background:#f1f5f9;border:1px solid #cbd5e1;border-radius:10px;margin-bottom:12px;">
          <svg width="14" height="14" fill="none" stroke="#64748b" stroke-width="2" viewBox="0 0 24 24" style="flex-shrink:0;margin-top:2px;"><circle cx="12" cy="12" r="10"/><path stroke-linecap="round" stroke-linejoin="round" d="M12 8v4M12 16h.01"/></svg>
          <div style="flex:1;font-size:11.5px;color:#475569;line-height:1.5;">
            <strong>Catatan validasi:</strong>
            <ul style="margin:4px 0 0 0;padding-left:18px;">
              ${rc.warnings.map(w => `<li>${w.replace(/</g, '&lt;')}</li>`).join('')}
            </ul>
          </div>
        </div>`;
    }
    banner.innerHTML = bannerHTML;
  }

  // Render rows
  const tbody = document.getElementById('importPreviewRows');
  tbody.innerHTML = data.rows.map(r => {
    const statusBadge = r.valid 
      ? `<span style="display:inline-flex;align-items:center;gap:4px;padding:3px 8px;background:#ecfdf5;color:#047857;border-radius:6px;font-size:11px;font-weight:600;">
           <svg width="10" height="10" fill="none" stroke="currentColor" stroke-width="3" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7"/></svg>
           Valid
         </span>`
      : `<span style="display:inline-flex;align-items:center;gap:4px;padding:3px 8px;background:#fef2f2;color:#b91c1c;border-radius:6px;font-size:11px;font-weight:600;">
           <svg width="10" height="10" fill="none" stroke="currentColor" stroke-width="3" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12"/></svg>
           Error
         </span>`;
    let actionBadge;
    if (r.action === 'create_auto') {
      actionBadge = `<span style="display:inline-flex;align-items:center;gap:4px;color:#475569;font-weight:600;font-size:12.5px;" title="ID akan di-generate otomatis">
        <svg width="13" height="13" fill="none" stroke="currentColor" stroke-width="2.2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M13 10V3L4 14h7v7l9-11h-7z"/></svg>
        Auto
      </span>`;
    } else if (r.action === 'create') {
      actionBadge = `<span style="display:inline-flex;align-items:center;gap:4px;color:#059669;font-weight:600;font-size:12.5px;">
        <svg width="13" height="13" fill="none" stroke="currentColor" stroke-width="2.2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M12 4v16m8-8H4"/></svg>
        Baru
      </span>`;
    } else {
      actionBadge = `<span style="display:inline-flex;align-items:center;gap:4px;color:#0284c7;font-weight:600;font-size:12.5px;">
        <svg width="13" height="13" fill="none" stroke="currentColor" stroke-width="2.2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M4 4v5h5M20 20v-5h-5M5 9a8 8 0 0114-3M19 15a8 8 0 01-14 3"/></svg>
        Update
      </span>`;
    }
    // Tampilkan placeholder untuk ID yang akan auto-generate
    const customerIdDisplay = r.action === 'create_auto'
      ? `<span style="color:#94a3b8;font-style:italic;font-family:inherit;">akan dibuat</span>`
      : `<span style="font-family:'DM Mono',monospace;color:#0f172a;">${escapeHtml(r.data.customer_id || '')}</span>`;
    const notes = [
      ...(r.errors || []).map(e => `<div style="display:flex;gap:4px;align-items:flex-start;color:#dc2626;font-size:11.5px;">
        <svg width="10" height="10" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24" style="flex-shrink:0;margin-top:3px;"><circle cx="12" cy="12" r="10"/><path stroke-linecap="round" stroke-linejoin="round" d="M12 8v4M12 16h.01"/></svg>
        <span>${escapeHtml(e)}</span></div>`),
      ...(r.warnings || []).map(w => `<div style="display:flex;gap:4px;align-items:flex-start;color:#d97706;font-size:11.5px;">
        <svg width="10" height="10" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24" style="flex-shrink:0;margin-top:3px;"><path stroke-linecap="round" stroke-linejoin="round" d="M10.29 3.86L1.82 18a2 2 0 001.71 3h16.94a2 2 0 001.71-3L13.71 3.86a2 2 0 00-3.42 0zM12 9v4M12 17h.01"/></svg>
        <span>${escapeHtml(w)}</span></div>`),
    ].join('') || '<span style="color:#cbd5e1;font-size:12px;">—</span>';
    return `<tr style="border-bottom:1px solid #f1f5f9;${!r.valid ? 'background:#fef2f2;' : ''}">
      <td style="padding:9px 12px;color:#94a3b8;font-size:12px;">${r.rowNumber}</td>
      <td style="padding:9px 12px;">${statusBadge}</td>
      <td style="padding:9px 12px;font-size:12.5px;">${customerIdDisplay}</td>
      <td style="padding:9px 12px;color:#0f172a;font-size:12.5px;">${escapeHtml(r.data.name || '')}</td>
      <td style="padding:9px 12px;font-size:12px;">${
        r.data.pppoe_username
          ? `<span style="font-family:'DM Mono',monospace;color:#0f172a;background:#eff6ff;padding:2px 7px;border-radius:5px;">${escapeHtml(r.data.pppoe_username)}</span>`
          : `<span style="color:#cbd5e1;">—</span>`
      }</td>
      <td style="padding:9px 12px;font-size:12px;">${
        // Static IP cell — tampilkan IP dengan style mono kalau ada
        r.data.static_ip
          ? `<span style="font-family:'DM Mono',monospace;color:#0f172a;background:#f0f9ff;padding:2px 7px;border-radius:5px;border:1px solid #bae6fd;">${escapeHtml(r.data.static_ip)}</span>`
          : `<span style="color:#cbd5e1;">—</span>`
      }</td>
      <td style="padding:9px 12px;font-size:11.5px;">${
        // Router cell: tampilkan nama router + badge source (PPPoE/ARP) kalau auto-detect.
        // 3 sumber: explicit (user isi nama), auto-PPPoE (badge biru), auto-ARP (badge teal).
        r.data.mikrotik_id
          ? `<span style="background:#dbeafe;color:#1e3a8a;padding:2px 7px;border-radius:5px;font-weight:500;">${escapeHtml(__importRouterIdToName.get(r.data.mikrotik_id) || ('ID #' + r.data.mikrotik_id))}</span>`
            + (r.data.router_auto_source === 'pppoe' ? ` <span style="font-size:9.5px;background:#f0f9ff;color:#0369a1;padding:1px 5px;border-radius:3px;font-weight:500;" title="Auto-detected dari secret PPPoE">auto: PPPoE</span>` : '')
            + (r.data.router_auto_source === 'arp'   ? ` <span style="font-size:9.5px;background:#f0fdfa;color:#0f766e;padding:1px 5px;border-radius:3px;font-weight:500;" title="Auto-detected dari ARP table">auto: ARP</span>` : '')
          : (r.data.router_name_input
              ? `<span style="color:#dc2626;font-style:italic;" title="Nama router tidak match Device Management">${escapeHtml(r.data.router_name_input)} ✗</span>`
              : `<span style="color:#cbd5e1;">—</span>`)
      }</td>
      <td style="padding:9px 12px;color:#64748b;font-size:12px;">${escapeHtml(r.data.phone || '')}</td>
      <td style="padding:9px 12px;color:#64748b;font-size:12px;">${escapeHtml(r.data.package_name || '')}</td>
      <td style="padding:9px 12px;font-size:11.5px;">${
        // Parent ODP cell: kalau resolved, tampilkan dgn badge hijau; kalau tidak resolved tapi user isi nama, tampilkan italic merah; kalau kosong, dash.
        r.data.infra_parent_id
          ? `<span style="background:#dcfce7;color:#166534;padding:2px 7px;border-radius:5px;font-weight:500;">${escapeHtml(r.data.infra_parent_name_input || ('#' + r.data.infra_parent_id))}</span>`
          : (r.data.infra_parent_name_input
              ? `<span style="color:#dc2626;font-style:italic;" title="Parent ODP tidak ditemukan di sistem">${escapeHtml(r.data.infra_parent_name_input)} ✗</span>`
              : `<span style="color:#cbd5e1;">—</span>`)
      }</td>
      <td style="padding:9px 12px;">${actionBadge}</td>
      <td style="padding:9px 12px;">${notes}</td>
    </tr>`;
  }).join('');
}

async function confirmImport() {
  if (!_importToken) {
    App.showToast('Sesi import sudah expired. Silakan upload ulang.', 'error');
    return;
  }
  const mode = document.getElementById('importMode').value;
  const btn = document.getElementById('importConfirmBtn');
  btn.disabled = true;
  btn.textContent = 'Mengimport...';

  try {
    const res = await App.api('/customers/import-confirm', {
      method: 'POST',
      body: JSON.stringify({ importToken: _importToken, mode }),
    });
    if (res.success) {
      showImportResult(res.summary, res.errors, res.generatedIds);
    } else {
      App.showToast(res.message || 'Import gagal', 'error');
      btn.disabled = false;
      btn.textContent = `Import ${_importValidCount} Baris`;
    }
  } catch (err) {
    App.showToast('Error: ' + err.message, 'error');
    btn.disabled = false;
    btn.textContent = `Import ${_importValidCount} Baris`;
  }
}

function showImportResult(summary, errors, generatedIds) {
  document.getElementById('importStep2').style.display = 'none';
  document.getElementById('importStep3').style.display = 'block';
  document.getElementById('importBackBtn').style.display = 'none';
  document.getElementById('importConfirmBtn').style.display = 'none';
  document.getElementById('importCancelBtn').textContent = 'Tutup';
  document.getElementById('importModalSubtitle').textContent = 'Import selesai.';

  const errorListHtml = (errors && errors.length > 0)
    ? `<div style="margin-top:18px;padding:14px;background:#fff;border:1px solid #fecaca;border-radius:10px;text-align:left;max-height:180px;overflow:auto;">
        <div style="display:flex;align-items:center;gap:6px;font-weight:600;color:#b91c1c;margin-bottom:8px;font-size:12.5px;">
          <svg width="14" height="14" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><circle cx="12" cy="12" r="10"/><path stroke-linecap="round" stroke-linejoin="round" d="M12 8v4M12 16h.01"/></svg>
          Beberapa baris gagal:
        </div>
        ${errors.map(e => `<div style="font-size:11.5px;color:#7f1d1d;padding:2px 0;">
          <code style="background:#fef2f2;padding:1px 5px;border-radius:4px;font-family:'DM Mono',monospace;">${escapeHtml(e.customer_id)}</code>
          <span style="margin-left:6px;color:#991b1b;">${escapeHtml(e.message)}</span>
        </div>`).join('')}
      </div>`
    : '';

  // List ID auto-generate (kalau ada)
  const generatedListHtml = (generatedIds && generatedIds.length > 0)
    ? `<div style="margin-top:18px;padding:14px;background:#fff;border:1px solid #e2e8f0;border-radius:10px;text-align:left;max-height:200px;overflow:auto;">
        <div style="display:flex;align-items:center;gap:6px;font-weight:600;color:#0f172a;margin-bottom:8px;font-size:12.5px;">
          <svg width="14" height="14" fill="none" stroke="#475569" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M13 10V3L4 14h7v7l9-11h-7z"/></svg>
          ID auto-generate (${generatedIds.length}):
        </div>
        ${generatedIds.map(g => `<div style="font-size:12px;display:flex;justify-content:space-between;padding:4px 0;border-bottom:1px solid #f8fafc;">
          <span style="color:#475569;">${escapeHtml(g.name)}</span>
          <code style="font-weight:600;color:#0f172a;background:#f1f5f9;padding:1px 6px;border-radius:4px;font-family:'DM Mono',monospace;font-size:11.5px;">${escapeHtml(g.customer_id)}</code>
        </div>`).join('')}
      </div>`
    : '';

  // Card auto-generate (hanya kalau ada)
  const autoCard = (summary.createdAuto && summary.createdAuto > 0)
    ? `<div style="padding:14px 16px;background:#fff;border:1px solid #e2e8f0;border-radius:10px;">
        <div style="font-size:10.5px;font-weight:600;color:#475569;text-transform:uppercase;letter-spacing:.4px;margin-bottom:4px;">ID otomatis</div>
        <div style="font-size:22px;font-weight:600;color:#0f172a;line-height:1;">${summary.createdAuto}</div>
      </div>`
    : '';

  // Hitung jumlah card untuk grid layout
  const cardCount = 4 + (autoCard ? 1 : 0);

  document.getElementById('importResultContent').innerHTML = `
    <div style="width:64px;height:64px;border-radius:50%;background:#ecfdf5;display:flex;align-items:center;justify-content:center;margin:0 auto 14px;">
      <svg width="30" height="30" fill="none" stroke="#059669" stroke-width="2.5" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7"/></svg>
    </div>
    <h3 style="margin:0 0 4px;font-size:18px;font-weight:600;color:#0f172a;letter-spacing:-.01em;">Import selesai</h3>
    <p style="margin:0 0 22px;color:#64748b;font-size:13px;">Berikut ringkasan hasil import:</p>
    <div style="display:grid;grid-template-columns:repeat(${cardCount},1fr);gap:10px;max-width:640px;margin:0 auto;">
      <div style="padding:14px 16px;background:#fff;border:1px solid #d1fae5;border-radius:10px;">
        <div style="font-size:10.5px;font-weight:600;color:#059669;text-transform:uppercase;letter-spacing:.4px;margin-bottom:4px;">Berhasil dibuat</div>
        <div style="font-size:22px;font-weight:600;color:#065f46;line-height:1;">${summary.created}</div>
      </div>
      ${autoCard}
      <div style="padding:14px 16px;background:#fff;border:1px solid #bfdbfe;border-radius:10px;">
        <div style="font-size:10.5px;font-weight:600;color:#1d4ed8;text-transform:uppercase;letter-spacing:.4px;margin-bottom:4px;">Diupdate</div>
        <div style="font-size:22px;font-weight:600;color:#1e3a8a;line-height:1;">${summary.updated}</div>
      </div>
      <div style="padding:14px 16px;background:#fff;border:1px solid #e2e8f0;border-radius:10px;">
        <div style="font-size:10.5px;font-weight:600;color:#64748b;text-transform:uppercase;letter-spacing:.4px;margin-bottom:4px;">Dilewati</div>
        <div style="font-size:22px;font-weight:600;color:#334155;line-height:1;">${summary.skipped}</div>
      </div>
      <div style="padding:14px 16px;background:#fff;border:1px solid #fecaca;border-radius:10px;">
        <div style="font-size:10.5px;font-weight:600;color:#dc2626;text-transform:uppercase;letter-spacing:.4px;margin-bottom:4px;">Gagal</div>
        <div style="font-size:22px;font-weight:600;color:#991b1b;line-height:1;">${summary.failed}</div>
      </div>
    </div>
    ${generatedListHtml}
    ${errorListHtml}
    <div style="margin-top:22px;">
      <button onclick="closeImportModal();location.reload();" class="btn-fin btn-primary-fin">Selesai &amp; refresh halaman</button>
    </div>
  `;
}

function escapeHtml(s) {
  return String(s == null ? '' : s).replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
}
</script>

</body>
</html>
