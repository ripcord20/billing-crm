<!DOCTYPE html>
<html lang="id" data-theme="light">
<head>
  <meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1.0">
  <title><%= title %> - <%= appName %></title>
  <link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600;700;800&family=DM+Mono:wght@400;500&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/app.css">
  <style>
    :root{--d-bg:#eef2f9;--d-card:#fff;--d-border:#e0e7f3;--d-text:#0d1b3e;--d-muted:#6b7fa8;--d-blue1:#1a6ef5;}
    .st-page{font-family:'DM Sans',sans-serif;}
    @keyframes fadeUp{from{opacity:0;transform:translateY(12px)}to{opacity:1;transform:translateY(0)}}
    .a1{animation:fadeUp .35s ease both}.a2{animation:fadeUp .35s .07s ease both}.a3{animation:fadeUp .35s .14s ease both}
    .st-layout{display:grid;grid-template-columns:220px 1fr;gap:20px;align-items:start;}
    @media(max-width:800px){.st-layout{grid-template-columns:1fr;}}
    /* Nav tabs kiri */
    .st-nav{background:#fff;border:1px solid var(--d-border);border-radius:18px;overflow:hidden;position:sticky;top:78px;}
    .st-nav-item{display:flex;align-items:center;gap:10px;padding:12px 16px;font-size:13px;font-weight:600;color:var(--d-muted);cursor:pointer;transition:all .15s;border-left:3px solid transparent;}
    .st-nav-item:hover{background:#f4f8ff;color:var(--d-blue1);}
    .st-nav-item.active{background:#eff6ff;color:var(--d-blue1);}
    .st-nav-item svg{flex-shrink:0;}
    .st-nav-sep{height:1px;background:var(--d-border);margin:4px 0;}
    /* Content cards */
    .st-section{display:none;} .st-section.show{display:block;}
    .st-card{background:#fff;border:1px solid var(--d-border);border-radius:20px;overflow:hidden;margin-bottom:14px;}
    .st-card-hdr{padding:16px 20px;border-bottom:1px solid var(--d-border);display:flex;align-items:center;gap:12px;background:linear-gradient(135deg,#f8faff,#f0f4ff);}
    .st-card-icon{width:36px;height:36px;border-radius:10px;background:linear-gradient(135deg,#1a6ef5,#0047cc);display:flex;align-items:center;justify-content:center;flex-shrink:0;}
    .st-card-body{padding:20px;}
    /* Operational sections */
    .op-grid{display:grid;grid-template-columns:1fr 1fr;gap:16px;}
    @media(max-width:620px){.op-grid{grid-template-columns:1fr;}}
    .op-hint{font-size:11px;color:var(--d-muted);margin-top:5px;line-height:1.5;}
    .op-hint code{background:rgba(15,23,42,.06);padding:1px 5px;border-radius:5px;font-size:10.5px;}
    .op-toggle-row{display:flex;align-items:flex-start;gap:12px;margin-top:16px;}
    .op-switch{position:relative;display:inline-block;width:42px;height:24px;flex-shrink:0;cursor:pointer;}
    .op-switch input{opacity:0;width:0;height:0;}
    .op-switch span{position:absolute;inset:0;background:#cbd5e1;border-radius:99px;transition:.2s;}
    .op-switch span:before{content:"";position:absolute;height:18px;width:18px;left:3px;top:3px;background:#fff;border-radius:50%;transition:.2s;}
    .op-switch input:checked + span{background:#1d4ed8;}
    .op-switch input:checked + span:before{transform:translateX(18px);}
    .op-btn-primary{display:inline-flex;align-items:center;gap:7px;border:none;cursor:pointer;padding:10px 18px;border-radius:11px;background:#1d4ed8;color:#fff;font-family:inherit;font-size:13px;font-weight:700;transition:opacity .15s;}
    .op-btn-primary:hover{opacity:.92;}
    .op-btn-outline{display:inline-flex;align-items:center;gap:7px;border:1px solid var(--d-border);cursor:pointer;padding:10px 16px;border-radius:11px;background:#fff;color:var(--d-text);font-family:inherit;font-size:13px;font-weight:700;}
    .op-btn-outline:hover{background:#f1f5f9;}
    .op-preview{font-family:'DM Mono',ui-monospace,monospace;font-size:16px;font-weight:700;color:#1d4ed8;background:#eff6ff;border:1px solid #bfdbfe;border-radius:10px;padding:9px 13px;letter-spacing:.5px;}
    .op-session-list{display:flex;flex-direction:column;gap:8px;}
    .op-session-row{display:flex;align-items:center;gap:10px;padding:10px 13px;border:1px solid var(--d-border);border-radius:11px;background:#fafbff;}
    .op-session-dot{width:9px;height:9px;border-radius:50%;flex-shrink:0;}
    .op-session-dot.on{background:#16a34a;}
    .op-session-dot.off{background:#cbd5e1;}
    .op-session-name{font-weight:700;font-size:12.5px;color:var(--d-text);}
    .op-session-meta{font-size:11px;color:var(--d-muted);}
    .op-session-badge{margin-left:auto;font-size:10.5px;font-weight:700;padding:2px 9px;border-radius:7px;}
    .op-session-badge.on{background:#dcfce7;color:#15803d;}
    .op-session-badge.off{background:#f1f5f9;color:#64748b;}
    /* Backup & Restore rows */
    .bk-row{display:flex;align-items:flex-start;gap:14px;padding:16px;border:1px solid var(--d-border);border-radius:14px;background:#f8faff;}
    .bk-row + .bk-row{margin-top:12px;}
    .bk-row-warn{background:#fffbeb;border-color:#fde68a;}
    .bk-row-icn{width:38px;height:38px;border-radius:11px;display:flex;align-items:center;justify-content:center;flex-shrink:0;}
    .bk-row-txt{flex:1;min-width:0;}
    .bk-row-ttl{font-size:13px;font-weight:700;color:var(--d-text);}
    .bk-row-sub{font-size:11.5px;color:var(--d-muted);margin-top:3px;line-height:1.55;}
    .bk-row code{background:rgba(15,23,42,.06);padding:1px 5px;border-radius:5px;font-size:11px;}
    .bk-btn{display:inline-flex;align-items:center;justify-content:center;gap:7px;border:none;cursor:pointer;padding:9px 16px;border-radius:11px;font-family:inherit;font-size:12.5px;font-weight:700;white-space:nowrap;flex-shrink:0;transition:opacity .15s,background .15s;}
    .bk-btn:hover{opacity:.92;}
    .bk-btn-primary{background:#1d4ed8;color:#fff;}
    .bk-btn-outline{background:#fff;color:var(--d-text);border:1px solid var(--d-border);}
    .bk-btn-outline:hover{background:#f1f5f9;opacity:1;}
    .bk-btn-warn{background:#d97706;color:#fff;}
    @media(max-width:620px){
      .bk-row{flex-direction:column;}
      .bk-btn{width:100%;}
    }
    /* Form fields */
    .pf-label{font-size:10.5px;font-weight:700;letter-spacing:.07em;text-transform:uppercase;color:var(--d-muted);display:block;margin-bottom:6px;}
    .pf-input,.pf-select{width:100%;padding:10px 12px;border-radius:10px;border:1.5px solid var(--d-border);font-size:13px;box-sizing:border-box;font-family:'DM Sans',sans-serif;outline:none;background:#fff;color:var(--d-text);transition:all .15s;}
    .pf-input:focus,.pf-select:focus{border-color:var(--d-blue1);box-shadow:0 0 0 3px rgba(26,110,245,.08);}
    .pf-field{margin-bottom:16px;}
    .pf-hint{font-size:11.5px;color:var(--d-muted);margin-top:4px;line-height:1.5;}
    /* Brand mode cards */
    .brand-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:12px;margin-bottom:16px;}
    @media(max-width:600px){.brand-grid{grid-template-columns:1fr;}}
    .brand-option{border:2px solid var(--d-border);border-radius:12px;padding:14px 12px;cursor:pointer;transition:all .15s;text-align:center;background:#fff;position:relative;}
    .brand-option:hover{border-color:#93c5fd;background:#f0f9ff;}
    .brand-option.selected{border-color:var(--d-blue1);background:#eff6ff;}
    .brand-option.selected::after{content:'✓';position:absolute;top:8px;right:10px;font-size:12px;font-weight:800;color:var(--d-blue1);}
    .brand-preview{height:42px;display:flex;align-items:center;justify-content:center;gap:8px;margin-bottom:8px;background:#f8faff;border-radius:8px;padding:0 10px;}
    .brand-preview .mini-icon{width:22px;height:22px;border-radius:6px;background:linear-gradient(135deg,#1a6ef5,#0047cc);display:flex;align-items:center;justify-content:center;flex-shrink:0;}
    .brand-option-title{font-size:12px;font-weight:700;color:var(--d-text);}
    .brand-option-desc{font-size:10.5px;color:var(--d-muted);margin-top:2px;}
    /* Preview box */
    .brand-live-preview{background:#f8faff;border:1.5px solid var(--d-border);border-radius:12px;padding:14px 16px;display:flex;align-items:center;gap:10px;margin-bottom:16px;}
    /* Save button */
    .save-btn{padding:11px 28px;border-radius:11px;font-size:13.5px;font-weight:700;background:linear-gradient(135deg,#1a6ef5,#0047cc);color:#fff;border:none;cursor:pointer;font-family:'DM Sans',sans-serif;transition:opacity .15s;display:inline-flex;align-items:center;gap:7px;}
    .pf-btn-outline{padding:10px 16px;border-radius:10px;font-size:13px;font-weight:700;background:#fff;color:var(--d-text);border:1.5px solid var(--d-border);cursor:pointer;font-family:inherit;display:inline-flex;align-items:center;justify-content:center;gap:7px;transition:all .15s;}
    .pf-btn-outline:hover{background:#f8fafc;border-color:#94a3b8;}
    .save-btn:hover{opacity:.9;}
    .divider{height:1px;background:var(--d-border);margin:20px 0;}

    /* ── Telegram Format Pesan (Template) ── */
    .tg-tpl-head{display:flex;align-items:center;gap:10px;margin:24px 0 6px;}
    .tg-tpl-head-ic{width:30px;height:30px;border-radius:9px;display:flex;align-items:center;justify-content:center;background:linear-gradient(135deg,#e8f0ff,#d9e6ff);flex:none;}
    .tg-tpl-head-t{font-size:13px;font-weight:800;color:var(--d-text);letter-spacing:.2px;}
    .tg-tpl-head-s{font-size:11.5px;color:var(--d-muted);margin-top:1px;}
    .tg-ph-row{display:flex;flex-wrap:wrap;gap:7px;margin:10px 0 16px;}
    .tg-ph{display:inline-flex;align-items:center;gap:5px;font-family:'DM Mono',monospace;font-size:11px;font-weight:600;color:var(--d-blue1);background:#eff5ff;border:1px solid #d4e3ff;border-radius:7px;padding:3px 8px;cursor:pointer;transition:all .13s;user-select:none;}
    .tg-ph:hover{background:#1a6ef5;color:#fff;border-color:#1a6ef5;transform:translateY(-1px);}
    .tg-ph:active{transform:translateY(0);}
    .tg-tpl-grid{display:grid;grid-template-columns:repeat(2,1fr);gap:12px;}
    @media(max-width:760px){.tg-tpl-grid{grid-template-columns:1fr;}}
    .tg-tpl-card{border:1px solid var(--d-border);border-radius:14px;background:#fff;overflow:hidden;transition:border-color .15s,box-shadow .15s;position:relative;}
    .tg-tpl-card:focus-within{border-color:var(--accent,#1a6ef5);box-shadow:0 0 0 3px var(--accent-soft,rgba(26,110,245,.08));}
    .tg-tpl-card-hdr{display:flex;align-items:center;gap:9px;padding:11px 13px 9px;}
    .tg-tpl-dot{width:9px;height:9px;border-radius:50%;background:var(--accent,#94a3b8);flex:none;box-shadow:0 0 0 3px var(--accent-soft,rgba(148,163,184,.18));}
    .tg-tpl-name{font-size:12.5px;font-weight:700;color:var(--d-text);}
    .tg-tpl-vars{margin-left:auto;font-family:'DM Mono',monospace;font-size:10px;color:var(--d-muted);font-weight:500;}
    .tg-tpl-card textarea{width:100%;border:none;border-top:1px solid var(--d-border);background:#fbfcff;padding:11px 13px;font-family:'DM Mono',monospace;font-size:12px;line-height:1.55;color:var(--d-text);box-sizing:border-box;outline:none;resize:vertical;min-height:88px;}
    .tg-tpl-card textarea::placeholder{color:#9fb0cf;}
    .tg-tpl-card textarea:focus{background:#fff;}
    .tg-tpl-note{display:flex;gap:9px;align-items:flex-start;font-size:11.5px;color:#92710e;margin-top:14px;line-height:1.55;padding:11px 13px;background:linear-gradient(135deg,#fffbeb,#fef9e7);border:1px solid #fde68a;border-radius:12px;}

    /* ═══ TAX / PPN ═══ */
    .tax-switch{
      width:38px;height:22px;border-radius:20px;background:#cbd5e1;
      position:relative;transition:background .2s;display:inline-block;flex-shrink:0;
    }
    .tax-switch::after{
      content:'';position:absolute;top:2px;left:2px;
      width:18px;height:18px;border-radius:50%;background:#fff;
      box-shadow:0 1px 3px rgba(0,0,0,.15);
      transition:left .2s;
    }
    #taxEnabled:checked + .tax-switch{ background:#0d9488; }
    #taxEnabled:checked + .tax-switch::after{ left:18px; }
    #taxEnabled:focus-visible + .tax-switch{ box-shadow:0 0 0 3px rgba(13,148,136,.25); }

    .tax-preset{
      display:inline-block;padding:2px 8px;margin-left:2px;border-radius:6px;
      background:#f1f5f9;color:#475569;border:1px solid #e2e8f0;
      font-size:10.5px;font-weight:700;cursor:pointer;font-family:inherit;
      transition:all .15s;
    }
    .tax-preset:hover{ background:#0d9488;color:#fff;border-color:#0d9488; }

    /* Body collapse saat PPN nonaktif */
    #taxCardBody.tax-disabled{ opacity:.55;pointer-events:none; }

    /* Spinner animation untuk tombol recalculate */
    @keyframes spin { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }

    /* Auto Generate Invoice toggle (sama pattern dgn tax-switch, warna biru) */
    .autogen-switch{
      width:38px;height:22px;border-radius:20px;background:#cbd5e1;
      position:relative;transition:background .2s;display:inline-block;flex-shrink:0;
    }
    .autogen-switch::after{
      content:'';position:absolute;top:2px;left:2px;
      width:18px;height:18px;border-radius:50%;background:#fff;
      box-shadow:0 1px 3px rgba(0,0,0,.15);
      transition:left .2s;
    }
    #autoGenInvoice:checked + .autogen-switch{ background:#2563eb; }
    #autoGenInvoice:checked + .autogen-switch::after{ left:18px; }
    #autoGenInvoice:focus-visible + .autogen-switch{ box-shadow:0 0 0 3px rgba(37,99,235,.25); }

    /* ═══ PUSH NOTIFICATION STYLES ═══ */
    .push-stat{background:linear-gradient(135deg,#f8faff,#eff6ff);border:1px solid var(--d-border);border-radius:12px;padding:12px 14px;}
    .push-stat-lbl{font-size:10.5px;color:var(--d-muted);font-weight:700;text-transform:uppercase;letter-spacing:.5px;}
    .push-stat-val{font-size:20px;font-weight:800;color:var(--d-text);font-family:'DM Mono',monospace;margin-top:4px;}

    .push-tab{padding:10px 14px;font-size:12.5px;font-weight:700;color:var(--d-muted);cursor:pointer;border-bottom:2.5px solid transparent;margin-bottom:-1.5px;transition:all .15s;display:inline-flex;align-items:center;gap:6px;}
    .push-tab svg{width:14px;height:14px;}
    .push-tab:hover{color:var(--d-blue1);}
    .push-tab.active{color:var(--d-blue1);border-bottom-color:var(--d-blue1);}
    .push-tab-content{display:none;}
    .push-tab-content.active{display:block;}

    .push-section-hdr{font-size:12px;font-weight:700;color:var(--d-text);text-transform:uppercase;letter-spacing:.5px;margin-bottom:10px;display:flex;align-items:center;gap:8px;}
    .push-section-hdr svg{width:14px;height:14px;color:var(--d-blue1);}

    @keyframes pushPulse{0%,100%{opacity:1;transform:scale(1)}50%{opacity:.6;transform:scale(.85)}}

    /* Inline SVG button helpers */
    .push-btn-icon{width:13px;height:13px;vertical-align:-2px;margin-right:4px;}

    .push-chip{display:inline-flex;align-items:center;gap:6px;padding:7px 12px;border:1.5px solid var(--d-border);border-radius:20px;font-size:12px;font-weight:600;cursor:pointer;user-select:none;background:#fff;transition:all .15s;}
    .push-chip:hover{border-color:#93c5fd;background:#f0f9ff;}
    .push-chip input{width:14px;height:14px;cursor:pointer;margin:0;}
    .push-chip:has(input:checked){background:#eff6ff;border-color:var(--d-blue1);color:var(--d-blue1);}

    .push-mini-btn{padding:6px 12px;border-radius:8px;background:#eff6ff;color:var(--d-blue1);border:1px solid #bfdbfe;font-size:11px;font-weight:700;cursor:pointer;font-family:inherit;}
    .push-mini-btn:hover{background:#dbeafe;}

    .emoji-chip{width:40px;height:40px;border:1.5px solid var(--d-border);border-radius:10px;background:#fff;font-size:20px;cursor:pointer;display:flex;align-items:center;justify-content:center;transition:all .15s;}
    .emoji-chip:hover{border-color:var(--d-blue1);background:#eff6ff;transform:scale(1.05);}

    .push-preview{background:#0f172a;color:#fff;border-radius:14px;padding:14px;display:flex;gap:12px;align-items:flex-start;box-shadow:0 10px 24px rgba(15,23,42,.25);}
    .push-preview .pp-icon{width:38px;height:38px;border-radius:9px;background:rgba(255,255,255,.12);display:flex;align-items:center;justify-content:center;font-size:20px;flex-shrink:0;}
    .push-preview .pp-body{flex:1;min-width:0;}
    .push-preview .pp-app{font-size:10.5px;color:rgba(255,255,255,.55);margin-bottom:3px;font-weight:600;}
    .push-preview .pp-title{font-size:13.5px;font-weight:700;margin-bottom:3px;word-wrap:break-word;}
    .push-preview .pp-text{font-size:12.5px;color:rgba(255,255,255,.8);line-height:1.4;word-wrap:break-word;}

    .push-tpl-card{background:#fff;border:1px solid var(--d-border);border-radius:12px;padding:14px;margin-bottom:10px;display:flex;align-items:flex-start;gap:12px;}
    .push-tpl-icon{width:40px;height:40px;border-radius:10px;display:flex;align-items:center;justify-content:center;font-size:20px;flex-shrink:0;}
    .push-tpl-info{flex:1;min-width:0;}
    .push-tpl-name{font-size:13px;font-weight:700;color:var(--d-text);display:flex;align-items:center;gap:8px;flex-wrap:wrap;}
    .push-tpl-badge{padding:1.5px 8px;border-radius:20px;font-size:10px;font-weight:800;text-transform:uppercase;letter-spacing:.4px;}
    .push-tpl-title{font-size:12.5px;color:var(--d-text);margin-top:4px;}
    .push-tpl-body{font-size:11.5px;color:var(--d-muted);margin-top:2px;display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;overflow:hidden;}

    .push-hist-card{background:#fff;border:1px solid var(--d-border);border-radius:12px;padding:14px;margin-bottom:10px;}
    .push-hist-head{display:flex;align-items:flex-start;gap:10px;margin-bottom:8px;}
    .push-hist-title{font-size:13px;font-weight:700;color:var(--d-text);flex:1;}
    .push-hist-body{font-size:12px;color:var(--d-muted);line-height:1.5;margin-bottom:8px;}
    .push-hist-meta{font-size:11px;color:var(--d-muted);display:flex;align-items:center;gap:10px;flex-wrap:wrap;}
    .push-hist-meta b{color:var(--d-text);}
    .push-status-badge{padding:2px 9px;border-radius:20px;font-size:10px;font-weight:800;text-transform:uppercase;letter-spacing:.4px;}
    .push-status-sent{background:#dcfce7;color:#166534;}
    .push-status-scheduled{background:#dbeafe;color:#1e40af;}
    .push-status-failed{background:#fee2e2;color:#991b1b;}
    .push-status-cancelled{background:#f1f5f9;color:#475569;}
    .push-status-pending{background:#fef3c7;color:#854d0e;}

    .push-cust-row{display:flex;align-items:center;gap:8px;padding:6px 8px;border-radius:6px;cursor:pointer;}
    .push-cust-row:hover{background:#f8faff;}
    .push-cust-row.picked{background:#eff6ff;}
    .push-cust-row input{width:14px;height:14px;cursor:pointer;margin:0;}
    .push-cust-name{flex:1;min-width:0;font-weight:600;color:var(--d-text);}
    .push-cust-meta{font-size:10.5px;color:var(--d-muted);}
    .push-cust-sub{width:8px;height:8px;border-radius:50%;background:#10b981;flex-shrink:0;}
    .push-cust-sub.off{background:#cbd5e1;}

    @media(max-width:900px){
      #pushStatsWrap{grid-template-columns:repeat(2,1fr)!important;}
      #push-tab-send > div{grid-template-columns:1fr!important;}
    }
  </style>
</head>
<body>
<div class="app-layout st-page">
  <%- include('../partials/sidebar') %>
  <div class="main-content">
    <%- include('../partials/topbar') %>
    <main class="page-content">

      <div style="margin-bottom:20px;" class="a1">
        <h1 style="font-size:21px;font-weight:800;color:#0d1b3e;margin:0 0 3px;">Settings</h1>
        <p style="font-size:12.5px;color:var(--d-muted);margin:0;">Konfigurasi aplikasi, tampilan, dan preferensi sistem</p>
      </div>

      <div class="st-layout">
        <!-- Nav kiri -->
        <div class="st-nav a2">
          <div class="st-nav-item active" onclick="showSection('brand',this)">
            <svg width="15" height="15" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M7 21a4 4 0 01-4-4V5a2 2 0 012-2h4a2 2 0 012 2v12a4 4 0 01-4 4zm0 0h12a2 2 0 002-2v-4a2 2 0 00-2-2h-2.343M11 7.343l1.657-1.657a2 2 0 012.828 0l2.829 2.829a2 2 0 010 2.828l-8.486 8.485M7 17h.01"/></svg>
            Tampilan & Brand
          </div>
          <div class="st-nav-item" onclick="showSection('general',this)">
            <svg width="15" height="15" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><circle cx="12" cy="12" r="3"/><path d="M19.4 15a1.65 1.65 0 00.33 1.82l.06.06a2 2 0 010 2.83 2 2 0 01-2.83 0l-.06-.06a1.65 1.65 0 00-1.82-.33 1.65 1.65 0 00-1 1.51V21a2 2 0 01-4 0v-.09A1.65 1.65 0 009 19.4a1.65 1.65 0 00-1.82.33l-.06.06a2 2 0 01-2.83-2.83l.06-.06A1.65 1.65 0 004.68 15a1.65 1.65 0 00-1.51-1H3a2 2 0 010-4h.09A1.65 1.65 0 004.6 9a1.65 1.65 0 00-.33-1.82l-.06-.06a2 2 0 012.83-2.83l.06.06A1.65 1.65 0 009 4.68a1.65 1.65 0 001-1.51V3a2 2 0 014 0v.09a1.65 1.65 0 001 1.51 1.65 1.65 0 001.82-.33l.06-.06a2 2 0 012.83 2.83l-.06.06A1.65 1.65 0 0019.4 9a1.65 1.65 0 001.51 1H21a2 2 0 010 4h-.09a1.65 1.65 0 00-1.51 1z"/></svg>
            Umum
          </div>
          <div class="st-nav-sep"></div>
          <div class="st-nav-item" onclick="showSection('payment',this)">
            <svg width="15" height="15" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><rect x="2" y="5" width="20" height="14" rx="2"/><line x1="2" y1="10" x2="22" y2="10"/></svg>
            Pembayaran
          </div>
          <div class="st-nav-item" onclick="showSection('gateway',this)">
            <svg width="15" height="15" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path d="M13 2L3 14h9l-1 8 10-12h-9l1-8z"/></svg>
            Payment Gateway
          </div>
          <div class="st-nav-item" onclick="showSection('push',this)">
            <svg width="15" height="15" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path d="M18 8A6 6 0 006 8c0 7-3 9-3 9h18s-3-2-3-9"/><path d="M13.73 21a2 2 0 01-3.46 0"/></svg>
            Push Notification
          </div>
          <div class="st-nav-item" onclick="showSection('announcements',this)">
            <svg width="15" height="15" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path d="M22 17H2a3 3 0 000 6h20v-6z"/><path d="M6 17V7a2 2 0 012-2h8a2 2 0 012 2v10"/><line x1="12" y1="12" x2="12" y2="17"/></svg>
            Pengumuman Portal
          </div>
          <div class="st-nav-item" onclick="showSection('telegram',this)">
            <svg width="15" height="15" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path d="M21.5 4.5L2.5 12.5l5.5 2 2 6 3-4 5 4z"/></svg>
            Telegram Monitoring
          </div>
          <div class="st-nav-sep"></div>
          <div class="st-nav-item" onclick="showSection('isolir',this)">
            <svg width="15" height="15" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><circle cx="12" cy="12" r="10"/><line x1="4.93" y1="4.93" x2="19.07" y2="19.07"/></svg>
            Isolir &amp; Suspend
          </div>
          <div class="st-nav-item" onclick="showSection('wagateway',this)">
            <svg width="15" height="15" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path d="M21 11.5a8.38 8.38 0 0 1-.9 3.8 8.5 8.5 0 0 1-7.6 4.7 8.38 8.38 0 0 1-3.8-.9L3 21l1.9-5.7a8.38 8.38 0 0 1-.9-3.8 8.5 8.5 0 0 1 4.7-7.6 8.38 8.38 0 0 1 3.8-.9h.5a8.48 8.48 0 0 1 8 8z"/></svg>
            WhatsApp Gateway
          </div>
          <div class="st-nav-item" onclick="showSection('invoice',this)">
            <svg width="15" height="15" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="9" y1="13" x2="15" y2="13"/><line x1="9" y1="17" x2="13" y2="17"/></svg>
            Invoice &amp; Penomoran
          </div>
          <div class="st-nav-item" onclick="showSection('theme',this)">
            <svg width="15" height="15" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><circle cx="13.5" cy="6.5" r="2.5"/><circle cx="17.5" cy="10.5" r="2.5"/><circle cx="8.5" cy="7.5" r="2.5"/><circle cx="6.5" cy="12.5" r="2.5"/><path d="M12 2a10 10 0 0 0 0 20 1.5 1.5 0 0 0 1.5-1.5 1.49 1.49 0 0 0-.39-1 1.49 1.49 0 0 1-.39-1 1.5 1.5 0 0 1 1.5-1.5H16a4 4 0 0 0 4-4 8 8 0 0 0-8-8z"/></svg>
            Tema Warna
          </div>
          <div class="st-nav-item" onclick="showSection('email',this)">
            <svg width="15" height="15" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path d="M4 4h16a2 2 0 0 1 2 2v12a2 2 0 0 1-2 2H4a2 2 0 0 1-2-2V6a2 2 0 0 1 2-2z"/><polyline points="22,6 12,13 2,6"/></svg>
            Email / SMTP
          </div>
          <div class="st-nav-item" onclick="showSection('locale',this)">
            <svg width="15" height="15" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><circle cx="12" cy="12" r="10"/><line x1="2" y1="12" x2="22" y2="12"/><path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"/></svg>
            Localization
          </div>
          <div class="st-nav-item" onclick="showSection('backup',this)">
            <svg width="15" height="15" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M4 4v5h5M20 20v-5h-5"/><path stroke-linecap="round" stroke-linejoin="round" d="M5.5 9a7 7 0 0111.9-2.4L20 9M18.5 15a7 7 0 01-11.9 2.4L4 15"/></svg>
            Backup &amp; Restore
          </div>
          <div class="st-nav-sep"></div>
          <div class="st-nav-item" onclick="window.location='/settings/users'">
            <svg width="15" height="15" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path d="M20 21v-2a4 4 0 00-4-4H8a4 4 0 00-4 4v2"/><circle cx="12" cy="7" r="4"/></svg>
            Users & Roles
          </div>
          <div class="st-nav-item" onclick="window.location='/system/database'">
            <svg width="15" height="15" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><ellipse cx="12" cy="5" rx="9" ry="3"/><path d="M3 5v14a9 3 0 0 0 18 0V5"/><path d="M3 12a9 3 0 0 0 18 0"/></svg>
            Database Management
          </div>
        </div>

        <!-- Content -->
        <div class="a3">

          <!-- BRAND SECTION -->
          <div class="st-section show" id="sec-brand">
            <div class="st-card">
              <div class="st-card-hdr">
                <div class="st-card-icon"><svg width="16" height="16" fill="none" stroke="#fff" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M7 21a4 4 0 01-4-4V5a2 2 0 012-2h4a2 2 0 012 2v12a4 4 0 01-4 4zm0 0h12a2 2 0 002-2v-4a2 2 0 00-2-2h-2.343M11 7.343l1.657-1.657a2 2 0 012.828 0l2.829 2.829a2 2 0 010 2.828l-8.486 8.485M7 17h.01"/></svg></div>
                <div>
                  <div style="font-size:14px;font-weight:700;color:var(--d-text);">Tampilan Judul Aplikasi</div>
                  <div style="font-size:12px;color:var(--d-muted);">Atur tampilan nama/logo di sidebar</div>
                </div>
              </div>
              <div class="st-card-body">

                <!-- Mode selection -->
                <div class="pf-field">
                  <label class="pf-label">Mode Tampilan</label>
                  <%
                    // Preview text — pakai nama/tagline dari setting (fallback ke default)
                    const previewName    = appSettings?.app_name    || 'Skynet';
                    const previewTagline = appSettings?.app_tagline || 'ISP Management';
                  %>
                  <div class="brand-grid">
                    <div class="brand-option <%- (appSettings?.brand_mode||'name_tagline')==='name_tagline'?'selected':'' %>" onclick="selectBrand('name_tagline',this)">
                      <div class="brand-preview">
                        <div class="mini-icon"><svg width="10" height="10" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2"><circle cx="12" cy="12" r="10"/><path d="M12 6v6l4 2"/></svg></div>
                        <div style="text-align:left;min-width:0;flex:1;overflow:hidden;">
                          <div id="bopt1Name" style="font-size:10px;font-weight:800;color:#0d1b3e;line-height:1.1;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;"><%= previewName %></div>
                          <div id="bopt1Tag"  style="font-size:7.5px;color:#94a3b8;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;"><%= previewTagline %></div>
                        </div>
                      </div>
                      <div class="brand-option-title">Nama + Tagline</div>
                      <div class="brand-option-desc">Ikon + nama + tagline kecil</div>
                    </div>
                    <div class="brand-option <%- (appSettings?.brand_mode||'')==='name_logo'?'selected':'' %>" onclick="selectBrand('name_logo',this)">
                      <div class="brand-preview">
                        <div style="width:22px;height:22px;border-radius:6px;background:#e2e8f0;display:flex;align-items:center;justify-content:center;font-size:8px;color:#64748b;flex-shrink:0;">IMG</div>
                        <div id="bopt2Name" style="font-size:10px;font-weight:800;color:#0d1b3e;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;min-width:0;flex:1;"><%= previewName %></div>
                      </div>
                      <div class="brand-option-title">Logo + Nama</div>
                      <div class="brand-option-desc">Logo custom + nama teks</div>
                    </div>
                    <div class="brand-option <%- (appSettings?.brand_mode||'')==='logo_only'?'selected':'' %>" onclick="selectBrand('logo_only',this)">
                      <div class="brand-preview">
                        <div style="width:80px;height:26px;background:#e2e8f0;border-radius:5px;display:flex;align-items:center;justify-content:center;font-size:8px;color:#64748b;">LOGO</div>
                      </div>
                      <div class="brand-option-title">Logo Only</div>
                      <div class="brand-option-desc">Hanya gambar logo</div>
                    </div>
                  </div>
                  <input type="hidden" id="brandMode" value="<%= appSettings?.brand_mode||'name_tagline' %>">
                </div>

                <div class="divider"></div>

                <div style="display:grid;grid-template-columns:1fr 1fr;gap:14px;">
                  <div class="pf-field" style="margin-bottom:0;">
                    <label class="pf-label">Nama Aplikasi</label>
                    <input type="text" class="pf-input" id="appName" value="<%= appSettings?.app_name||'Skynet' %>" oninput="updatePreview()">
                  </div>
                  <div class="pf-field" style="margin-bottom:0;" id="taglineField">
                    <label class="pf-label">Tagline</label>
                    <input type="text" class="pf-input" id="appTagline" value="<%= appSettings?.app_tagline||'ISP Management' %>" placeholder="ISP Management" oninput="updatePreview()">
                  </div>
                </div>

                <div class="pf-field" style="margin-top:14px;" id="logoField">
                  <label class="pf-label">Logo Aplikasi <span style="font-weight:400;text-transform:none;color:var(--d-muted);">(PNG/SVG/WebP, maks 2MB)</span></label>

                  <!-- Upload area -->
                  <div id="logoDropArea" onclick="document.getElementById('logoFileInput').click()" ondragover="event.preventDefault();this.style.borderColor='#1a6ef5';" ondragleave="this.style.borderColor='';" ondrop="handleLogoDrop(event)"
                    style="border:2px dashed #e0e7f3;border-radius:12px;padding:20px;text-align:center;cursor:pointer;transition:all .15s;margin-bottom:10px;background:#f8faff;">
                    <div id="logoPreviewThumb" style="display:none;margin-bottom:10px;">
                      <img id="logoThumbImg" src="" alt="logo" style="max-height:52px;max-width:160px;object-fit:contain;border-radius:6px;border:1px solid #e0e7f3;padding:4px;background:#fff;">
                    </div>
                    <div id="logoDropText">
                      <svg xmlns="http://www.w3.org/2000/svg" width="28" height="28" fill="none" viewBox="0 0 24 24" stroke="#94a3b8" stroke-width="1.5" style="margin-bottom:6px;"><path stroke-linecap="round" stroke-linejoin="round" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-8l-4-4m0 0L8 8m4-4v12"/></svg>
                      <div style="font-size:13px;font-weight:600;color:#374151;">Klik atau drag & drop logo</div>
                      <div style="font-size:11.5px;color:#94a3b8;margin-top:2px;">PNG, SVG, JPG, WebP — maks 2MB</div>
                    </div>
                    <input type="file" id="logoFileInput" accept=".png,.jpg,.jpeg,.svg,.webp" style="display:none;" onchange="handleLogoFile(this.files[0])">
                  </div>

                  <!-- URL manual -->
                  <div style="display:flex;align-items:center;gap:8px;">
                    <div style="flex:1;position:relative;">
                      <input type="text" class="pf-input" id="logoUrl" value="<%= appSettings?.logo_url||'' %>" placeholder="Atau masukkan URL: https://..." oninput="updatePreview()" style="padding-right:80px;">
                      <span style="position:absolute;right:10px;top:50%;transform:translateY(-50%);font-size:10.5px;color:#94a3b8;pointer-events:none;">URL manual</span>
                    </div>
                    <button type="button" onclick="uploadLogo()" id="btnUploadLogo" style="padding:10px 16px;border-radius:9px;border:none;background:linear-gradient(135deg,#1a6ef5,#0047cc);color:#fff;font-size:12.5px;font-weight:700;cursor:pointer;white-space:nowrap;display:none;font-family:'DM Sans',sans-serif;">
                      Upload
                    </button>
                  </div>
                  <div class="pf-hint">Disarankan format PNG/SVG transparan, tinggi 32–40px.</div>
                </div>

                <!-- Favicon Upload -->
                <div class="pf-field" style="margin-top:14px;" id="faviconField">
                  <label class="pf-label">Favicon <span style="font-weight:400;text-transform:none;color:var(--d-muted);">(ICO/PNG/SVG, maks 1MB — tampil di tab browser)</span></label>

                  <div id="faviconDropArea" onclick="document.getElementById('faviconFileInput').click()" ondragover="event.preventDefault();this.style.borderColor='#1a6ef5';" ondragleave="this.style.borderColor='';" ondrop="handleFaviconDrop(event)"
                    style="border:2px dashed #e0e7f3;border-radius:12px;padding:20px;text-align:center;cursor:pointer;transition:all .15s;margin-bottom:10px;background:#f8faff;">
                    <div id="faviconPreviewThumb" style="display:<%= appSettings?.favicon_url ? 'block' : 'none' %>;margin-bottom:10px;">
                      <img id="faviconThumbImg" src="<%= appSettings?.favicon_url || '' %>" alt="favicon" style="height:32px;width:32px;object-fit:contain;border-radius:6px;border:1px solid #e0e7f3;padding:3px;background:#fff;">
                      <span style="display:inline-block;margin-left:8px;vertical-align:middle;font-size:12px;color:#374151;font-weight:500;">Favicon aktif</span>
                    </div>
                    <div id="faviconDropText">
                      <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24" stroke="#94a3b8" stroke-width="1.5" style="margin-bottom:4px;"><circle cx="12" cy="12" r="9"/><path d="M12 7v5l3 2"/></svg>
                      <div style="font-size:13px;font-weight:600;color:#374151;">Klik atau drag & drop favicon</div>
                      <div style="font-size:11.5px;color:#94a3b8;margin-top:2px;">ICO, PNG, SVG — ukuran 32×32 atau 64×64 piksel</div>
                    </div>
                    <input type="file" id="faviconFileInput" accept=".ico,.png,.svg,.jpg,.jpeg" style="display:none;" onchange="handleFaviconFile(this.files[0])">
                  </div>

                  <div style="display:flex;align-items:center;gap:8px;">
                    <button type="button" onclick="uploadFavicon()" id="btnUploadFavicon" style="padding:9px 16px;border-radius:9px;border:none;background:linear-gradient(135deg,#1a6ef5,#0047cc);color:#fff;font-size:12.5px;font-weight:700;cursor:pointer;display:none;font-family:'DM Sans',sans-serif;">
                      Upload Favicon
                    </button>
                    <button type="button" onclick="resetFavicon()" id="btnResetFavicon" style="padding:9px 16px;border-radius:9px;border:1px solid #e0e7f3;background:#fff;color:#dc2626;font-size:12.5px;font-weight:600;cursor:pointer;font-family:'DM Sans',sans-serif;display:<%= appSettings?.favicon_url ? 'inline-block' : 'none' %>;">
                      Reset ke Default
                    </button>
                  </div>
                  <div class="pf-hint">Setelah upload, refresh halaman (Ctrl+F5) untuk melihat favicon baru di tab browser.</div>
                </div>

                <!-- Background Halaman Pendaftaran Publik -->
                <div class="pf-field" style="margin-top:14px;">
                  <label class="pf-label">Background Halaman Pendaftaran <span style="font-weight:400;text-transform:none;color:var(--d-muted);">(foto latar di halaman /registrasi-layanan)</span></label>
                  <div id="registerBgPreview" style="position:relative;width:100%;aspect-ratio:16/10;max-height:220px;border:1.5px dashed var(--d-border);border-radius:14px;overflow:hidden;background:#f8fafc;display:flex;align-items:center;justify-content:center;margin-bottom:10px;">
                    <!-- diisi JS -->
                  </div>
                  <div style="display:flex;gap:10px;flex-wrap:wrap">
                    <label class="pf-btn-outline" style="flex:1;min-width:140px;cursor:pointer;">
                      <svg width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M4 16v2a2 2 0 002 2h12a2 2 0 002-2v-2M16 8l-4-4m0 0L8 8m4-4v12"/></svg>
                      Pilih / Upload Gambar
                      <input type="file" id="registerBgInput" accept="image/png,image/jpeg,image/webp" style="display:none" onchange="onRegisterBgPicked(event)">
                    </label>
                    <button type="button" class="pf-btn-outline" style="color:#dc2626;border-color:#fecaca;" onclick="deleteRegisterBg()">
                      <svg width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.2" viewBox="0 0 24 24"><polyline points="3 6 5 6 21 6"/><path d="M19 6v14a2 2 0 01-2 2H7a2 2 0 01-2-2V6m3 0V4a2 2 0 012-2h4a2 2 0 012 2v2"/></svg>
                      Hapus
                    </button>
                  </div>
                  <div class="pf-hint">Format PNG/JPG/WebP · max 5MB · rekomendasi foto landscape resolusi tinggi (≥1600px). Gambar tersimpan di server.</div>
                  <!-- URL manual (alternatif) -->
                  <div style="display:flex;align-items:center;gap:8px;margin-top:8px;">
                    <div style="flex:1;position:relative;">
                      <input type="text" class="pf-input" id="registerBgUrl" value="<%= appSettings?.register_bg_url||'' %>" placeholder="atau tempel URL gambar: https://..." oninput="renderRegisterBgPreview()" style="padding-right:80px;">
                      <span style="position:absolute;right:10px;top:50%;transform:translateY(-50%);font-size:10.5px;color:#94a3b8;pointer-events:none;">URL manual</span>
                    </div>
                  </div>
                  <div style="margin-top:10px;">
                    <label class="pf-label" style="font-size:11.5px;">Transparansi Overlay Putih
                      <span style="font-weight:400;text-transform:none;color:var(--d-muted);">(agar form tetap terbaca — 0 = foto penuh, 1 = putih solid)</span>
                    </label>
                    <div style="display:flex;align-items:center;gap:12px;">
                      <input type="range" id="registerBgOverlay" min="0" max="1" step="0.01" value="<%= appSettings?.register_bg_overlay||'0.82' %>" oninput="document.getElementById('registerBgOverlayVal').textContent=this.value;renderRegisterBgPreview()" style="flex:1;">
                      <span id="registerBgOverlayVal" style="font-size:12.5px;font-weight:700;color:#374151;min-width:36px;text-align:right;"><%= appSettings?.register_bg_overlay||'0.82' %></span>
                    </div>
                  </div>
                  <div class="pf-hint">Kosongkan untuk memakai pola titik default.</div>
                </div>

                <!-- Live preview -->
                <div class="divider"></div>
                <label class="pf-label">Preview Sidebar</label>
                <div class="brand-live-preview" id="brandPreview">
                  <!-- diisi JS -->
                </div>

                <button class="save-btn" onclick="saveBrandSettings()">
                  <svg width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7"/></svg>
                  Simpan Pengaturan
                </button>
              </div>
            </div>

            <!-- Portal Customer Hero Image -->
            <div class="st-card" style="margin-top:14px">
              <div class="st-card-hdr">
                <div class="st-card-icon" style="background:linear-gradient(135deg,#0891b2,#0e7490)">
                  <svg width="16" height="16" fill="none" stroke="#fff" stroke-width="2" viewBox="0 0 24 24"><rect x="3" y="3" width="18" height="18" rx="2"/><circle cx="8.5" cy="8.5" r="1.5"/><polyline points="21 15 16 10 5 21"/></svg>
                </div>
                <div>
                  <div style="font-size:14px;font-weight:700;color:var(--d-text);">Foto Hero Portal Pelanggan</div>
                  <div style="font-size:12px;color:var(--d-muted);">Background gambar di halaman login portal customer</div>
                </div>
              </div>
              <div class="st-card-body">
                <!-- Preview -->
                <div class="pf-field">
                  <label class="pf-label">Preview</label>
                  <div id="portalHeroPreview" style="position:relative;width:100%;aspect-ratio:16/10;max-height:240px;border:1.5px dashed var(--d-border);border-radius:14px;overflow:hidden;background:#f8fafc;display:flex;align-items:center;justify-content:center;">
                    <!-- diisi JS -->
                  </div>
                </div>

                <div style="display:flex;gap:10px;flex-wrap:wrap">
                  <label class="pf-btn-outline" style="flex:1;min-width:140px;cursor:pointer;">
                    <svg width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M4 16v2a2 2 0 002 2h12a2 2 0 002-2v-2M16 8l-4-4m0 0L8 8m4-4v12"/></svg>
                    Pilih Gambar
                    <input type="file" id="portalHeroInput" accept="image/png,image/jpeg,image/webp" style="display:none" onchange="onPortalHeroPicked(event)">
                  </label>
                  <button class="pf-btn-outline" style="color:#dc2626;border-color:#fecaca;" onclick="deletePortalHero()">
                    <svg width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.2" viewBox="0 0 24 24"><polyline points="3 6 5 6 21 6"/><path d="M19 6v14a2 2 0 01-2 2H7a2 2 0 01-2-2V6m3 0V4a2 2 0 012-2h4a2 2 0 012 2v2"/></svg>
                    Hapus
                  </button>
                </div>
                <div class="pf-hint">Format PNG/JPG/WebP · max 4MB · rekomendasi rasio 3:2 atau 16:10 · resolusi minimum 1200px.</div>

                <!-- Overlay opacity slider -->
                <div class="pf-field" style="margin-top:14px">
                  <label class="pf-label">Opacity Overlay Biru</label>
                  <div style="display:flex;align-items:center;gap:12px">
                    <input type="range" id="portalHeroOverlay" min="0" max="1" step="0.05" value="0.55" style="flex:1" oninput="document.getElementById('portalHeroOverlayVal').textContent=this.value">
                    <span id="portalHeroOverlayVal" style="font-size:12px;font-weight:700;color:var(--d-text);font-family:monospace;min-width:36px;text-align:center;">0.55</span>
                  </div>
                  <div class="pf-hint">0 = foto terlihat jelas, 1 = overlay biru penuh. Rekomendasi 0.45-0.65.</div>
                </div>

                <!-- Welcome title & subtitle -->
                <div class="pf-field" style="margin-top:10px">
                  <label class="pf-label">Welcome Title <span style="color:var(--d-muted);font-weight:400">(opsional)</span></label>
                  <input type="text" class="pf-input" id="portalWelcomeTitle" placeholder="Welcome Back">
                </div>
                <div class="pf-field">
                  <label class="pf-label">Welcome Subtitle <span style="color:var(--d-muted);font-weight:400">(opsional)</span></label>
                  <input type="text" class="pf-input" id="portalWelcomeSub" placeholder="Masukkan detail akun Anda">
                </div>
                <div class="pf-field">
                  <label class="pf-label">Teks Link Syarat &amp; Ketentuan <span style="color:var(--d-muted);font-weight:400">(opsional)</span></label>
                  <input type="text" class="pf-input" id="portalTermsLabel" placeholder="Syarat &amp; Ketentuan">
                  <div style="font-size:11px;color:var(--d-muted);margin-top:4px">Teks tautan yang ditampilkan, mis. "Term of Service", "Kebijakan Privasi". Kosong = "Syarat &amp; Ketentuan".</div>
                </div>
                <div class="pf-field">
                  <label class="pf-label">Link Syarat &amp; Ketentuan <span style="color:var(--d-muted);font-weight:400">(opsional)</span></label>
                  <input type="url" class="pf-input" id="portalTermsUrl" placeholder="https://website-anda.com/syarat-ketentuan">
                  <div style="font-size:11px;color:var(--d-muted);margin-top:4px">Muncul sebagai tautan di footer halaman login portal pelanggan. Kosongkan untuk menyembunyikan.</div>
                </div>

                <button class="save-btn" onclick="savePortalHeroSettings()" style="margin-top:6px">
                  <svg width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7"/></svg>
                  Simpan Pengaturan
                </button>
              </div>
            </div>
          </div>

          <!-- GENERAL SECTION -->
          <div class="st-section" id="sec-general">
            <div class="st-card">
              <div class="st-card-hdr">
                <div class="st-card-icon"><svg width="16" height="16" fill="none" stroke="#fff" stroke-width="2" viewBox="0 0 24 24"><circle cx="12" cy="12" r="3"/><path d="M12 2v3m0 14v3M4.22 4.22l2.12 2.12m11.32 11.32l2.12 2.12M2 12h3m14 0h3M4.22 19.78l2.12-2.12M17.66 6.34l2.12-2.12"/></svg></div>
                <div>
                  <div style="font-size:14px;font-weight:700;color:var(--d-text);">Pengaturan Umum</div>
                  <div style="font-size:12px;color:var(--d-muted);">Konfigurasi dasar sistem</div>
                </div>
              </div>
              <div class="st-card-body">
                <div class="pf-field">
                  <label class="pf-label">Nama Perusahaan</label>
                  <input type="text" class="pf-input" id="companyName" value="<%= appSettings?.company_name||'' %>" placeholder="PT. Nama Perusahaan">
                </div>
                <div class="pf-field">
                  <label class="pf-label">Nomor WhatsApp Customer Service</label>
                  <input type="text" class="pf-input" id="companyWhatsapp" value="<%= appSettings?.company_whatsapp||'' %>" placeholder="6281234567890">
                  <div class="pf-hint">Nomor CS yang dipakai di Customer Portal — tombol <b>Hubungi CS</b> &amp; <b>Lupa password</b> di halaman login, serta tombol konfirmasi pembayaran. Format: kode negara tanpa '+' (contoh: <b>6281234567890</b>).</div>
                </div>
                <div class="pf-field">
                  <label class="pf-label">Alamat Perusahaan</label>
                  <textarea class="pf-input" id="companyAddress" rows="2" placeholder="Jl. Contoh No. 123, Kota, Provinsi" style="resize:vertical;font-family:inherit;"><%= appSettings?.company_address||'' %></textarea>
                  <div class="pf-hint">Alamat yang ditampilkan di footer halaman pembayaran publik &amp; invoice.</div>
                </div>
                <div class="pf-field">
                  <label class="pf-label">Website</label>
                  <input type="text" class="pf-input" id="companyWebsite" value="<%= appSettings?.company_website||'' %>" placeholder="https://www.contoh.com">
                  <div class="pf-hint">Alamat website perusahaan yang ditampilkan di footer halaman pembayaran publik &amp; invoice.</div>
                </div>
                <div class="pf-field">
                  <label class="pf-label">SNMP Community</label>
                  <input type="text" class="pf-input" id="snmpCommunity" value="<%= appSettings?.snmp_community||'public' %>">
                </div>
                <div class="pf-field">
                  <label class="pf-label">Poll Interval (detik)</label>
                  <input type="number" class="pf-input" id="pollInterval" value="<%= appSettings?.poll_interval||'60' %>" min="10" max="300">
                </div>
                <button class="save-btn" onclick="saveGeneralSettings()">
                  <svg width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7"/></svg>
                  Simpan Pengaturan
                </button>
              </div>
            </div>
          </div>

          <!-- PAYMENT SECTION -->
          <div class="st-section" id="sec-payment">

            <!-- ═══ PPN / TAX CONFIG ═══ -->
            <div class="st-card" style="margin-bottom:16px;">
              <div class="st-card-hdr">
                <div class="st-card-icon" style="background:linear-gradient(135deg,#0d9488,#0f766e);">
                  <svg width="16" height="16" fill="none" stroke="#fff" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M9 8h6M9 12h6M9 16h3M5 21h14a2 2 0 002-2V5a2 2 0 00-2-2H5a2 2 0 00-2 2v14a2 2 0 002 2z"/></svg>
                </div>
                <div style="flex:1">
                  <div style="font-size:14px;font-weight:700;color:var(--d-text);">PPN / Pajak</div>
                  <div style="font-size:12px;color:var(--d-muted);">Atur tarif PPN yang otomatis ditambahkan pada tagihan baru</div>
                </div>
                <label style="display:inline-flex;align-items:center;gap:9px;cursor:pointer;user-select:none;position:relative;">
                  <span style="font-size:12px;font-weight:700;color:var(--d-text);" id="taxEnabledLbl"><%= (appSettings?.tax_enabled === '1' || appSettings?.tax_enabled === 'true') ? 'Aktif' : 'Nonaktif' %></span>
                  <input type="checkbox" id="taxEnabled" <%= (appSettings?.tax_enabled === '1' || appSettings?.tax_enabled === 'true') ? 'checked' : '' %> onchange="onTaxEnabledChange()" style="position:absolute;opacity:0;pointer-events:none;">
                  <span class="tax-switch"></span>
                </label>
              </div>
              <div class="st-card-body" id="taxCardBody" style="padding:16px 20px">
                <div style="background:#f0fdfa;border:1px solid #99f6e4;border-radius:10px;padding:11px 14px;margin-bottom:14px;display:flex;gap:10px;align-items:flex-start;font-size:12px;color:#115e59;line-height:1.55;">
                  <svg width="16" height="16" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24" style="flex-shrink:0;margin-top:1px"><circle cx="12" cy="12" r="10"/><line x1="12" y1="16" x2="12" y2="12"/><line x1="12" y1="8" x2="12.01" y2="8"/></svg>
                  <div>
                    PPN ini diterapkan otomatis saat <b>generate tagihan baru</b>. Tagihan yang sudah dibuat tidak berubah. Pastikan tarif sesuai aturan pajak terbaru (PPN umum di Indonesia 11%, beberapa kategori 12% sejak Januari 2025).
                  </div>
                </div>

                <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(180px,1fr));gap:14px;margin-bottom:14px;">
                  <div class="pf-field" style="margin:0">
                    <label class="pf-label">Tarif PPN (%)</label>
                    <div style="display:flex;gap:6px;align-items:center;">
                      <input type="number" class="pf-input" id="taxRate" value="<%= appSettings?.tax_rate || '11' %>" min="0" max="100" step="0.01" style="flex:1" oninput="updateTaxPreview()">
                      <span style="font-size:14px;font-weight:700;color:var(--d-muted);">%</span>
                    </div>
                    <div class="pf-hint" style="display:flex;align-items:center;gap:5px;flex-wrap:wrap;">Preset:
                      <button type="button" class="tax-preset" onclick="setTaxRate(11)">11%</button>
                      <button type="button" class="tax-preset" onclick="setTaxRate(12)">12%</button>
                      <button type="button" class="tax-preset" onclick="setTaxRate(0)">0%</button>
                    </div>
                  </div>
                  <div class="pf-field" style="margin:0">
                    <label class="pf-label">Mode Perhitungan</label>
                    <select class="pf-select" id="taxMode" onchange="updateTaxPreview()">
                      <option value="exclusive" <%= (appSettings?.tax_mode || 'exclusive') === 'exclusive' ? 'selected' : '' %>>Exclusive — Harga + PPN</option>
                      <option value="inclusive" <%= appSettings?.tax_mode === 'inclusive' ? 'selected' : '' %>>Inclusive — Harga sudah termasuk PPN</option>
                    </select>
                  </div>
                  <div class="pf-field" style="margin:0">
                    <label class="pf-label">Label di Invoice</label>
                    <input type="text" class="pf-input" id="taxLabel" value="<%= appSettings?.tax_label || 'PPN' %>" placeholder="PPN" maxlength="30">
                    <div class="pf-hint">Contoh: PPN, VAT, Pajak Layanan</div>
                  </div>
                </div>

                <!-- Live preview -->
                <div style="background:#f8fafc;border:1px solid #e2e8f0;border-radius:10px;padding:14px;">
                  <div style="font-size:11px;font-weight:700;color:#64748b;text-transform:uppercase;letter-spacing:.06em;margin-bottom:10px;display:flex;align-items:center;gap:6px;">
                    <svg width="13" height="13" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/><circle cx="12" cy="12" r="3"/></svg>
                    Preview Perhitungan
                  </div>
                  <div style="display:flex;align-items:center;gap:14px;flex-wrap:wrap;">
                    <div style="display:flex;align-items:center;gap:8px;">
                      <label style="font-size:12px;color:#64748b;">Harga paket:</label>
                      <div style="display:flex;align-items:center;gap:4px;background:#fff;border:1px solid #e2e8f0;border-radius:8px;padding:4px 10px;">
                        <span style="font-size:12px;color:#94a3b8;">Rp</span>
                        <input type="number" id="taxPreviewBase" value="100000" min="0" step="1000" oninput="updateTaxPreview()" style="border:none;outline:none;width:90px;font-size:13px;font-weight:700;color:#0f172a;font-family:'DM Mono',monospace;">
                      </div>
                    </div>
                    <div style="flex:1;min-width:240px;display:grid;grid-template-columns:repeat(3,1fr);gap:8px;text-align:right;">
                      <div>
                        <div style="font-size:10px;color:#94a3b8;text-transform:uppercase;letter-spacing:.06em;font-weight:700;">Subtotal</div>
                        <div id="taxPrevSubtotal" style="font-size:13px;font-weight:700;color:#0f172a;font-family:'DM Mono',monospace;">Rp 100.000</div>
                      </div>
                      <div>
                        <div style="font-size:10px;color:#0d9488;text-transform:uppercase;letter-spacing:.06em;font-weight:700;" id="taxPrevTaxLbl">PPN 11%</div>
                        <div id="taxPrevTax" style="font-size:13px;font-weight:700;color:#0d9488;font-family:'DM Mono',monospace;">Rp 11.000</div>
                      </div>
                      <div>
                        <div style="font-size:10px;color:#1d4ed8;text-transform:uppercase;letter-spacing:.06em;font-weight:700;">Total</div>
                        <div id="taxPrevTotal" style="font-size:14px;font-weight:800;color:#1d4ed8;font-family:'DM Mono',monospace;">Rp 111.000</div>
                      </div>
                    </div>
                  </div>
                </div>

                <div style="display:flex;justify-content:space-between;align-items:center;gap:10px;flex-wrap:wrap;margin-top:14px;">
                  <button type="button" onclick="recalculateInvoiceTax()" id="recalcTaxBtn" style="display:inline-flex;align-items:center;gap:6px;padding:8px 13px;border-radius:9px;font-size:12.5px;font-weight:600;cursor:pointer;border:1.5px solid #cbd5e1;background:#fff;color:#475569;transition:all .15s;font-family:inherit;" onmouseover="this.style.background='#f8fafc'" onmouseout="this.style.background='#fff'" title="Terapkan setting PPN saat ini ke invoice yang masih unpaid/overdue">
                    <svg width="13" height="13" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"/></svg>
                    Recalculate ke Invoice Unpaid
                  </button>
                  <button class="save-btn" onclick="saveTaxSettings()">
                    <svg width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7"/></svg>
                    Simpan PPN
                  </button>
                </div>
              </div>
            </div>

            <!-- ═══ AUTO GENERATE INVOICE ═══ -->
            <div class="st-card" style="margin-bottom:16px;">
              <div class="st-card-hdr">
                <div class="st-card-icon" style="background:linear-gradient(135deg,#2563eb,#1d4ed8);">
                  <svg width="16" height="16" fill="none" stroke="#fff" stroke-width="2" viewBox="0 0 24 24"><rect x="3" y="4" width="18" height="18" rx="2" ry="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/><circle cx="12" cy="16" r="2.5" fill="#fff"/></svg>
                </div>
                <div style="flex:1">
                  <div style="font-size:14px;font-weight:700;color:var(--d-text);">Auto Generate Invoice</div>
                  <div style="font-size:12px;color:var(--d-muted);">Otomatis bikin tagihan untuk semua pelanggan aktif tiap awal bulan</div>
                </div>
                <label style="display:inline-flex;align-items:center;gap:9px;cursor:pointer;user-select:none;position:relative;">
                  <span style="font-size:12px;font-weight:700;color:var(--d-text);" id="autoGenLbl"><%= (appSettings?.auto_generate_invoice === '0' || appSettings?.auto_generate_invoice === 'false') ? 'Nonaktif' : 'Aktif' %></span>
                  <input type="checkbox" id="autoGenInvoice" <%= (appSettings?.auto_generate_invoice === '0' || appSettings?.auto_generate_invoice === 'false') ? '' : 'checked' %> onchange="onAutoGenChange()" style="position:absolute;opacity:0;pointer-events:none;">
                  <span class="autogen-switch"></span>
                </label>
              </div>
              <div class="st-card-body" style="padding:14px 20px">
                <div style="background:#eff6ff;border:1px solid #bfdbfe;border-radius:10px;padding:11px 14px;display:flex;gap:10px;align-items:flex-start;font-size:12px;color:#1e40af;line-height:1.6;">
                  <svg width="16" height="16" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24" style="flex-shrink:0;margin-top:1px"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>
                  <div>
                    Saat aktif, sistem otomatis generate invoice <b>tanggal 1 setiap bulan jam 01:30</b> untuk semua customer dengan status aktif. Periode mengikuti bulan saat itu, due date sesuai <code style="background:#dbeafe;padding:1px 5px;border-radius:4px;font-size:11px;">due_date</code> di profil customer.
                    <br>Kalau server di-restart di tengah bulan dan periode bulan ini belum di-generate sama sekali, sistem akan auto catch-up beberapa detik setelah server start.
                    <br><b style="color:#0d9488">Idempotent</b> — invoice yang sudah ada tidak akan di-duplikasi.
                  </div>
                </div>
              </div>
            </div>

            <!-- ═══ METODE PEMBAYARAN PORTAL CUSTOMER ═══ -->
            <div class="st-card">
              <div class="st-card-hdr">
                <div class="st-card-icon"><svg width="16" height="16" fill="none" stroke="#fff" stroke-width="2" viewBox="0 0 24 24"><rect x="2" y="5" width="20" height="14" rx="2"/><line x1="2" y1="10" x2="22" y2="10"/></svg></div>
                <div style="flex:1">
                  <div style="font-size:14px;font-weight:700;color:var(--d-text);">Metode Pembayaran Portal Customer</div>
                  <div style="font-size:12px;color:var(--d-muted);">Rekening bank, e-wallet, dan QRIS yang tampil di portal customer</div>
                </div>
                <button onclick="openPayAcctModal()" style="display:flex;align-items:center;gap:6px;padding:8px 14px;border-radius:10px;background:linear-gradient(135deg,#1a6ef5,#0047cc);color:#fff;border:none;font-size:12px;font-weight:700;cursor:pointer;font-family:inherit">
                  <svg width="13" height="13" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg>
                  Tambah
                </button>
              </div>
              <div class="st-card-body" style="padding:16px 20px">
                <div style="background:#eff6ff;border:1px solid #bfdbfe;border-radius:10px;padding:11px 14px;margin-bottom:14px;display:flex;gap:10px;align-items:flex-start;font-size:12px;color:#1e3a8a;line-height:1.55;">
                  <svg width="16" height="16" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24" style="flex-shrink:0;margin-top:1px"><circle cx="12" cy="12" r="10"/><line x1="12" y1="16" x2="12" y2="12"/><line x1="12" y1="8" x2="12.01" y2="8"/></svg>
                  <div>Daftar rekening ini akan ditampilkan di modal "Bayar" pada portal customer. Pastikan nomor rekening sudah benar sebelum mengaktifkan.</div>
                </div>
                <div id="payAcctList" style="min-height:60px">
                  <div style="padding:24px;text-align:center;color:var(--d-muted);font-size:13px">
                    <div style="width:20px;height:20px;border:2px solid #e0e7f3;border-top-color:#1a6ef5;border-radius:50%;animation:spin .6s linear infinite;margin:0 auto 10px"></div>
                    Memuat...
                  </div>
                </div>
              </div>
            </div>
          </div>

          <!-- MODAL: Tambah/Edit Metode Pembayaran -->
          <div id="payAcctModal" style="display:none;position:fixed;inset:0;background:rgba(0,0,0,.5);z-index:1000;align-items:center;justify-content:center;padding:20px;">
            <div style="background:#fff;border-radius:16px;width:100%;max-width:460px;max-height:90vh;overflow-y:auto;">
              <div style="padding:18px 22px;border-bottom:1px solid var(--d-border);display:flex;align-items:center;justify-content:space-between">
                <div style="font-size:15px;font-weight:700;color:var(--d-text)" id="payAcctModalTitle">Tambah Metode Pembayaran</div>
                <button onclick="closePayAcctModal()" style="background:none;border:none;cursor:pointer;color:var(--d-muted);padding:4px">
                  <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>
                </button>
              </div>
              <div style="padding:20px 22px">
                <input type="hidden" id="paId">
                <div class="pf-field">
                  <label class="pf-label">Tipe Metode</label>
                  <select class="pf-select" id="paType" onchange="onPaTypeChange()">
                    <option value="bank">Transfer Bank</option>
                    <option value="ewallet">E-Wallet</option>
                    <option value="qris">QRIS</option>
                  </select>
                </div>
                <div class="pf-field" id="paProviderWrap">
                  <label class="pf-label" id="paProviderLabel">Bank</label>
                  <select class="pf-select" id="paProvider"></select>
                </div>
                <div class="pf-field" id="paNumberWrap">
                  <label class="pf-label" id="paNumberLabel">Nomor Rekening</label>
                  <input type="text" class="pf-input" id="paNumber" placeholder="1234567890">
                </div>
                <div class="pf-field">
                  <label class="pf-label">Atas Nama / Owner</label>
                  <input type="text" class="pf-input" id="paOwner" placeholder="PT. ISP Jaya">
                </div>
                <div class="pf-field" id="paLogoWrap">
                  <label class="pf-label" id="paLogoLabel">Logo / QR Image (Opsional)</label>

                  <!-- Upload area -->
                  <div id="paLogoDropArea" style="border:1.5px dashed var(--d-border);border-radius:10px;padding:14px;text-align:center;background:#f8faff;cursor:pointer;transition:all .15s;position:relative">
                    <input type="file" id="paLogoFile" accept="image/png,image/jpeg,image/svg+xml,image/webp" style="position:absolute;inset:0;opacity:0;cursor:pointer" onchange="handlePayLogoFile(this.files[0])">

                    <!-- Preview thumbnail (hidden by default) -->
                    <div id="paLogoPreview" style="display:none">
                      <img id="paLogoPreviewImg" src="" alt="" style="max-width:100px;max-height:60px;border-radius:6px;background:#fff;padding:4px;border:1px solid var(--d-border)">
                      <div style="font-size:11px;color:var(--d-muted);margin-top:6px" id="paLogoFileName">—</div>
                      <button type="button" onclick="clearPayLogo(event)" style="margin-top:8px;padding:4px 10px;border-radius:6px;background:#fef2f2;color:#dc2626;border:1px solid #fecaca;font-size:11px;font-weight:700;cursor:pointer;font-family:inherit">Hapus</button>
                    </div>

                    <!-- Drop text (default) -->
                    <div id="paLogoDropText">
                      <svg width="26" height="26" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24" style="margin:0 auto 6px;display:block;color:var(--d-muted)"><path d="M21 15v4a2 2 0 01-2 2H5a2 2 0 01-2-2v-4M17 8l-5-5-5 5M12 3v12"/></svg>
                      <div style="font-size:12px;color:var(--d-text);font-weight:600">Klik untuk upload logo</div>
                      <div style="font-size:11px;color:var(--d-muted);margin-top:3px">PNG, JPG, SVG, WebP · Max 2MB</div>
                    </div>
                  </div>

                  <!-- Hidden field to hold the final URL -->
                  <input type="hidden" id="paLogoUrl" value="">
                  <div class="pf-hint" id="paLogoHint" style="margin-top:6px">Opsional. Kalau kosong, akan tampil singkatan nama bank/e-wallet.</div>
                </div>
                <div class="pf-field">
                  <label style="display:flex;align-items:center;gap:8px;cursor:pointer;font-size:13px;color:var(--d-text);font-weight:600">
                    <input type="checkbox" id="paActive" checked style="width:16px;height:16px;cursor:pointer">
                    Aktifkan di portal customer
                  </label>
                </div>
                <div style="display:flex;gap:10px;margin-top:18px">
                  <button onclick="closePayAcctModal()" style="flex:1;padding:11px;border-radius:10px;background:#f1f5f9;color:var(--d-text);border:1px solid var(--d-border);font-weight:700;font-size:13px;cursor:pointer;font-family:inherit">Batal</button>
                  <button onclick="savePayAcct()" class="save-btn" style="flex:1.4;margin:0;justify-content:center">
                    <svg width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7"/></svg>
                    Simpan
                  </button>
                </div>
              </div>
            </div>
          </div>

          <!-- PAYMENT GATEWAY SECTION -->
          <div class="st-section" id="sec-gateway">
            <div class="st-card">
              <div class="st-card-hdr">
                <div class="st-card-icon" style="background:linear-gradient(135deg,#06b6d4,#0891b2)"><svg width="16" height="16" fill="none" stroke="#fff" stroke-width="2.2" viewBox="0 0 24 24"><path d="M13 2L3 14h9l-1 8 10-12h-9l1-8z"/></svg></div>
                <div style="flex:1">
                  <div style="font-size:14px;font-weight:700;color:var(--d-text);">Payment Gateway</div>
                  <div style="font-size:12px;color:var(--d-muted);">Aktivasi pembayaran online otomatis (VA, QRIS, e-wallet, kartu kredit)</div>
                </div>
                <div id="gwStatusBadge" style="padding:4px 10px;border-radius:8px;font-size:11px;font-weight:700;background:#f1f5f9;color:#64748b">—</div>
              </div>
              <div class="st-card-body">

                <!-- Toggle aktif -->
                <div class="pf-field" style="background:#f8fafc;border-radius:12px;padding:14px 16px;margin-bottom:18px;border:1px solid var(--d-border)">
                  <label style="display:flex;align-items:center;gap:12px;cursor:pointer;">
                    <input type="checkbox" id="gwEnabled" style="width:18px;height:18px;cursor:pointer;flex-shrink:0">
                    <div style="flex:1">
                      <div style="font-size:13.5px;font-weight:700;color:var(--d-text)">Aktifkan Payment Gateway</div>
                      <div style="font-size:11.5px;color:var(--d-muted);margin-top:2px;line-height:1.5">Kalau aktif, pelanggan bisa bayar otomatis dari portal. Mode manual transfer tetap tersedia sebagai opsi.</div>
                    </div>
                  </label>
                </div>

                <!-- Provider -->
                <div class="pf-field">
                  <label class="pf-label">Provider</label>
                  <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(140px,1fr));gap:10px">
                    <label class="gw-prov-card" id="gwProvCard-midtrans" onclick="selectGwProvider('midtrans')">
                      <input type="radio" name="gwProvider" value="midtrans" id="gwProvRadioMidtrans" style="display:none">
                      <div style="font-size:13px;font-weight:700;color:var(--d-text)">Midtrans</div>
                      <div style="font-size:10.5px;color:var(--d-muted);margin-top:2px;line-height:1.4">Snap popup<br>VA, QRIS, GoPay, OVO, dll</div>
                    </label>
                    <label class="gw-prov-card" id="gwProvCard-xendit" onclick="selectGwProvider('xendit')">
                      <input type="radio" name="gwProvider" value="xendit" id="gwProvRadioXendit" style="display:none">
                      <div style="font-size:13px;font-weight:700;color:var(--d-text)">Xendit</div>
                      <div style="font-size:10.5px;color:var(--d-muted);margin-top:2px;line-height:1.4">Hosted invoice<br>VA, QRIS, e-wallet, kartu</div>
                    </label>
                    <label class="gw-prov-card" id="gwProvCard-duitku" onclick="selectGwProvider('duitku')">
                      <input type="radio" name="gwProvider" value="duitku" id="gwProvRadioDuitku" style="display:none">
                      <div style="font-size:13px;font-weight:700;color:var(--d-text)">Duitku</div>
                      <div style="font-size:10.5px;color:var(--d-muted);margin-top:2px;line-height:1.4">POP redirect<br>VA, QRIS, e-wallet, retail</div>
                    </label>
                    <label class="gw-prov-card" id="gwProvCard-tripay" onclick="selectGwProvider('tripay')">
                      <input type="radio" name="gwProvider" value="tripay" id="gwProvRadioTripay" style="display:none">
                      <div style="font-size:13px;font-weight:700;color:var(--d-text)">Tripay</div>
                      <div style="font-size:10.5px;color:var(--d-muted);margin-top:2px;line-height:1.4">Hosted checkout<br>VA, QRIS, e-wallet, retail</div>
                    </label>
                  </div>
                </div>

                <!-- Environment -->
                <div class="pf-field">
                  <label class="pf-label">Environment</label>
                  <select class="pf-select" id="gwEnv">
                    <option value="sandbox">Sandbox (testing)</option>
                    <option value="production">Production (live)</option>
                  </select>
                  <div class="pf-hint">Pakai <b>sandbox</b> dulu untuk testing. Setelah yakin lancar, ganti ke <b>production</b>.</div>
                </div>

                <!-- Merchant Code (Duitku & Tripay) -->
                <div class="pf-field" id="gwMerchantCodeWrap" style="display:none">
                  <label class="pf-label">
                    <span id="gwMerchantCodeLabel">Merchant Code</span>
                    <span style="color:#dc2626">*</span>
                  </label>
                  <input type="text" class="pf-input" id="gwMerchantCode" placeholder="DXXXXX (production) atau DSXXXXX (sandbox)" style="font-family:monospace;font-size:12px">
                  <div class="pf-hint" id="gwMerchantCodeHint">Project Code dari Duitku. Lihat di Project Setting → Project Code.</div>
                </div>

                <!-- Server Key -->
                <div class="pf-field">
                  <label class="pf-label">
                    <span id="gwKeyLabel">Server Key</span>
                    <span style="color:#dc2626">*</span>
                  </label>
                  <input type="text" class="pf-input" id="gwServerKey" placeholder="SB-Mid-server-XXXXXXXXX atau xnd_development_..." style="font-family:monospace;font-size:12px">
                  <div class="pf-hint" id="gwKeyHint">Ambil dari dashboard Midtrans → Settings → Access Keys</div>
                </div>

                <!-- Client Key (Midtrans only) -->
                <div class="pf-field" id="gwClientKeyWrap">
                  <label class="pf-label">Client Key</label>
                  <input type="text" class="pf-input" id="gwClientKey" placeholder="SB-Mid-client-XXXXXXXXX" style="font-family:monospace;font-size:12px">
                  <div class="pf-hint">Dipakai di portal customer untuk Snap.js. Ambil dari dashboard Midtrans.</div>
                </div>

                <!-- Private Key (Tripay only) -->
                <div class="pf-field" id="gwPrivateKeyWrap" style="display:none">
                  <label class="pf-label">
                    Private Key
                    <span style="color:#dc2626">*</span>
                  </label>
                  <input type="text" class="pf-input" id="gwPrivateKey" placeholder="Masukkan Private Key dari Tripay" style="font-family:monospace;font-size:12px">
                  <div class="pf-hint">Dipakai untuk signature transaksi & verifikasi callback (HMAC-SHA256). Ambil dari dashboard Tripay → Merchant → Detail Merchant → <b>Private Key</b>.</div>
                </div>

                <!-- Callback Token (Xendit only) -->
                <div class="pf-field" id="gwCallbackTokenWrap" style="display:none">
                  <label class="pf-label">
                    Callback Token
                    <span style="color:#dc2626">*</span>
                  </label>
                  <input type="text" class="pf-input" id="gwCallbackToken" placeholder="Masukkan callback token dari Xendit" style="font-family:monospace;font-size:12px">
                  <div class="pf-hint">Wajib diisi untuk verifikasi webhook Xendit. Ambil dari Xendit Dashboard → Settings → Developers → Webhooks → <b>Verification Token</b>.</div>
                </div>

                <!-- Webhook URL info -->
                <div style="background:#eff6ff;border:1px solid #bfdbfe;border-radius:10px;padding:12px 14px;margin:16px 0;font-size:12px;color:#1e3a8a;line-height:1.55">
                  <div style="font-weight:700;margin-bottom:8px;display:flex;align-items:center;gap:6px">
                    <svg width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.2" viewBox="0 0 24 24"><circle cx="12" cy="12" r="10"/><line x1="12" y1="16" x2="12" y2="12"/><line x1="12" y1="8" x2="12.01" y2="8"/></svg>
                    Setup Webhook URL di Dashboard Provider
                  </div>
                  <div style="margin-bottom:6px">Daftarkan URL berikut di dashboard provider (Notification URL / Callback URL):</div>
                  <div style="display:flex;gap:6px;align-items:center;background:#fff;border:1px solid #bfdbfe;border-radius:8px;padding:8px 10px;font-family:monospace;font-size:11.5px;color:#1e40af">
                    <span id="webhookUrlText" style="flex:1;word-break:break-all">—</span>
                    <button onclick="copyWebhookUrl()" style="padding:4px 10px;border-radius:6px;background:#1e40af;color:#fff;border:none;font-size:10px;font-weight:700;cursor:pointer;font-family:inherit;flex-shrink:0">Salin</button>
                  </div>
                  <div style="margin-top:8px;font-size:11px;color:#1e40af">
                    <b>Midtrans:</b> Settings → Configuration → Payment Notification URL<br>
                    <b>Xendit:</b> Settings → Developers → Webhooks → Invoices Paid Callback URL<br>
                    <b>Duitku:</b> Project Setting → Callback URL (URL Callback)<br>
                    <b>Tripay:</b> Merchant → Detail Merchant → <b>URL Callback</b>
                  </div>
                </div>

                <!-- Action buttons -->
                <div style="display:flex;gap:10px;margin-top:18px">
                  <button class="save-btn" onclick="saveGatewaySettings()" style="margin:0;flex:1.4;justify-content:center">
                    <svg width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7"/></svg>
                    Simpan Pengaturan
                  </button>
                  <button onclick="testGatewayConfig()" style="flex:1;padding:11px;border-radius:10px;background:#fff;color:var(--d-text);border:1px solid var(--d-border);font-weight:700;font-size:13px;cursor:pointer;font-family:inherit;display:flex;align-items:center;justify-content:center;gap:6px">
                    <svg width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.2" viewBox="0 0 24 24"><polyline points="20 6 9 17 4 12"/></svg>
                    Test Konfigurasi
                  </button>
                </div>

              </div>
            </div>

            <!-- Setup guide cards -->
            <div class="st-card" style="margin-top:14px">
              <div class="st-card-hdr">
                
                <div>
                  <div style="font-size:14px;font-weight:700;color:var(--d-text);">Panduan Singkat Setup</div>
                  <div style="font-size:12px;color:var(--d-muted);">Langkah aktivasi payment gateway</div>
                </div>
              </div>
              <div class="st-card-body" style="padding:6px 20px 18px">
                <div id="setupGuideMidtrans" class="setup-guide">
                  <ol style="margin:0;padding-left:22px;font-size:12.5px;color:var(--d-text);line-height:1.85">
                    <li>Daftar akun di <a href="https://dashboard.midtrans.com" target="_blank" style="color:var(--d-blue1);font-weight:700">dashboard.midtrans.com</a></li>
                    <li>Lengkapi data merchant & verifikasi (untuk production)</li>
                    <li>Settings → <b>Access Keys</b> → copy <b>Server Key</b> & <b>Client Key</b></li>
                    <li>Paste ke form di atas → simpan</li>
                    <li>Settings → <b>Configuration</b> → set <b>Payment Notification URL</b> = URL webhook di atas</li>
                    <li>Test dengan invoice di portal customer (mode sandbox dulu)</li>
                  </ol>
                </div>
                <div id="setupGuideXendit" class="setup-guide" style="display:none">
                  <ol style="margin:0;padding-left:22px;font-size:12.5px;color:var(--d-text);line-height:1.85">
                    <li>Daftar akun di <a href="https://dashboard.xendit.co" target="_blank" style="color:var(--d-blue1);font-weight:700">dashboard.xendit.co</a></li>
                    <li>Lengkapi data perusahaan & aktivasi (untuk production)</li>
                    <li>Settings → Developers → <b>API Keys</b> → buat API Key dengan permission <b>Money-in</b></li>
                    <li>Copy <b>Secret API Key</b> ke form di atas (tidak ada Client Key)</li>
                    <li>Settings → Developers → <b>Webhooks</b> → copy <b>Verification Token</b> ke form Callback Token</li>
                    <li>Set <b>Invoices Paid Callback URL</b> = URL webhook di atas</li>
                    <li>Test dengan invoice di portal customer</li>
                  </ol>
                </div>
                <div id="setupGuideDuitku" class="setup-guide" style="display:none">
                  <ol style="margin:0;padding-left:22px;font-size:12.5px;color:var(--d-text);line-height:1.85">
                    <li>Daftar akun di <a href="https://duitku.com/merchant" target="_blank" style="color:var(--d-blue1);font-weight:700">duitku.com/merchant</a> (sandbox: <a href="https://sandbox.duitku.com" target="_blank" style="color:var(--d-blue1)">sandbox.duitku.com</a>)</li>
                    <li>Lengkapi data perusahaan & verifikasi (untuk production)</li>
                    <li>Buat <b>Project</b> baru di dashboard Duitku</li>
                    <li>Project Setting → copy <b>Project Code</b> (mulai "D…", contoh DXXXXX) ke field <b>Merchant Code</b> di atas</li>
                    <li>Project Setting → copy <b>API Key</b> (32 karakter hex) ke field <b>API Key</b> di atas</li>
                    <li>Project Setting → set <b>Callback URL</b> = URL webhook di atas, set <b>Return URL</b> ke domain portal Anda</li>
                    <li>Aktifkan metode pembayaran yang diinginkan (VA bank, QRIS, OVO, DANA, dll) di Project Setting</li>
                    <li>Test dengan invoice di portal customer (mode sandbox dulu)</li>
                  </ol>
                  <div style="margin-top:10px;padding:10px 12px;background:#fef3c7;border:1px solid #fde68a;border-radius:8px;font-size:11.5px;color:#78350f;line-height:1.55">
                    <b>Catatan:</b> Duitku menggunakan model <b>POP (Payment Outline Page)</b> — pelanggan akan diarahkan ke halaman pembayaran Duitku, lalu pilih metode pembayaran di sana. Tidak perlu Client Key (berbeda dengan Midtrans).
                  </div>
                </div>
                <div id="setupGuideTripay" class="setup-guide" style="display:none">
                  <ol style="margin:0;padding-left:22px;font-size:12.5px;color:var(--d-text);line-height:1.85">
                    <li>Daftar akun di <a href="https://tripay.co.id" target="_blank" style="color:var(--d-blue1);font-weight:700">tripay.co.id</a> (sandbox tersedia setelah login: <a href="https://tripay.co.id/simulator" target="_blank" style="color:var(--d-blue1)">tripay.co.id/simulator</a>)</li>
                    <li>Lengkapi data merchant & verifikasi (untuk production)</li>
                    <li>Masuk ke <b>Merchant</b> → pilih merchant aktif → <b>Detail Merchant</b></li>
                    <li>Copy <b>Kode Merchant</b> (mulai "T…", contoh T12345) ke field <b>Merchant Code</b> di atas</li>
                    <li>Copy <b>API Key</b> ke field <b>API Key</b> di atas</li>
                    <li>Copy <b>Private Key</b> ke field <b>Private Key</b> di atas (untuk signature & verifikasi callback)</li>
                    <li>Set <b>URL Callback</b> = URL webhook di atas, set <b>URL Return</b> ke domain portal Anda</li>
                    <li>Aktifkan channel pembayaran yang diinginkan (BRIVA, BNIVA, BCAVA, QRIS, OVO, DANA, ShopeePay, Alfamart, Indomaret, dll) di menu <b>Channel Pembayaran</b></li>
                    <li>Test dengan invoice di portal customer (mode sandbox dulu, pakai simulator Tripay)</li>
                  </ol>
                  <div style="margin-top:10px;padding:10px 12px;background:#fef3c7;border:1px solid #fde68a;border-radius:8px;font-size:11.5px;color:#78350f;line-height:1.55">
                    <b>Catatan:</b> Tripay menggunakan model <b>Hosted Checkout</b> — pelanggan akan diarahkan ke halaman pembayaran Tripay (checkout_url), lalu pilih channel pembayaran di sana. Signature transaksi & callback memakai <b>HMAC-SHA256</b> dengan <b>Private Key</b>, jadi pastikan key ini benar dan tidak tertukar dengan API Key.
                  </div>
                </div>
              </div>
            </div>
          </div>

          <!-- Add CSS for gateway provider cards -->
          <style>
            .gw-prov-card{display:block;border:2px solid var(--d-border);border-radius:11px;padding:12px 14px;cursor:pointer;transition:all .15s;background:#fff;text-align:left;}
            .gw-prov-card:hover{border-color:#93c5fd;background:#f0f9ff;}
            .gw-prov-card.selected{border-color:var(--d-blue1);background:#eff6ff;}
          </style>

          <!-- PUSH NOTIFICATION SECTION -->
          <div class="st-section" id="sec-push">

            <!-- ═══ Reminder Tagihan Otomatis (Web Push) ═══ -->
            <div class="st-card" style="margin-bottom:14px">
              <div class="st-card-hdr">
                <div class="st-card-icon"><svg width="16" height="16" fill="none" stroke="#fff" stroke-width="2" viewBox="0 0 24 24"><path d="M12 8v4l3 3"/><circle cx="12" cy="12" r="9"/></svg></div>
                <div style="flex:1">
                  <div style="font-size:14px;font-weight:700;color:var(--d-text);">Reminder Tagihan Otomatis</div>
                  <div style="font-size:12px;color:var(--d-muted);">Push notif otomatis ke pelanggan sebelum &amp; sesudah jatuh tempo</div>
                </div>
              </div>
              <div class="st-card-body" style="padding:16px 20px">
                <!-- Master toggle -->
                <label style="display:flex;align-items:center;gap:10px;margin-bottom:6px;cursor:pointer;">
                  <input type="checkbox" id="prEnabled" style="width:18px;height:18px;accent-color:var(--d-blue1);cursor:pointer;">
                  <span style="font-size:13.5px;font-weight:700;color:var(--d-text)">Aktifkan reminder tagihan otomatis</span>
                </label>
                <div style="font-size:11.5px;color:var(--d-muted);margin-bottom:16px;line-height:1.5">
                  Reminder hanya terkirim ke pelanggan yang sudah mengaktifkan notifikasi di portal. Placeholder yang bisa dipakai di teks: <code>{nama}</code>, <code>{jumlah}</code>, <code>{tgl}</code>.
                </div>

                <div id="prBody" style="display:flex;flex-direction:column;gap:16px;">
                  <!-- Jam kirim -->
                  <div style="display:grid;grid-template-columns:1fr 1fr;gap:12px;">
                    <div>
                      <label class="pf-label">Jam kirim reminder jatuh tempo</label>
                      <select class="pf-select" id="prSendHour"></select>
                    </div>
                    <div>
                      <label class="pf-label">Jam kirim reminder overdue</label>
                      <select class="pf-select" id="prOverdueHour"></select>
                    </div>
                  </div>

                  <!-- H-3 -->
                  <div style="border:1px solid var(--d-border);border-radius:10px;padding:12px 14px;">
                    <label style="display:flex;align-items:center;gap:8px;margin-bottom:10px;cursor:pointer;">
                      <input type="checkbox" id="prH3" style="width:16px;height:16px;accent-color:var(--d-blue1);cursor:pointer;">
                      <span style="font-size:13px;font-weight:700;color:var(--d-text)">H-3 · 3 hari sebelum jatuh tempo</span>
                    </label>
                    <input type="text" class="pf-input" id="prH3Title" placeholder="Judul notifikasi" style="margin-bottom:8px;">
                    <textarea class="pf-input" id="prH3Body" rows="2" placeholder="Isi notifikasi" style="font-family:inherit;resize:vertical;"></textarea>
                  </div>

                  <!-- H-1 -->
                  <div style="border:1px solid var(--d-border);border-radius:10px;padding:12px 14px;">
                    <label style="display:flex;align-items:center;gap:8px;margin-bottom:10px;cursor:pointer;">
                      <input type="checkbox" id="prH1" style="width:16px;height:16px;accent-color:var(--d-blue1);cursor:pointer;">
                      <span style="font-size:13px;font-weight:700;color:var(--d-text)">H-1 · 1 hari sebelum jatuh tempo</span>
                    </label>
                    <input type="text" class="pf-input" id="prH1Title" placeholder="Judul notifikasi" style="margin-bottom:8px;">
                    <textarea class="pf-input" id="prH1Body" rows="2" placeholder="Isi notifikasi" style="font-family:inherit;resize:vertical;"></textarea>
                  </div>

                  <!-- H+0 -->
                  <div style="border:1px solid var(--d-border);border-radius:10px;padding:12px 14px;">
                    <label style="display:flex;align-items:center;gap:8px;margin-bottom:10px;cursor:pointer;">
                      <input type="checkbox" id="prH0" style="width:16px;height:16px;accent-color:var(--d-blue1);cursor:pointer;">
                      <span style="font-size:13px;font-weight:700;color:var(--d-text)">H+0 · hari jatuh tempo</span>
                    </label>
                    <input type="text" class="pf-input" id="prH0Title" placeholder="Judul notifikasi" style="margin-bottom:8px;">
                    <textarea class="pf-input" id="prH0Body" rows="2" placeholder="Isi notifikasi" style="font-family:inherit;resize:vertical;"></textarea>
                  </div>

                  <!-- Overdue -->
                  <div style="border:1px solid var(--d-border);border-radius:10px;padding:12px 14px;">
                    <label style="display:flex;align-items:center;gap:8px;margin-bottom:10px;cursor:pointer;">
                      <input type="checkbox" id="prOverdue" style="width:16px;height:16px;accent-color:var(--d-blue1);cursor:pointer;">
                      <span style="font-size:13px;font-weight:700;color:var(--d-text)">Overdue · setelah lewat jatuh tempo (tiap hari, sampai bayar/isolir)</span>
                    </label>
                    <input type="text" class="pf-input" id="prOverdueTitle" placeholder="Judul notifikasi" style="margin-bottom:8px;">
                    <textarea class="pf-input" id="prOverdueBody" rows="2" placeholder="Isi notifikasi" style="font-family:inherit;resize:vertical;"></textarea>
                  </div>
                </div>

                <div style="display:flex;gap:12px;align-items:center;margin-top:16px;">
                  <button class="save-btn" onclick="savePushReminderSettings()" id="prSaveBtn">
                    <svg width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7"/></svg>
                    Simpan Pengaturan Reminder
                  </button>
                  <span id="prSaveMsg" style="font-size:12px;color:var(--d-muted)"></span>
                </div>

                <!-- ── Verifikasi Sistem (Test) ─────────────────────────── -->
                <div style="margin-top:18px;border-top:1px dashed var(--d-border);padding-top:16px;">
                  <div style="font-size:13px;font-weight:700;color:var(--d-text);margin-bottom:4px;">Verifikasi Sistem</div>
                  <div style="font-size:12px;color:var(--d-muted);margin-bottom:12px;">
                    Pastikan push notification &amp; alur cron otomatis berjalan tanpa menunggu jam terjadwal.
                  </div>

                  <!-- Test kirim 1 notifikasi -->
                  <div style="background:#f8faff;border:1px solid var(--d-border);border-radius:10px;padding:12px 14px;margin-bottom:10px;">
                    <div style="font-size:12px;font-weight:700;color:var(--d-text);margin-bottom:8px;">1 · Test Kirim Notifikasi</div>
                    <div style="display:flex;gap:8px;flex-wrap:wrap;align-items:center;">
                      <select class="pf-select" id="prTestCustomer" style="flex:1;min-width:200px;">
                        <option value="">Subscriber pertama (otomatis)</option>
                      </select>
                      <button class="save-btn" id="prTestSendBtn" onclick="testPushSend()">
                        <svg width="14" height="14" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><line x1="22" y1="2" x2="11" y2="13"/><polygon points="22 2 15 22 11 13 2 9 22 2"/></svg>
                        Kirim Tes
                      </button>
                    </div>
                    <div style="font-size:11px;color:var(--d-muted);margin-top:6px;">
                      Mengirim 1 notifikasi uji ke pelanggan terpilih. Tidak tercatat di riwayat broadcast.
                    </div>
                  </div>

                  <!-- Test cron otomatis -->
                  <div style="background:#f8faff;border:1px solid var(--d-border);border-radius:10px;padding:12px 14px;">
                    <div style="font-size:12px;font-weight:700;color:var(--d-text);margin-bottom:8px;">2 · Test Cron Reminder Otomatis</div>
                    <div style="display:flex;gap:8px;flex-wrap:wrap;">
                      <button class="save-btn" id="prTestDueBtn" onclick="testPushCron('due-soon')" style="background:#0ea5e9;">
                        <svg width="14" height="14" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>
                        Jalankan Reminder Jatuh Tempo
                      </button>
                      <button class="save-btn" id="prTestOverdueBtn" onclick="testPushCron('overdue')" style="background:#f59e0b;">
                        <svg width="14" height="14" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path d="M10.29 3.86L1.82 18a2 2 0 001.71 3h16.94a2 2 0 001.71-3L13.71 3.86a2 2 0 00-3.42 0z"/><line x1="12" y1="9" x2="12" y2="13"/><line x1="12" y1="17" x2="12.01" y2="17"/></svg>
                        Jalankan Reminder Overdue
                      </button>
                    </div>
                    <div style="font-size:11px;color:var(--d-muted);margin-top:6px;">
                      Menjalankan logika cron yang sama dengan jadwal harian, mengabaikan jam kirim. Notifikasi
                      hanya terkirim ke pelanggan yang invoice-nya cocok kriteria H-3/H-1/H+0 atau overdue.
                    </div>
                    <span id="prTestMsg" style="display:block;font-size:12px;margin-top:8px;"></span>
                  </div>
                </div>
              </div>
            </div>

            <!-- ═══ Notifikasi Isolir & Restore (Web Push) ═══ -->
            <div class="st-card" style="margin-bottom:14px">
              <div class="st-card-hdr">
                <div class="st-card-icon"><svg width="16" height="16" fill="none" stroke="#fff" stroke-width="2" viewBox="0 0 24 24"><rect x="3" y="11" width="18" height="11" rx="2"/><path d="M7 11V7a5 5 0 0110 0v4"/></svg></div>
                <div style="flex:1">
                  <div style="font-size:14px;font-weight:700;color:var(--d-text);">Notifikasi Isolir &amp; Restore</div>
                  <div style="font-size:12px;color:var(--d-muted);">Push notif ke pelanggan saat layanan diisolir &amp; saat diaktifkan kembali</div>
                </div>
              </div>
              <div class="st-card-body" style="padding:16px 20px">
                <label style="display:flex;align-items:center;gap:10px;margin-bottom:6px;cursor:pointer;">
                  <input type="checkbox" id="piEnabled" style="width:18px;height:18px;accent-color:var(--d-blue1);cursor:pointer;">
                  <span style="font-size:13.5px;font-weight:700;color:var(--d-text)">Aktifkan notifikasi isolir &amp; restore</span>
                </label>
                <div style="font-size:11.5px;color:var(--d-muted);margin-bottom:16px;line-height:1.5">
                  Terkirim otomatis baik dari isolir manual (mis. halaman Hotspot Binding) maupun isolir/restore otomatis sistem. Hanya ke pelanggan yang mengaktifkan notifikasi di portal. Placeholder: <code>{nama}</code>, <code>{cid}</code>.
                </div>

                <div id="piBody" style="display:flex;flex-direction:column;gap:16px;">
                  <div style="border:1px solid var(--d-border);border-radius:10px;padding:12px 14px;">
                    <div style="display:flex;align-items:center;gap:8px;margin-bottom:10px;">
                      <span style="width:9px;height:9px;border-radius:50%;background:#ef4444;"></span>
                      <span style="font-size:13px;font-weight:700;color:var(--d-text)">Saat Terisolir</span>
                    </div>
                    <input type="text" class="pf-input" id="piIsolirTitle" placeholder="Judul notifikasi" style="margin-bottom:8px;">
                    <textarea class="pf-input" id="piIsolirBody" rows="2" placeholder="Isi notifikasi" style="font-family:inherit;resize:vertical;"></textarea>
                  </div>
                  <div style="border:1px solid var(--d-border);border-radius:10px;padding:12px 14px;">
                    <div style="display:flex;align-items:center;gap:8px;margin-bottom:10px;">
                      <span style="width:9px;height:9px;border-radius:50%;background:#22c55e;"></span>
                      <span style="font-size:13px;font-weight:700;color:var(--d-text)">Saat Aktif Kembali (Restore)</span>
                    </div>
                    <input type="text" class="pf-input" id="piRestoreTitle" placeholder="Judul notifikasi" style="margin-bottom:8px;">
                    <textarea class="pf-input" id="piRestoreBody" rows="2" placeholder="Isi notifikasi" style="font-family:inherit;resize:vertical;"></textarea>
                  </div>
                </div>

                <div style="display:flex;gap:12px;align-items:center;margin-top:16px;">
                  <button class="save-btn" onclick="savePushIsolirSettings()" id="piSaveBtn">
                    <svg width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7"/></svg>
                    Simpan Pengaturan
                  </button>
                  <span id="piSaveMsg" style="font-size:12px;color:var(--d-muted)"></span>
                </div>
              </div>
            </div>

            <div class="st-card" style="margin-bottom:14px">
              <div class="st-card-hdr">
                <div class="st-card-icon"><svg width="16" height="16" fill="none" stroke="#fff" stroke-width="2" viewBox="0 0 24 24"><path d="M18 8A6 6 0 006 8c0 7-3 9-3 9h18s-3-2-3-9"/><path d="M13.73 21a2 2 0 01-3.46 0"/></svg></div>
                <div style="flex:1">
                  <div style="font-size:14px;font-weight:700;color:var(--d-text);">Push Notification</div>
                  <div style="font-size:12px;color:var(--d-muted);">Kirim notifikasi ke pelanggan yang subscribe di portal</div>
                </div>
                <div id="pushReadyBadge" style="display:none"></div>
              </div>

              <!-- Stats cards -->
              <div class="st-card-body" style="padding:16px 20px">
                <div style="display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin-bottom:16px" id="pushStatsWrap">
                  <div class="push-stat"><div class="push-stat-lbl">Subscribers</div><div class="push-stat-val" id="pushStatSubs">—</div></div>
                  <div class="push-stat"><div class="push-stat-lbl">Hari ini</div><div class="push-stat-val" id="pushStatToday">—</div></div>
                  <div class="push-stat"><div class="push-stat-lbl">30 hari</div><div class="push-stat-val" id="pushStatMonth">—</div></div>
                  <div class="push-stat"><div class="push-stat-lbl">Scheduled</div><div class="push-stat-val" id="pushStatSched">—</div></div>
                </div>

                <!-- Tabs -->
                <div style="display:flex;gap:4px;border-bottom:1.5px solid var(--d-border);margin-bottom:16px;flex-wrap:wrap">
                  <div class="push-tab active" onclick="switchPushTab('send',this)">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="22" y1="2" x2="11" y2="13"/><polygon points="22 2 15 22 11 13 2 9 22 2"/></svg>
                    Kirim Notifikasi
                  </div>
                  <div class="push-tab" onclick="switchPushTab('scheduled',this)">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4" width="18" height="18" rx="2" ry="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/><circle cx="12" cy="15" r="2"/></svg>
                    Schedule
                    <span id="pushSchedBadge" style="display:none"></span>
                  </div>
                  <div class="push-tab" onclick="switchPushTab('template',this)">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M14 2H6a2 2 0 00-2 2v16a2 2 0 002 2h12a2 2 0 002-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="9" y1="13" x2="15" y2="13"/><line x1="9" y1="17" x2="15" y2="17"/></svg>
                    Template
                  </div>
                  <div class="push-tab" onclick="switchPushTab('history',this)">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>
                    Riwayat
                  </div>
                </div>

                <!-- ═══ TAB: KIRIM ═══ -->
                <div class="push-tab-content active" id="push-tab-send">
                  <!-- Compose form -->
                  <div style="display:grid;grid-template-columns:1fr 1fr;gap:16px">

                    <!-- LEFT: Filter target -->
                    <div>
                      <div class="push-section-hdr">
                        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><circle cx="12" cy="12" r="6"/><circle cx="12" cy="12" r="2"/></svg>
                        Target Pelanggan
                      </div>

                      <div class="pf-field">
                        <label class="pf-label">Paket Layanan (opsional — kosongkan = semua)</label>
                        <div id="pushFilterPackages" style="max-height:130px;overflow-y:auto;border:1px solid var(--d-border);border-radius:10px;padding:8px;background:#fff">
                          <div style="color:var(--d-muted);font-size:12px;padding:4px 2px">Memuat paket...</div>
                        </div>
                      </div>

                      <div class="pf-field">
                        <label class="pf-label">Status Tagihan</label>
                        <div style="display:flex;flex-wrap:wrap;gap:8px">
                          <label class="push-chip"><input type="checkbox" class="push-fbill" value="unpaid"> Belum Bayar</label>
                          <label class="push-chip"><input type="checkbox" class="push-fbill" value="overdue"> Jatuh Tempo</label>
                          <label class="push-chip"><input type="checkbox" class="push-fbill" value="due_soon"> H-3</label>
                          <label class="push-chip"><input type="checkbox" class="push-fbill" value="paid"> Lunas</label>
                        </div>
                      </div>

                      <div class="pf-field">
                        <label class="pf-label">Status Pelanggan</label>
                        <div style="display:flex;flex-wrap:wrap;gap:8px">
                          <label class="push-chip"><input type="checkbox" class="push-fcust" value="active"> Aktif</label>
                          <label class="push-chip"><input type="checkbox" class="push-fcust" value="inactive"> Inaktif</label>
                          <label class="push-chip"><input type="checkbox" class="push-fisol" value="isolir"> Isolir</label>
                        </div>
                      </div>

                      <div class="pf-field">
                        <label class="pf-label">Area / Alamat (partial match)</label>
                        <input type="text" class="pf-input" id="pushFilterArea" placeholder="mis. Kampung Baru, Jl. Merdeka">
                      </div>

                      <div class="pf-field">
                        <label class="pf-label">Pilih Manual (override semua filter di atas)</label>

                        <!-- Chip list pelanggan yang sudah dipilih -->
                        <div id="pushPickedChips" style="display:none;padding:8px;background:#eff6ff;border:1px solid #bfdbfe;border-radius:10px;margin-bottom:8px;flex-wrap:wrap;gap:6px"></div>

                        <!-- Search input -->
                        <div style="display:flex;gap:8px;margin-bottom:6px;position:relative">
                          <input type="text" class="pf-input" id="pushCustSearch" placeholder="Ketik nama / ID / telepon pelanggan..." style="flex:1" oninput="debouncedCustSearch()" onfocus="this.select()">
                          <button class="save-btn" style="flex-shrink:0;margin:0;padding:10px 14px;background:#64748b" onclick="clearManualPick()" type="button">Reset</button>
                        </div>

                        <!-- Search results dropdown -->
                        <div id="pushCustList" style="max-height:220px;overflow-y:auto;border:1px solid var(--d-border);border-radius:10px;padding:6px;background:#fff;font-size:12px;display:none"></div>
                        <div class="pf-hint" id="pushManualCount" style="margin-top:4px"></div>
                      </div>

                      <!-- Preview target -->
                      <div style="background:linear-gradient(135deg,#eff6ff,#dbeafe);border:1px solid #bfdbfe;border-radius:12px;padding:12px 14px;margin-top:6px">
                        <div style="font-size:11px;color:var(--d-muted);font-weight:700;text-transform:uppercase;letter-spacing:.5px;margin-bottom:4px">Preview Target</div>
                        <div style="font-size:13px;color:var(--d-text)" id="pushPreviewText">Klik <b>Hitung Target</b> untuk melihat jumlah pelanggan.</div>
                        <button class="save-btn" style="margin-top:10px;padding:8px 14px;font-size:12px" onclick="calcPushTargets()">
                          <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"><rect x="4" y="2" width="16" height="20" rx="2"/><line x1="8" y1="6" x2="16" y2="6"/><line x1="16" y1="14" x2="16" y2="18"/><line x1="12" y1="14" x2="12" y2="18"/><line x1="8" y1="14" x2="8" y2="18"/><line x1="8" y1="10" x2="16" y2="10"/></svg>
                          Hitung Target
                        </button>
                      </div>
                    </div>

                    <!-- RIGHT: Compose pesan -->
                    <div>
                      <div class="push-section-hdr" style="display:flex;align-items:center;gap:8px">
                        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 20h9"/><path d="M16.5 3.5a2.121 2.121 0 013 3L7 19l-4 1 1-4L16.5 3.5z"/></svg>
                        Isi Notifikasi
                        <button class="push-mini-btn" style="margin-left:auto" onclick="openTemplatePicker()">
                          <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" style="vertical-align:-2px;margin-right:3px"><rect x="9" y="9" width="13" height="13" rx="2"/><path d="M5 15H4a2 2 0 01-2-2V4a2 2 0 012-2h9a2 2 0 012 2v1"/></svg>
                          Pakai Template
                        </button>
                      </div>

                      <div class="pf-field">
                        <label class="pf-label">Icon Emoji (opsional)</label>
                        <div style="display:flex;flex-wrap:wrap;gap:6px;margin-bottom:6px">
                          <button type="button" class="emoji-chip" onclick="pickEmoji('🎉')">🎉</button>
                          <button type="button" class="emoji-chip" onclick="pickEmoji('📢')">📢</button>
                          <button type="button" class="emoji-chip" onclick="pickEmoji('💳')">💳</button>
                          <button type="button" class="emoji-chip" onclick="pickEmoji('⚠️')">⚠️</button>
                          <button type="button" class="emoji-chip" onclick="pickEmoji('🎁')">🎁</button>
                          <button type="button" class="emoji-chip" onclick="pickEmoji('ℹ️')">ℹ️</button>
                          <button type="button" class="emoji-chip" onclick="pickEmoji('🔧')">🔧</button>
                          <button type="button" class="emoji-chip" onclick="pickEmoji('🙏')">🙏</button>
                          <button type="button" class="emoji-chip" onclick="pickEmoji('')">✕</button>
                        </div>
                        <input type="text" class="pf-input" id="pushIcon" maxlength="4" placeholder="Emoji dipilih di atas..." style="font-size:18px;text-align:center" oninput="updatePushPreview()">
                      </div>

                      <div class="pf-field">
                        <label class="pf-label">Judul (max 60 karakter) *</label>
                        <input type="text" class="pf-input" id="pushTitle" maxlength="60" placeholder="Promo Spesial!" oninput="updatePushPreview()">
                        <div class="pf-hint"><span id="pushTitleCount">0</span>/60</div>
                      </div>

                      <div class="pf-field">
                        <label class="pf-label">Isi Pesan (max 180 karakter) *</label>
                        <textarea class="pf-input" id="pushBody" maxlength="180" rows="3" placeholder="Detail notifikasi di sini..." oninput="updatePushPreview()" style="resize:vertical;font-family:inherit"></textarea>
                        <div class="pf-hint"><span id="pushBodyCount">0</span>/180</div>
                      </div>

                      <div class="pf-field">
                        <label class="pf-label">Link Tujuan (opsional)</label>
                        <input type="text" class="pf-input" id="pushUrl" placeholder="/portal" value="/portal">
                        <div class="pf-hint">URL yang dibuka saat user klik notifikasi. Default: /portal</div>
                      </div>

                      <div class="pf-field">
                        <label style="display:flex;align-items:center;gap:8px;cursor:pointer;font-size:13px;color:var(--d-text);font-weight:600">
                          <input type="checkbox" id="pushScheduleToggle" onchange="toggleSchedule()" style="width:16px;height:16px;cursor:pointer">
                          <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4" width="18" height="18" rx="2" ry="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg>
                          Jadwalkan kirim nanti
                        </label>
                      </div>

                      <div class="pf-field" id="pushScheduleWrap" style="display:none">
                        <label class="pf-label">Kirim Pada</label>
                        <input type="datetime-local" class="pf-input" id="pushScheduledAt">
                        <div class="pf-hint">Minimal <b>2 menit</b> dari sekarang. Scheduler akan cek setiap 1 menit.</div>
                      </div>

                      <!-- Notification Preview -->
                      <div style="font-size:11px;color:var(--d-muted);font-weight:700;text-transform:uppercase;letter-spacing:.5px;margin:14px 0 6px">Preview</div>
                      <div class="push-preview" id="pushPreview">
                        <div class="pp-icon">🔔</div>
                        <div class="pp-body">
                          <div class="pp-app">ISP NetOps · sekarang</div>
                          <div class="pp-title" id="ppTitle">(judul notifikasi)</div>
                          <div class="pp-text" id="ppBody">(isi pesan)</div>
                        </div>
                      </div>

                      <!-- Action buttons -->
                      <div style="display:flex;gap:10px;margin-top:16px">
                        <button class="save-btn" style="flex:1;justify-content:center;background:linear-gradient(135deg,#10b981,#059669)" onclick="sendPushNotif()" id="btnSendPush">
                          <svg width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24"><line x1="22" y1="2" x2="11" y2="13"/><polygon points="22 2 15 22 11 13 2 9 22 2"/></svg>
                          <span id="btnSendPushLabel">Kirim Sekarang</span>
                        </button>
                        <button class="save-btn" style="flex:0;padding:11px 18px;background:#64748b" onclick="resetPushForm()">Reset</button>
                      </div>
                    </div>
                  </div>
                </div>

                <!-- ═══ TAB: SCHEDULED ═══ -->
                <div class="push-tab-content" id="push-tab-scheduled">
                  <div style="background:linear-gradient(135deg,#eff6ff,#dbeafe);border:1px solid #bfdbfe;border-radius:12px;padding:14px 16px;margin-bottom:14px;display:flex;align-items:flex-start;gap:12px">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="#1e40af" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="flex-shrink:0;margin-top:1px"><rect x="3" y="4" width="18" height="18" rx="2" ry="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg>
                    <div>
                      <div style="font-size:13px;font-weight:700;color:#1e3a8a">Notifikasi Terjadwal</div>
                      <div style="font-size:11.5px;color:#1e3a8a;margin-top:2px;line-height:1.5">Daftar notifikasi yang menunggu waktu kirim. Scheduler berjalan setiap 1 menit memeriksa jadwal.</div>
                    </div>
                    <button class="push-mini-btn" style="margin-left:auto;flex-shrink:0" onclick="loadPushScheduled()">
                      <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" style="vertical-align:-2px;margin-right:3px"><polyline points="23 4 23 10 17 10"/><polyline points="1 20 1 14 7 14"/><path d="M3.51 9a9 9 0 0114.85-3.36L23 10M1 14l4.64 4.36A9 9 0 0020.49 15"/></svg>
                      Refresh
                    </button>
                  </div>
                  <div id="pushScheduledList" style="min-height:120px"></div>
                </div>

                <!-- ═══ TAB: TEMPLATE ═══ -->
                <div class="push-tab-content" id="push-tab-template">
                  <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:14px">
                    <div style="font-size:13px;color:var(--d-muted)">Simpan pesan yang sering dipakai agar bisa dipanggil ulang dengan cepat.</div>
                    <button class="save-btn" style="padding:9px 15px;margin:0" onclick="openTemplateModal()">
                      <svg width="13" height="13" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg>
                      Tambah Template
                    </button>
                  </div>
                  <div id="pushTemplateList" style="min-height:100px"></div>
                </div>

                <!-- ═══ TAB: RIWAYAT ═══ -->
                <div class="push-tab-content" id="push-tab-history">
                  <div style="display:flex;gap:8px;margin-bottom:12px">
                    <select class="pf-select" id="pushHistFilter" onchange="loadPushHistory()" style="max-width:180px">
                      <option value="">Semua status</option>
                      <option value="sent">Terkirim</option>
                      <option value="failed">Gagal</option>
                      <option value="cancelled">Dibatalkan</option>
                    </select>
                    <input type="text" class="pf-input" id="pushHistSearch" placeholder="Cari judul/isi..." oninput="debouncedHistSearch()" style="flex:1">
                  </div>
                  <div id="pushHistoryList" style="min-height:100px"></div>
                </div>

              </div>
            </div>
          </div>

          <!-- MODAL: Template CRUD -->
          <div id="pushTplModal" style="display:none;position:fixed;inset:0;background:rgba(0,0,0,.5);z-index:1000;align-items:center;justify-content:center;padding:20px;">
            <div style="background:#fff;border-radius:16px;width:100%;max-width:520px;max-height:90vh;overflow-y:auto;">
              <div style="padding:18px 22px;border-bottom:1px solid var(--d-border);display:flex;align-items:center;justify-content:space-between">
                <div style="font-size:15px;font-weight:700;color:var(--d-text)" id="pushTplModalTitle">Tambah Template</div>
                <button onclick="closeTemplateModal()" style="background:none;border:none;cursor:pointer;color:var(--d-muted);padding:4px">
                  <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>
                </button>
              </div>
              <div style="padding:20px 22px">
                <input type="hidden" id="tplId">
                <div class="pf-field">
                  <label class="pf-label">Nama Template *</label>
                  <input type="text" class="pf-input" id="tplName" maxlength="100" placeholder="Promo Diskon Bulanan">
                </div>
                <div class="pf-field">
                  <label class="pf-label">Kategori</label>
                  <select class="pf-select" id="tplCategory">
                    <option value="promo">🎉 Promo</option>
                    <option value="info">ℹ️ Info</option>
                    <option value="maintenance">🔧 Maintenance</option>
                    <option value="warning">⚠️ Peringatan</option>
                    <option value="greeting">🙏 Ucapan</option>
                    <option value="other">Lainnya</option>
                  </select>
                </div>
                <div class="pf-field">
                  <label class="pf-label">Icon Emoji</label>
                  <input type="text" class="pf-input" id="tplIcon" maxlength="4" style="font-size:18px;text-align:center" placeholder="🎉">
                </div>
                <div class="pf-field">
                  <label class="pf-label">Judul *</label>
                  <input type="text" class="pf-input" id="tplTitle" maxlength="120">
                </div>
                <div class="pf-field">
                  <label class="pf-label">Isi Pesan *</label>
                  <textarea class="pf-input" id="tplBody" maxlength="500" rows="3" style="resize:vertical;font-family:inherit"></textarea>
                </div>
                <div class="pf-field">
                  <label class="pf-label">Link (opsional)</label>
                  <input type="text" class="pf-input" id="tplUrl" placeholder="/portal">
                </div>
                <div style="display:flex;gap:10px;margin-top:18px">
                  <button onclick="closeTemplateModal()" style="flex:1;padding:11px;border-radius:10px;background:#f1f5f9;color:var(--d-text);border:1px solid var(--d-border);font-weight:700;font-size:13px;cursor:pointer;font-family:inherit">Batal</button>
                  <button onclick="saveTemplate()" class="save-btn" style="flex:1.4;margin:0;justify-content:center">Simpan</button>
                </div>
              </div>
            </div>
          </div>

          <!-- ANNOUNCEMENTS SECTION -->
          <div class="st-section" id="sec-announcements">
            <div class="st-card">
              <div class="st-card-hdr">
                <div class="st-card-icon"><svg width="16" height="16" fill="none" stroke="#fff" stroke-width="2" viewBox="0 0 24 24"><path d="M22 17H2a3 3 0 000 6h20v-6z"/><path d="M6 17V7a2 2 0 012-2h8a2 2 0 012 2v10"/></svg></div>
                <div>
                  <div style="font-size:14px;font-weight:700;color:var(--d-text);">Pengumuman Portal Pelanggan</div>
                  <div style="font-size:12px;color:var(--d-muted);">Gangguan massal, maintenance, info, promo</div>
                </div>
                <button onclick="openAnnModal()" style="margin-left:auto;display:flex;align-items:center;gap:6px;padding:8px 14px;border-radius:10px;background:linear-gradient(135deg,#1a6ef5,#0047cc);color:#fff;border:none;font-size:12px;font-weight:700;cursor:pointer;font-family:inherit">
                  <svg width="13" height="13" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg>
                  Tambah
                </button>
              </div>
              <div class="st-card-body" style="padding:0">
                <div id="annList" style="min-height:80px">
                  <div style="padding:32px;text-align:center;color:var(--d-muted);font-size:13px">
                    <div style="width:20px;height:20px;border:2px solid #e0e7f3;border-top-color:#1a6ef5;border-radius:50%;animation:spin .6s linear infinite;margin:0 auto 10px"></div>
                    Memuat...
                  </div>
                </div>
              </div>
            </div>
          </div>

          <!-- TELEGRAM MONITORING SECTION -->
          <div class="st-section" id="sec-telegram">
            <div class="st-card">
              <div class="st-card-hdr">
                <div class="st-card-icon"><svg width="16" height="16" fill="none" stroke="#fff" stroke-width="2" viewBox="0 0 24 24"><path d="M21.5 4.5L2.5 12.5l5.5 2 2 6 3-4 5 4z"/></svg></div>
                <div>
                  <div style="font-weight:700;color:var(--d-text);">Telegram Monitoring</div>
                  <div style="font-size:12px;color:var(--d-muted);">Kirim notifikasi otomatis ke Telegram saat ada gangguan jaringan</div>
                </div>
              </div>
              <div class="st-card-body">
                <div style="display:flex;align-items:center;gap:10px;margin-bottom:18px;padding:12px 14px;background:#f8faff;border:1px solid var(--d-border);border-radius:12px;">
                  <label style="display:flex;align-items:center;gap:10px;cursor:pointer;font-weight:700;color:var(--d-text);">
                    <input type="checkbox" id="tg-enabled" style="width:18px;height:18px;cursor:pointer;">
                    Aktifkan Telegram Monitoring
                  </label>
                </div>

                <div class="pf-group" style="margin-bottom:14px;">
                  <label class="pf-label">Bot Token</label>
                  <input type="text" class="pf-input" id="tg-bot-token" placeholder="123456789:ABCdef... (dari @BotFather)">
                  <div class="pf-hint">Buat bot di <b>@BotFather</b> → /newbot → salin token-nya ke sini.</div>
                </div>

                <div class="pf-group" style="margin-bottom:14px;">
                  <label class="pf-label">Chat ID</label>
                  <input type="text" class="pf-input" id="tg-chat-id" placeholder="123456789 (atau -100xxx untuk grup, bisa multi pisah koma)">
                  <div class="pf-hint">Chat ke bot Anda, lalu buka <code>https://api.telegram.org/bot&lt;TOKEN&gt;/getUpdates</code> → ambil <code>chat.id</code>. Untuk grup: tambahkan bot ke grup &amp; kirim pesan dulu.</div>
                </div>

                <div style="font-size:12px;font-weight:700;color:var(--d-text);text-transform:uppercase;letter-spacing:.5px;margin:18px 0 10px;">Jenis Notifikasi</div>
                <div style="display:flex;flex-direction:column;gap:10px;">
                  <label style="display:flex;align-items:center;gap:10px;cursor:pointer;padding:11px 14px;border:1px solid var(--d-border);border-radius:10px;">
                    <input type="checkbox" id="tg-notif-device" style="width:17px;height:17px;cursor:pointer;">
                    <span><b>Perangkat Down</b> — router/OLT/switch tidak terjangkau</span>
                  </label>
                  <label style="display:flex;align-items:center;gap:10px;cursor:pointer;padding:11px 14px;border:1px solid var(--d-border);border-radius:10px;">
                    <input type="checkbox" id="tg-notif-ont" style="width:17px;height:17px;cursor:pointer;">
                    <span><b>ONT Pelanggan Disconnect</b> — ONT pelanggan offline</span>
                  </label>
                  <label style="display:flex;align-items:center;gap:10px;cursor:pointer;padding:11px 14px;border:1px solid var(--d-border);border-radius:10px;">
                    <input type="checkbox" id="tg-notif-ip" style="width:17px;height:17px;cursor:pointer;">
                    <span><b>IP Pelanggan Timeout</b> — IP statis pelanggan tidak reachable</span>
                  </label>
                  <label style="display:flex;align-items:center;gap:10px;cursor:pointer;padding:11px 14px;border:1px solid var(--d-border);border-radius:10px;">
                    <input type="checkbox" id="tg-notif-pppoe" style="width:17px;height:17px;cursor:pointer;">
                    <span><b>PPPoE Pelanggan Disconnect</b> — sesi PPPoE pelanggan tidak aktif</span>
                  </label>
                </div>

                <div style="font-size:12px;font-weight:700;color:var(--d-text);text-transform:uppercase;letter-spacing:.5px;margin:22px 0 6px;">Interval Pengecekan</div>
                <div style="font-size:11.5px;color:var(--d-muted);margin-bottom:10px;line-height:1.55;">
                  Seberapa sering sistem memeriksa status perangkat & pelanggan. Semakin singkat, semakin cepat notifikasi terkirim, tapi beban ke router sedikit lebih besar.
                </div>
                <div class="pf-group" style="max-width:320px;">
                  <select class="pf-select" id="tg-interval">
                    <option value="1">Setiap 1 menit (paling cepat)</option>
                    <option value="2">Setiap 2 menit</option>
                    <option value="3">Setiap 3 menit (disarankan)</option>
                    <option value="5">Setiap 5 menit</option>
                    <option value="10">Setiap 10 menit (hemat)</option>
                  </select>
                  <div class="pf-hint">Perubahan interval diterapkan otomatis setelah klik <b>Simpan</b> (tanpa restart).</div>
                </div>

                <!-- Editor Template Pesan -->
                <div class="tg-tpl-head">
                  <div class="tg-tpl-head-ic">
                    <svg width="16" height="16" fill="none" stroke="#1a6ef5" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" viewBox="0 0 24 24"><path d="M12 20h9"/><path d="M16.5 3.5a2.12 2.12 0 0 1 3 3L7 19l-4 1 1-4Z"/></svg>
                  </div>
                  <div>
                    <div class="tg-tpl-head-t">Format Pesan (Template)</div>
                    <div class="tg-tpl-head-s">Sesuaikan isi tiap notifikasi. Kosongkan untuk pakai format bawaan.</div>
                  </div>
                </div>

                <div style="font-size:11.5px;color:var(--d-muted);line-height:1.55;margin-top:8px;">
                  Klik placeholder untuk menyisipkan ke kolom yang sedang aktif. Mendukung tag HTML Telegram: <code>&lt;b&gt;</code> <code>&lt;code&gt;</code> <code>&lt;i&gt;</code>.
                </div>
                <div class="tg-ph-row">
                  <span class="tg-ph" onclick="tgInsertPlaceholder('{nama}')">{nama}</span>
                  <span class="tg-ph" onclick="tgInsertPlaceholder('{tipe}')">{tipe}</span>
                  <span class="tg-ph" onclick="tgInsertPlaceholder('{ip}')">{ip}</span>
                  <span class="tg-ph" onclick="tgInsertPlaceholder('{cid}')">{cid}</span>
                  <span class="tg-ph" onclick="tgInsertPlaceholder('{user}')">{user}</span>
                  <span class="tg-ph" onclick="tgInsertPlaceholder('{waktu}')">{waktu}</span>
                </div>

                <div class="tg-tpl-grid">
                  <div class="tg-tpl-card" style="--accent:#ef4444;--accent-soft:rgba(239,68,68,.14);">
                    <div class="tg-tpl-card-hdr">
                      <span class="tg-tpl-dot"></span>
                      <span class="tg-tpl-name">Perangkat Down</span>
                      <span class="tg-tpl-vars">{nama} {tipe} {ip} {waktu}</span>
                    </div>
                    <textarea id="tg-tpl-device_down" rows="4"></textarea>
                  </div>
                  <div class="tg-tpl-card" style="--accent:#22c55e;--accent-soft:rgba(34,197,94,.14);">
                    <div class="tg-tpl-card-hdr">
                      <span class="tg-tpl-dot"></span>
                      <span class="tg-tpl-name">Perangkat Pulih</span>
                      <span class="tg-tpl-vars">{nama} {tipe} {ip} {waktu}</span>
                    </div>
                    <textarea id="tg-tpl-device_recover" rows="3"></textarea>
                  </div>
                  <div class="tg-tpl-card" style="--accent:#f97316;--accent-soft:rgba(249,115,22,.14);">
                    <div class="tg-tpl-card-hdr">
                      <span class="tg-tpl-dot"></span>
                      <span class="tg-tpl-name">ONT Disconnect</span>
                      <span class="tg-tpl-vars">{nama} {cid} {waktu}</span>
                    </div>
                    <textarea id="tg-tpl-ont_down" rows="4"></textarea>
                  </div>
                  <div class="tg-tpl-card" style="--accent:#22c55e;--accent-soft:rgba(34,197,94,.14);">
                    <div class="tg-tpl-card-hdr">
                      <span class="tg-tpl-dot"></span>
                      <span class="tg-tpl-name">ONT Pulih</span>
                      <span class="tg-tpl-vars">{nama} {cid} {waktu}</span>
                    </div>
                    <textarea id="tg-tpl-ont_recover" rows="3"></textarea>
                  </div>
                  <div class="tg-tpl-card" style="--accent:#f59e0b;--accent-soft:rgba(245,158,11,.14);">
                    <div class="tg-tpl-card-hdr">
                      <span class="tg-tpl-dot"></span>
                      <span class="tg-tpl-name">IP Timeout</span>
                      <span class="tg-tpl-vars">{nama} {cid} {ip} {waktu}</span>
                    </div>
                    <textarea id="tg-tpl-ip_down" rows="4"></textarea>
                  </div>
                  <div class="tg-tpl-card" style="--accent:#22c55e;--accent-soft:rgba(34,197,94,.14);">
                    <div class="tg-tpl-card-hdr">
                      <span class="tg-tpl-dot"></span>
                      <span class="tg-tpl-name">IP Pulih</span>
                      <span class="tg-tpl-vars">{nama} {cid} {ip} {waktu}</span>
                    </div>
                    <textarea id="tg-tpl-ip_recover" rows="3"></textarea>
                  </div>
                  <div class="tg-tpl-card" style="--accent:#f97316;--accent-soft:rgba(249,115,22,.14);">
                    <div class="tg-tpl-card-hdr">
                      <span class="tg-tpl-dot"></span>
                      <span class="tg-tpl-name">PPPoE Disconnect</span>
                      <span class="tg-tpl-vars">{nama} {cid} {user} {waktu}</span>
                    </div>
                    <textarea id="tg-tpl-pppoe_down" rows="4"></textarea>
                  </div>
                  <div class="tg-tpl-card" style="--accent:#22c55e;--accent-soft:rgba(34,197,94,.14);">
                    <div class="tg-tpl-card-hdr">
                      <span class="tg-tpl-dot"></span>
                      <span class="tg-tpl-name">PPPoE Pulih</span>
                      <span class="tg-tpl-vars">{nama} {cid} {user} {ip} {waktu}</span>
                    </div>
                    <textarea id="tg-tpl-pppoe_recover" rows="3"></textarea>
                  </div>
                </div>

                <div style="margin-top:12px;">
                  <button type="button" class="pf-btn-outline" style="font-size:12px;padding:8px 14px;color:var(--d-muted);" onclick="resetTelegramTemplates()">
                    <svg width="14" height="14" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" viewBox="0 0 24 24"><path d="M3 12a9 9 0 1 0 3-6.7L3 8"/><path d="M3 3v5h5"/></svg>
                    Kembalikan ke Format Bawaan
                  </button>
                </div>

                <div class="tg-tpl-note">
                  <svg width="15" height="15" fill="none" stroke="#d99e0b" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" viewBox="0 0 24 24" style="flex:none;margin-top:1px;"><path d="M12 16v-4"/><path d="M12 8h.01"/><circle cx="12" cy="12" r="10"/></svg>
                  <span>Notifikasi hanya dikirim saat <b>perubahan status</b> (online→down dan pemulihan), bukan berulang tiap menit. Pengecekan berjalan tiap 3 menit.</span>
                </div>

                <div style="display:flex;gap:10px;margin-top:18px;flex-wrap:wrap;">
                  <button class="save-btn" onclick="saveTelegramSettings()">Simpan</button>
                  <button class="save-btn" style="background:#fff;color:var(--d-blue1);border:1.5px solid var(--d-blue1);" onclick="testTelegram()" id="tg-test-btn">Tes Koneksi &amp; Kirim Pesan</button>
                  <button class="save-btn" style="background:#fff;color:#f59e0b;border:1.5px solid #f59e0b;" onclick="runTelegramChecks()" id="tg-run-btn">Jalankan Pengecekan Sekarang</button>
                  <span id="tg-test-result" style="font-size:12.5px;align-self:center;"></span>
                </div>
                <div style="font-size:11px;color:var(--d-muted);margin-top:6px;line-height:1.5;">
                  <b>Jalankan Pengecekan Sekarang</b> memaksa sistem memeriksa semua perangkat/ONT/IP/PPPoE saat ini (mereset baseline), tanpa menunggu jadwal. Notifikasi terkirim bila ada yang sedang down.
                </div>
              </div>
            </div>
          </div>

          <!-- ISOLIR & SUSPEND SECTION -->
          <div class="st-section" id="sec-isolir">
            <div class="st-card">
              <div class="st-card-hdr">
                <div class="st-card-icon"><svg width="16" height="16" fill="none" stroke="#fff" stroke-width="2" viewBox="0 0 24 24"><circle cx="12" cy="12" r="10"/><line x1="4.93" y1="4.93" x2="19.07" y2="19.07"/></svg></div>
                <div>
                  <div style="font-size:14px;font-weight:700;color:var(--d-text);">Kebijakan Isolir &amp; Suspend</div>
                  <div style="font-size:12px;color:var(--d-muted);">Atur kapan & bagaimana pelanggan menunggak diisolir otomatis</div>
                </div>
              </div>
              <div class="st-card-body">
                <div class="op-grid">
                  <div>
                    <label class="pf-label">Grace Period (hari)</label>
                    <input type="number" class="pf-input" id="isoGraceDays" min="0" max="60" placeholder="3">
                    <div class="op-hint">Jumlah hari setelah jatuh tempo sebelum pelanggan diisolir.</div>
                  </div>
                  <div>
                    <label class="pf-label">Jam Auto-Isolir</label>
                    <select class="pf-input" id="isoAutoHour">
                      <option value="-1">Setiap jam (begitu lewat grace)</option>
                    </select>
                    <div class="op-hint">Jam (server) saat proses auto-isolir dijalankan tiap hari.</div>
                  </div>
                </div>

                <div class="op-toggle-row">
                  <label class="op-switch"><input type="checkbox" id="isoAutoEnable"><span></span></label>
                  <div>
                    <div class="pf-label" style="margin:0;">Aktifkan Auto-Isolir</div>
                    <div class="op-hint">Sistem otomatis mengisolir pelanggan menunggak sesuai aturan di atas.</div>
                  </div>
                </div>
                <div class="op-toggle-row">
                  <label class="op-switch"><input type="checkbox" id="isoNotifyWa"><span></span></label>
                  <div>
                    <div class="pf-label" style="margin:0;">Kirim Notifikasi WA saat Diisolir</div>
                    <div class="op-hint">Pelanggan menerima pesan WhatsApp ketika layanannya diisolir.</div>
                  </div>
                </div>

                <label class="pf-label" style="margin-top:14px;">Template Pesan WA Isolir</label>
                <textarea class="pf-input" id="isoWaMessage" rows="4" placeholder="Yth {nama}, layanan internet Anda (ID: {cid}) untuk sementara kami nonaktifkan karena tagihan belum dibayar. Total: {tagihan}. Bayar di: {url}" style="resize:vertical;font-family:inherit;"></textarea>
                <div class="op-hint">Placeholder: <code>{nama}</code> <code>{cid}</code> <code>{tagihan}</code> <code>{url}</code></div>

                <div style="margin-top:18px;">
                  <button class="op-btn-primary" onclick="saveIsolirSettings()">Simpan Pengaturan Isolir</button>
                </div>
              </div>
            </div>
          </div>

          <!-- WHATSAPP GATEWAY SECTION -->
          <div class="st-section" id="sec-wagateway">
            <div class="st-card">
              <div class="st-card-hdr">
                <div class="st-card-icon"><svg width="16" height="16" fill="none" stroke="#fff" stroke-width="2" viewBox="0 0 24 24"><path d="M21 11.5a8.38 8.38 0 0 1-.9 3.8 8.5 8.5 0 0 1-7.6 4.7 8.38 8.38 0 0 1-3.8-.9L3 21l1.9-5.7a8.38 8.38 0 0 1-.9-3.8 8.5 8.5 0 0 1 4.7-7.6 8.38 8.38 0 0 1 3.8-.9h.5a8.48 8.48 0 0 1 8 8z"/></svg></div>
                <div>
                  <div style="font-size:14px;font-weight:700;color:var(--d-text);">WhatsApp Gateway</div>
                  <div style="font-size:12px;color:var(--d-muted);">Session default untuk notifikasi & reminder otomatis</div>
                </div>
              </div>
              <div class="st-card-body">
                <label class="pf-label">Session Default (Pengiriman Otomatis)</label>
                <select class="pf-input" id="waDefaultSession"><option value="">Memuat session…</option></select>
                <div class="op-hint">Dipakai untuk semua pengiriman otomatis: reminder tagihan, notifikasi isolir, konfirmasi pembayaran, dll.</div>

                <label class="pf-label" style="margin-top:16px;">Status Session</label>
                <div id="waSessionStatus" class="op-session-list"><div class="op-hint">Memuat…</div></div>

                <div style="margin-top:18px;display:flex;gap:10px;flex-wrap:wrap;">
                  <button class="op-btn-primary" onclick="saveWaGateway()">Simpan Session Default</button>
                  <button class="op-btn-outline" onclick="loadWaGateway()">Refresh Status</button>
                  <a href="/whatsapp" class="op-btn-outline" style="text-decoration:none;">Kelola Session &rarr;</a>
                </div>
              </div>
            </div>
          </div>

          <!-- INVOICE & PENOMORAN SECTION -->
          <div class="st-section" id="sec-invoice">
            <div class="st-card">
              <div class="st-card-hdr">
                <div class="st-card-icon"><svg width="16" height="16" fill="none" stroke="#fff" stroke-width="2" viewBox="0 0 24 24"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/></svg></div>
                <div>
                  <div style="font-size:14px;font-weight:700;color:var(--d-text);">Invoice &amp; Penomoran</div>
                  <div style="font-size:12px;color:var(--d-muted);">Format nomor, jatuh tempo default, denda keterlambatan</div>
                </div>
              </div>
              <div class="st-card-body">
                <div class="op-grid">
                  <div>
                    <label class="pf-label">Prefix Nomor Invoice</label>
                    <input type="text" class="pf-input" id="invPrefix" maxlength="8" placeholder="INV" oninput="updateInvPreview()">
                    <div class="op-hint">Huruf/angka saja. Contoh: <code>INV</code>, <code>VICTORI</code>.</div>
                  </div>
                  <div>
                    <label class="pf-label">Preview Nomor</label>
                    <div id="invPreview" class="op-preview">INV-2606-00001</div>
                    <div class="op-hint">Format: <code>PREFIX-TahunBulan-Urutan</code></div>
                  </div>
                </div>

                <div class="op-grid" style="margin-top:8px;">
                  <div>
                    <label class="pf-label">Jatuh Tempo Default (hari)</label>
                    <input type="number" class="pf-input" id="invDueDays" min="0" max="60" placeholder="30">
                    <div class="op-hint">Default offset jatuh tempo saat mencatat pembayaran baru.</div>
                  </div>
                  <div>
                    <label class="pf-label">Denda Keterlambatan (%)</label>
                    <input type="number" class="pf-input" id="invLateFee" min="0" max="100" step="0.5" placeholder="0">
                    <div class="op-hint">Persentase denda dari nilai tagihan (0 = tanpa denda).</div>
                  </div>
                </div>

                <div style="margin-top:18px;">
                  <button class="op-btn-primary" onclick="saveInvoiceSettings()">Simpan Pengaturan Invoice</button>
                </div>
                <div class="op-hint" style="margin-top:10px;">
                  <b>Catatan:</b> Mengubah prefix hanya berlaku untuk invoice baru; invoice lama tetap dengan nomor yang sudah ada.
                </div>
              </div>
            </div>
          </div>

          <!-- TEMA WARNA SECTION -->
          <div class="st-section" id="sec-theme">
            <div class="st-card">
              <div class="st-card-hdr">
                <div class="st-card-icon"><svg width="16" height="16" fill="none" stroke="#fff" stroke-width="2" viewBox="0 0 24 24"><circle cx="13.5" cy="6.5" r="2.5"/><circle cx="8.5" cy="7.5" r="2.5"/><circle cx="6.5" cy="12.5" r="2.5"/><path d="M12 2a10 10 0 0 0 0 20 1.5 1.5 0 0 0 1.5-1.5 1.49 1.49 0 0 0-.39-1 1.49 1.49 0 0 1-.39-1 1.5 1.5 0 0 1 1.5-1.5H16a4 4 0 0 0 4-4 8 8 0 0 0-8-8z"/></svg></div>
                <div>
                  <div style="font-size:14px;font-weight:700;color:var(--d-text);">Tema Warna (White-label)</div>
                  <div style="font-size:12px;color:var(--d-muted);">Sesuaikan warna utama aplikasi dengan brand Anda</div>
                </div>
              </div>
              <div class="st-card-body">
                <div class="op-toggle-row" style="margin-top:0;">
                  <label class="op-switch"><input type="checkbox" id="themeEnabled" onchange="updateThemePreview()"><span></span></label>
                  <div>
                    <div class="pf-label" style="margin:0;">Aktifkan Tema Kustom</div>
                    <div class="op-hint">Jika nonaktif, aplikasi memakai warna default (biru navy).</div>
                  </div>
                </div>

                <div class="op-grid" style="margin-top:16px;">
                  <div>
                    <label class="pf-label">Warna Utama (Primary)</label>
                    <div style="display:flex;align-items:center;gap:10px;">
                      <input type="color" id="themePrimary" value="#1d4ed8" oninput="syncColor('themePrimary','themePrimaryHex');updateThemePreview()" style="width:46px;height:38px;border:1px solid var(--d-border);border-radius:9px;cursor:pointer;padding:2px;background:#fff;">
                      <input type="text" class="pf-input" id="themePrimaryHex" value="#1d4ed8" maxlength="7" oninput="syncHex('themePrimaryHex','themePrimary');updateThemePreview()" style="max-width:120px;">
                    </div>
                    <div class="op-hint">Tombol, link, header, highlight.</div>
                  </div>
                  <div>
                    <label class="pf-label">Warna Aksen (Accent)</label>
                    <div style="display:flex;align-items:center;gap:10px;">
                      <input type="color" id="themeAccent" value="#2563eb" oninput="syncColor('themeAccent','themeAccentHex');updateThemePreview()" style="width:46px;height:38px;border:1px solid var(--d-border);border-radius:9px;cursor:pointer;padding:2px;background:#fff;">
                      <input type="text" class="pf-input" id="themeAccentHex" value="#2563eb" maxlength="7" oninput="syncHex('themeAccentHex','themeAccent');updateThemePreview()" style="max-width:120px;">
                    </div>
                    <div class="op-hint">Gradien & elemen sekunder.</div>
                  </div>
                </div>

                <label class="pf-label" style="margin-top:16px;">Preview</label>
                <div id="themePreview" style="display:flex;align-items:center;gap:12px;padding:18px;border-radius:13px;background:#f8faff;border:1px solid var(--d-border);flex-wrap:wrap;">
                  <span id="tpBtn" style="padding:9px 16px;border-radius:10px;background:#1d4ed8;color:#fff;font-weight:700;font-size:13px;">Tombol Utama</span>
                  <span id="tpGrad" style="padding:9px 16px;border-radius:10px;background:linear-gradient(135deg,#1d4ed8,#2563eb);color:#fff;font-weight:700;font-size:13px;">Gradien</span>
                  <span id="tpLink" style="color:#1d4ed8;font-weight:700;font-size:13px;">Teks Link</span>
                </div>

                <div style="margin-top:18px;">
                  <button class="op-btn-primary" onclick="saveThemeSettings()">Simpan Tema</button>
                  <button class="op-btn-outline" onclick="resetTheme()">Reset ke Default</button>
                </div>
                <div class="op-hint" style="margin-top:10px;">Perubahan tema berlaku di semua halaman setelah disimpan & refresh.</div>
              </div>
            </div>
          </div>

          <!-- EMAIL / SMTP SECTION -->
          <div class="st-section" id="sec-email">
            <div class="st-card">
              <div class="st-card-hdr">
                <div class="st-card-icon"><svg width="16" height="16" fill="none" stroke="#fff" stroke-width="2" viewBox="0 0 24 24"><path d="M4 4h16a2 2 0 0 1 2 2v12a2 2 0 0 1-2 2H4a2 2 0 0 1-2-2V6a2 2 0 0 1 2-2z"/><polyline points="22,6 12,13 2,6"/></svg></div>
                <div>
                  <div style="font-size:14px;font-weight:700;color:var(--d-text);">Email / SMTP</div>
                  <div style="font-size:12px;color:var(--d-muted);">Kirim invoice & notifikasi via email</div>
                </div>
              </div>
              <div class="st-card-body">
                <div class="op-toggle-row" style="margin-top:0;">
                  <label class="op-switch"><input type="checkbox" id="smtpEnabled"><span></span></label>
                  <div>
                    <div class="pf-label" style="margin:0;">Aktifkan Email SMTP</div>
                    <div class="op-hint">Mengaktifkan pengiriman email melalui server SMTP Anda.</div>
                  </div>
                </div>

                <div class="op-grid" style="margin-top:16px;">
                  <div>
                    <label class="pf-label">SMTP Host</label>
                    <input type="text" class="pf-input" id="smtpHost" placeholder="smtp.gmail.com">
                  </div>
                  <div>
                    <label class="pf-label">Port</label>
                    <input type="number" class="pf-input" id="smtpPort" placeholder="587">
                  </div>
                </div>
                <div class="op-grid" style="margin-top:8px;">
                  <div>
                    <label class="pf-label">Username / Email</label>
                    <input type="text" class="pf-input" id="smtpUser" placeholder="email@domain.com" autocomplete="off">
                  </div>
                  <div>
                    <label class="pf-label">Password / App Password</label>
                    <input type="password" class="pf-input" id="smtpPass" placeholder="••••••••" autocomplete="new-password">
                  </div>
                </div>
                <div class="op-grid" style="margin-top:8px;">
                  <div>
                    <label class="pf-label">Nama Pengirim</label>
                    <input type="text" class="pf-input" id="smtpFromName" placeholder="Skynet / Nama ISP Anda">
                    <div class="op-hint">Nama yang tampil sebagai pengirim di inbox penerima.</div>
                  </div>
                  <div>
                    <label class="pf-label">Email Pengirim (From)</label>
                    <input type="text" class="pf-input" id="smtpFrom" placeholder="noreply@domain.com">
                    <div class="op-hint">Gmail biasanya tetap memakai email akun Anda, tapi <b>nama pengirim</b> akan mengikuti setting di atas.</div>
                  </div>
                </div>
                <div class="op-grid" style="margin-top:8px;">
                  <div>
                    <label class="pf-label">Koneksi Aman (SSL/TLS)</label>
                    <select class="pf-input" id="smtpSecure">
                      <option value="0">STARTTLS (port 587)</option>
                      <option value="1">SSL/TLS (port 465)</option>
                    </select>
                  </div>
                  <div></div>
                </div>

                <div class="op-toggle-row">
                  <label class="op-switch"><input type="checkbox" id="emailReminderEnabled"><span></span></label>
                  <div>
                    <div class="pf-label" style="margin:0;">Kirim Reminder Tagihan via Email</div>
                    <div class="op-hint">Reminder H-3/H-1/jatuh tempo/overdue juga dikirim ke email pelanggan (paralel dengan WhatsApp). Konfirmasi pembayaran (+ kwitansi PDF) & notifikasi isolir otomatis terkirim via email saat SMTP aktif.</div>
                  </div>
                </div>

                <div class="op-toggle-row">
                  <label class="op-switch"><input type="checkbox" id="emailAttachReceipt" checked><span></span></label>
                  <div>
                    <div class="pf-label" style="margin:0;">Lampirkan Kwitansi PDF</div>
                    <div class="op-hint">Sertakan file kwitansi PDF pada email konfirmasi pembayaran. Bila dimatikan, email tetap terkirim namun tanpa lampiran PDF.</div>
                  </div>
                </div>

                <div style="margin-top:18px;display:flex;gap:10px;flex-wrap:wrap;">
                  <button class="op-btn-primary" onclick="saveSmtpSettings()">Simpan SMTP</button>
                  <button class="op-btn-outline" onclick="testSmtp()">Kirim Email Test</button>
                </div>
                <div class="op-hint" style="margin-top:10px;">Gmail: aktifkan 2FA lalu buat <b>App Password</b> (bukan password akun). Host <code>smtp.gmail.com</code>, port <code>587</code>.</div>
                <div class="op-hint" style="margin-top:6px;">Format isi email invoice (subjek, judul, kalimat pembuka, footer, tombol bayar) bisa diatur & dipratinjau di halaman <a href="/invoice-broadcast" style="font-weight:700;">Broadcast Invoice</a> — pengaturannya saling sinkron dengan halaman ini.</div>
              </div>
            </div>
          </div>

          <!-- LOCALIZATION SECTION -->
          <div class="st-section" id="sec-locale">
            <div class="st-card">
              <div class="st-card-hdr">
                <div class="st-card-icon"><svg width="16" height="16" fill="none" stroke="#fff" stroke-width="2" viewBox="0 0 24 24"><circle cx="12" cy="12" r="10"/><line x1="2" y1="12" x2="22" y2="12"/><path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"/></svg></div>
                <div>
                  <div style="font-size:14px;font-weight:700;color:var(--d-text);">Localization</div>
                  <div style="font-size:12px;color:var(--d-muted);">Zona waktu, format tanggal, mata uang</div>
                </div>
              </div>
              <div class="st-card-body">
                <div class="op-grid">
                  <div>
                    <label class="pf-label">Zona Waktu</label>
                    <select class="pf-input" id="locTimezone">
                      <option value="Asia/Jakarta">WIB — Asia/Jakarta (GMT+7)</option>
                      <option value="Asia/Makassar">WITA — Asia/Makassar (GMT+8)</option>
                      <option value="Asia/Jayapura">WIT — Asia/Jayapura (GMT+9)</option>
                    </select>
                    <div class="op-hint">Dipakai untuk penjadwalan (reminder, auto-isolir) & tampilan waktu.</div>
                  </div>
                  <div>
                    <label class="pf-label">Format Tanggal</label>
                    <select class="pf-input" id="locDateFormat" onchange="updateLocalePreview()">
                      <option value="DD/MM/YYYY">DD/MM/YYYY (31/12/2026)</option>
                      <option value="DD-MM-YYYY">DD-MM-YYYY (31-12-2026)</option>
                      <option value="YYYY-MM-DD">YYYY-MM-DD (2026-12-31)</option>
                      <option value="DD MMM YYYY">DD MMM YYYY (31 Des 2026)</option>
                    </select>
                  </div>
                </div>
                <div class="op-grid" style="margin-top:8px;">
                  <div>
                    <label class="pf-label">Mata Uang</label>
                    <select class="pf-input" id="locCurrency" onchange="updateLocalePreview()">
                      <option value="IDR">IDR — Rupiah (Rp)</option>
                      <option value="USD">USD — Dollar ($)</option>
                      <option value="MYR">MYR — Ringgit (RM)</option>
                    </select>
                  </div>
                  <div>
                    <label class="pf-label">Preview</label>
                    <div id="localePreview" class="op-preview" style="font-size:13px;">31 Des 2026 · Rp 150.000</div>
                  </div>
                </div>
                <div style="margin-top:18px;">
                  <button class="op-btn-primary" onclick="saveLocaleSettings()">Simpan Localization</button>
                </div>
                <div class="op-hint" style="margin-top:10px;"><b>Catatan:</b> Zona waktu memengaruhi penjadwalan cron. Format tanggal & mata uang disimpan sebagai preferensi tampilan (diterapkan bertahap di seluruh modul).</div>
              </div>
            </div>
          </div>

          <!-- BACKUP & RESTORE SECTION -->
          <div class="st-section" id="sec-backup">
            <div class="st-card">
              <div class="st-card-hdr">
                <div class="st-card-icon"><svg width="16" height="16" fill="none" stroke="#fff" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M4 4v5h5M20 20v-5h-5"/><path stroke-linecap="round" stroke-linejoin="round" d="M5.5 9a7 7 0 0111.9-2.4L20 9M18.5 15a7 7 0 01-11.9 2.4L4 15"/></svg></div>
                <div>
                  <div style="font-size:14px;font-weight:700;color:var(--d-text);">Backup &amp; Restore Pengaturan</div>
                  <div style="font-size:12px;color:var(--d-muted);">Simpan &amp; pulihkan seluruh konfigurasi aplikasi</div>
                </div>
              </div>

              <div class="st-card-body">
                <div style="font-size:12.5px;color:var(--d-muted);line-height:1.65;margin-bottom:18px;">
                  Backup mencakup pengaturan brand &amp; tampilan, pembayaran, payment gateway, PPN/pajak,
                  Telegram Monitoring, reminder &amp; push notification (termasuk template pesan), serta template WhatsApp.
                  <b style="color:var(--d-text);">Data pelanggan, invoice, dan transaksi tidak termasuk.</b>
                  <span style="display:block;margin-top:8px;">Butuh backup <b>seluruh database</b> (termasuk pelanggan, invoice, transaksi)? Gunakan
                  <a href="/system/database" style="color:#1d4ed8;font-weight:700;text-decoration:none;">Database Management &rarr;</a></span>
                </div>

                <!-- Unduh backup -->
                <div class="bk-row">
                  <div class="bk-row-icn" style="background:#eff6ff;color:#1d4ed8;">
                    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M4 16v2a2 2 0 002 2h12a2 2 0 002-2v-2M7 11l5 5 5-5M12 4v12"/></svg>
                  </div>
                  <div class="bk-row-txt">
                    <div class="bk-row-ttl">Unduh Backup</div>
                    <div class="bk-row-sub">Ekspor seluruh pengaturan ke file <code>.json</code> untuk disimpan atau dipindahkan ke server lain.</div>
                  </div>
                  <button onclick="downloadSettingsBackup()" id="btnBackup" class="bk-btn bk-btn-primary">
                    <svg width="15" height="15" fill="none" stroke="currentColor" stroke-width="2.2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M4 16v2a2 2 0 002 2h12a2 2 0 002-2v-2M7 11l5 5 5-5M12 4v12"/></svg>
                    Unduh Backup
                  </button>
                </div>

                <!-- Restore -->
                <div class="bk-row bk-row-warn">
                  <div class="bk-row-icn" style="background:#fef3c7;color:#d97706;">
                    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M4 16v2a2 2 0 002 2h12a2 2 0 002-2v-2M7 9l5-5 5 5M12 4v12"/></svg>
                  </div>
                  <div class="bk-row-txt">
                    <div class="bk-row-ttl" style="color:#92400e;">Pulihkan dari Backup</div>
                    <div class="bk-row-sub" style="color:#a16207;">Pilih file <code>.json</code> hasil backup. Pengaturan saat ini akan <b>ditimpa</b>. Pastikan file berasal dari aplikasi ini.</div>
                    <div id="restoreFileName" style="font-size:11.5px;margin-top:7px;color:var(--d-muted);"></div>
                  </div>
                  <input type="file" id="restoreFile" accept="application/json,.json" style="display:none" onchange="onRestoreFilePicked(this)">
                  <div style="display:flex;flex-direction:column;gap:8px;flex-shrink:0;">
                    <button onclick="document.getElementById('restoreFile').click()" class="bk-btn bk-btn-outline">
                      <svg width="15" height="15" fill="none" stroke="currentColor" stroke-width="2.2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M3 16.5V19a2 2 0 002 2h14a2 2 0 002-2v-2.5M7 9l5-5 5 5M12 4v12"/></svg>
                      Pilih File
                    </button>
                    <button id="btnRestore" onclick="restoreSettings()" disabled class="bk-btn bk-btn-warn" style="display:none;">
                      <svg width="15" height="15" fill="none" stroke="currentColor" stroke-width="2.2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7"/></svg>
                      Pulihkan
                    </button>
                  </div>
                </div>
              </div>
            </div>
          </div>

        </div>
      </div>
    </main>
  </div>
</div>
<script src="https://cdn.jsdelivr.net/npm/socket.io-client@4.7.2/dist/socket.io.min.js"></script>
<script src="/js/app.js"></script>
<script>
function showSection(id, el) {
  document.querySelectorAll('.st-section').forEach(s => s.classList.remove('show'));
  document.querySelectorAll('.st-nav-item').forEach(i => i.classList.remove('active'));
  document.getElementById('sec-' + id)?.classList.add('show');
  el.classList.add('active');

  // Lazy-load section data
  if (id === 'gateway' && typeof loadGatewaySettings === 'function') {
    loadGatewaySettings();
  }
  if (id === 'isolir')    loadIsolirSettings();
  if (id === 'wagateway') loadWaGateway();
  if (id === 'invoice')   loadInvoiceSettings();
  if (id === 'theme')     loadThemeSettings();
  if (id === 'email')     loadSmtpSettings();
  if (id === 'locale')    loadLocaleSettings();
}

// ══════════════════════════════════════════════════════════════
// TEMA WARNA (white-label)
// ══════════════════════════════════════════════════════════════
function syncColor(colorId, hexId){ document.getElementById(hexId).value = document.getElementById(colorId).value; }
function syncHex(hexId, colorId){
  let v = document.getElementById(hexId).value.trim();
  if (/^#?[0-9a-fA-F]{6}$/.test(v)) { if (v[0] !== '#') v = '#'+v; document.getElementById(colorId).value = v; document.getElementById(hexId).value = v; }
}
function updateThemePreview(){
  const on = document.getElementById('themeEnabled').checked;
  const p = on ? document.getElementById('themePrimary').value : '#1d4ed8';
  const a = on ? document.getElementById('themeAccent').value  : '#2563eb';
  document.getElementById('tpBtn').style.background = p;
  document.getElementById('tpGrad').style.background = `linear-gradient(135deg,${p},${a})`;
  document.getElementById('tpLink').style.color = p;
}
async function loadThemeSettings(){
  const c = await _getAppSettings(true);
  document.getElementById('themeEnabled').checked = c.theme_enabled === '1';
  const p = c.theme_primary_color || '#1d4ed8';
  const a = c.theme_accent_color  || '#2563eb';
  document.getElementById('themePrimary').value = p; document.getElementById('themePrimaryHex').value = p;
  document.getElementById('themeAccent').value  = a; document.getElementById('themeAccentHex').value  = a;
  updateThemePreview();
}
async function saveThemeSettings(){
  const payload = {
    theme_enabled: document.getElementById('themeEnabled').checked ? '1' : '0',
    theme_primary_color: document.getElementById('themePrimary').value,
    theme_accent_color:  document.getElementById('themeAccent').value
  };
  const d = await App.api('/app-settings', { method:'POST', body: JSON.stringify(payload) });
  if (d?.success) { App.showToast('Tema disimpan. Memuat ulang…', 'success'); setTimeout(()=>location.reload(), 1200); }
  else App.showToast(d?.message || 'Gagal menyimpan', 'error');
}
function resetTheme(){
  document.getElementById('themeEnabled').checked = false;
  document.getElementById('themePrimary').value = '#1d4ed8'; document.getElementById('themePrimaryHex').value = '#1d4ed8';
  document.getElementById('themeAccent').value  = '#2563eb'; document.getElementById('themeAccentHex').value  = '#2563eb';
  updateThemePreview();
}

// ══════════════════════════════════════════════════════════════
// EMAIL / SMTP
// ══════════════════════════════════════════════════════════════
async function loadSmtpSettings(){
  const c = await _getAppSettings(true);
  document.getElementById('smtpEnabled').checked = c.smtp_enabled === '1';
  document.getElementById('smtpHost').value = c.smtp_host || '';
  document.getElementById('smtpPort').value = c.smtp_port || '587';
  document.getElementById('smtpUser').value = c.smtp_user || '';
  document.getElementById('smtpPass').value = c.smtp_pass || '';
  document.getElementById('smtpFrom').value = c.smtp_from || '';
  document.getElementById('smtpFromName').value = c.smtp_from_name || c.app_name || '';
  document.getElementById('smtpSecure').value = c.smtp_secure === '1' ? '1' : '0';
  document.getElementById('emailReminderEnabled').checked = c.email_reminder_enabled === '1';
  // Default ON kalau setting belum pernah disimpan
  document.getElementById('emailAttachReceipt').checked = (c.email_attach_receipt === undefined || c.email_attach_receipt === '' || c.email_attach_receipt === '1');
}
function _smtpPayload(){
  return {
    smtp_enabled: document.getElementById('smtpEnabled').checked ? '1' : '0',
    smtp_host: document.getElementById('smtpHost').value.trim(),
    smtp_port: document.getElementById('smtpPort').value || '587',
    smtp_user: document.getElementById('smtpUser').value.trim(),
    smtp_pass: document.getElementById('smtpPass').value,
    smtp_from: document.getElementById('smtpFrom').value.trim(),
    smtp_from_name: document.getElementById('smtpFromName').value.trim(),
    smtp_secure: document.getElementById('smtpSecure').value,
    email_reminder_enabled: document.getElementById('emailReminderEnabled').checked ? '1' : '0',
    email_attach_receipt: document.getElementById('emailAttachReceipt').checked ? '1' : '0'
  };
}
async function saveSmtpSettings(){
  const d = await App.api('/app-settings', { method:'POST', body: JSON.stringify(_smtpPayload()) });
  if (d?.success) App.showToast('Pengaturan SMTP disimpan', 'success');
  else App.showToast(d?.message || 'Gagal menyimpan', 'error');
}
async function testSmtp(){
  // Simpan dulu agar konfigurasi terbaru dipakai saat test.
  await App.api('/app-settings', { method:'POST', body: JSON.stringify(_smtpPayload()) });
  // Buka modal test custom (bukan prompt browser).
  const to = document.getElementById('smtpUser').value.trim();
  document.getElementById('testEmailTo').value = to;
  const st = document.getElementById('testEmailStatus');
  st.style.display = 'none'; st.textContent = '';
  document.getElementById('testEmailModal').style.display = 'flex';
  setTimeout(() => document.getElementById('testEmailTo').focus(), 60);
}
function closeTestEmailModal(){ document.getElementById('testEmailModal').style.display = 'none'; }
async function sendTestEmailNow(){
  const to = document.getElementById('testEmailTo').value.trim();
  const st = document.getElementById('testEmailStatus');
  const btn = document.getElementById('testEmailSendBtn');
  if (!to || !/^[^@\s]+@[^@\s]+\.[^@\s]+$/.test(to)) {
    st.style.display = 'block'; st.style.background = '#fef2f2'; st.style.color = '#b91c1c';
    st.textContent = 'Masukkan alamat email yang valid.';
    return;
  }
  const orig = btn.innerHTML;
  btn.disabled = true; btn.style.opacity = '.7'; btn.innerHTML = 'Mengirim…';
  st.style.display = 'block'; st.style.background = '#eff6ff'; st.style.color = '#1d4ed8';
  st.textContent = 'Mengirim email test ke ' + to + '…';
  try {
    const d = await App.api('/app-settings/test-email', { method:'POST', body: JSON.stringify({ to }) });
    if (d?.success) {
      st.style.background = '#f0fdf4'; st.style.color = '#15803d';
      st.textContent = '✓ ' + (d.message || 'Email test terkirim. Cek inbox/spam.');
      App.showToast('Email test terkirim', 'success');
    } else {
      st.style.background = '#fef2f2'; st.style.color = '#b91c1c';
      st.textContent = '✕ ' + (d?.message || 'Gagal mengirim email test.');
    }
  } catch (e) {
    st.style.background = '#fef2f2'; st.style.color = '#b91c1c';
    st.textContent = '✕ Gagal: ' + e.message;
  } finally {
    btn.disabled = false; btn.style.opacity = '1'; btn.innerHTML = orig;
  }
}

// ══════════════════════════════════════════════════════════════
// LOCALIZATION
// ══════════════════════════════════════════════════════════════
function updateLocalePreview(){
  const fmt = document.getElementById('locDateFormat').value;
  const cur = document.getElementById('locCurrency').value;
  const d = new Date(2026, 11, 31);
  const dd = '31', mm = '12', yyyy = '2026';
  const monShort = 'Des';
  let dateStr = fmt.replace('YYYY',yyyy).replace('MMM',monShort).replace('DD',dd).replace('MM',mm);
  const curMap = { IDR:'Rp 150.000', USD:'$ 150,000', MYR:'RM 150,000' };
  document.getElementById('localePreview').textContent = dateStr + ' · ' + (curMap[cur] || 'Rp 150.000');
}
async function loadLocaleSettings(){
  const c = await _getAppSettings(true);
  document.getElementById('locTimezone').value   = c.locale_timezone   || 'Asia/Jakarta';
  document.getElementById('locDateFormat').value = c.locale_date_format || 'DD/MM/YYYY';
  document.getElementById('locCurrency').value   = c.locale_currency   || 'IDR';
  updateLocalePreview();
}
async function saveLocaleSettings(){
  const payload = {
    locale_timezone: document.getElementById('locTimezone').value,
    locale_date_format: document.getElementById('locDateFormat').value,
    locale_currency: document.getElementById('locCurrency').value
  };
  const d = await App.api('/app-settings', { method:'POST', body: JSON.stringify(payload) });
  if (d?.success) App.showToast('Pengaturan localization disimpan', 'success');
  else App.showToast(d?.message || 'Gagal menyimpan', 'error');
}

// ══════════════════════════════════════════════════════════════
// OPERASIONAL: Isolir, WhatsApp Gateway, Invoice
// ══════════════════════════════════════════════════════════════
let _opAppSettings = null;
async function _getAppSettings(force) {
  if (_opAppSettings && !force) return _opAppSettings;
  const d = await App.api('/app-settings');
  _opAppSettings = (d && d.success) ? (d.data || {}) : {};
  return _opAppSettings;
}

// ── Isolir ──
async function loadIsolirSettings() {
  // Isi dropdown jam 0-23 sekali
  const hourSel = document.getElementById('isoAutoHour');
  if (hourSel && hourSel.options.length <= 1) {
    for (let h = 0; h < 24; h++) {
      const o = document.createElement('option');
      o.value = String(h); o.textContent = String(h).padStart(2,'0') + ':00';
      hourSel.appendChild(o);
    }
  }
  try {
    const d = await App.api('/isolir/settings');
    const c = (d && d.success) ? (d.data || {}) : {};
    document.getElementById('isoGraceDays').value = c.isolir_grace_days ?? '3';
    document.getElementById('isoAutoHour').value  = (c.isolir_auto_hour ?? '') === '' ? '-1' : c.isolir_auto_hour;
    document.getElementById('isoAutoEnable').checked = c.isolir_auto_enable === '1';
    document.getElementById('isoNotifyWa').checked   = c.isolir_notify_wa === '1' || c.isolir_notify_wa === undefined;
    document.getElementById('isoWaMessage').value    = c.isolir_wa_message || '';
  } catch (e) { App.showToast('Gagal memuat pengaturan isolir', 'error'); }
}
async function saveIsolirSettings() {
  const payload = {
    isolir_grace_days: document.getElementById('isoGraceDays').value || '0',
    isolir_auto_hour:  document.getElementById('isoAutoHour').value,   // '-1' = setiap jam
    isolir_auto_enable: document.getElementById('isoAutoEnable').checked ? '1' : '0',
    isolir_notify_wa:   document.getElementById('isoNotifyWa').checked ? '1' : '0',
    isolir_wa_message:  document.getElementById('isoWaMessage').value || ''
  };
  const d = await App.api('/isolir/settings', { method:'POST', body: JSON.stringify(payload) });
  if (d?.success) App.showToast('Pengaturan isolir disimpan', 'success');
  else App.showToast(d?.message || 'Gagal menyimpan', 'error');
}

// ── WhatsApp Gateway ──
async function loadWaGateway() {
  const sel = document.getElementById('waDefaultSession');
  const statusBox = document.getElementById('waSessionStatus');
  try {
    const [sessRes, settings] = await Promise.all([ App.api('/wa/sessions'), _getAppSettings(true) ]);
    const sessions = (sessRes && sessRes.success) ? (sessRes.data || []) : [];
    const current = settings.default_wa_session || '';

    sel.innerHTML = '<option value="">— Tidak ada / pilih session —</option>' +
      sessions.map(s => `<option value="${s.session_id}" ${s.session_id===current?'selected':''}>${(s.name||s.session_id)} (${s.session_id})</option>`).join('');

    if (!sessions.length) {
      statusBox.innerHTML = '<div class="op-hint">Belum ada session WhatsApp. Tambahkan di halaman <a href="/whatsapp" style="color:#1d4ed8;font-weight:700;">WhatsApp</a>.</div>';
      return;
    }
    statusBox.innerHTML = sessions.map(s => {
      const st = (s.runtime_status || s.status || 'disconnected').toLowerCase();
      const on = st === 'connected' || st === 'ready' || st === 'authenticated';
      return `<div class="op-session-row">
        <span class="op-session-dot ${on?'on':'off'}"></span>
        <div>
          <div class="op-session-name">${s.name || s.session_id}</div>
          <div class="op-session-meta">${s.session_id} · ${s.provider || 'baileys'}${s.phone_number?' · '+s.phone_number:''}</div>
        </div>
        <span class="op-session-badge ${on?'on':'off'}">${on?'Terhubung':'Terputus'}</span>
      </div>`;
    }).join('');
  } catch (e) {
    statusBox.innerHTML = '<div class="op-hint">Gagal memuat status session.</div>';
  }
}
async function saveWaGateway() {
  const val = document.getElementById('waDefaultSession').value;
  const d = await App.api('/app-settings', { method:'POST', body: JSON.stringify({ default_wa_session: val }) });
  if (d?.success) App.showToast('Session default disimpan', 'success');
  else App.showToast(d?.message || 'Gagal menyimpan', 'error');
}

// ── Invoice & Penomoran ──
function updateInvPreview() {
  let p = (document.getElementById('invPrefix').value || 'INV').replace(/[^A-Za-z0-9]/g,'').toUpperCase() || 'INV';
  const now = new Date();
  const yy = String(now.getFullYear()).slice(-2);
  const mm = String(now.getMonth()+1).padStart(2,'0');
  document.getElementById('invPreview').textContent = `${p}-${yy}${mm}-00001`;
}
async function loadInvoiceSettings() {
  try {
    const c = await _getAppSettings(true);
    document.getElementById('invPrefix').value   = c.invoice_prefix || 'INV';
    document.getElementById('invDueDays').value  = c.invoice_due_days || '30';
    document.getElementById('invLateFee').value  = c.invoice_late_fee_percent || '0';
    updateInvPreview();
  } catch (e) { App.showToast('Gagal memuat pengaturan invoice', 'error'); }
}
async function saveInvoiceSettings() {
  const prefix = (document.getElementById('invPrefix').value || 'INV').replace(/[^A-Za-z0-9]/g,'').toUpperCase() || 'INV';
  const payload = {
    invoice_prefix: prefix,
    invoice_due_days: document.getElementById('invDueDays').value || '30',
    invoice_late_fee_percent: document.getElementById('invLateFee').value || '0'
  };
  const d = await App.api('/app-settings', { method:'POST', body: JSON.stringify(payload) });
  if (d?.success) { App.showToast('Pengaturan invoice disimpan', 'success'); document.getElementById('invPrefix').value = prefix; updateInvPreview(); }
  else App.showToast(d?.message || 'Gagal menyimpan', 'error');
}

// ══════════════════════════════════════════════════════════════
// BACKUP & RESTORE PENGATURAN
// ══════════════════════════════════════════════════════════════
let _restorePayload = null;

async function downloadSettingsBackup() {
  const btn = document.getElementById('btnBackup');
  const orig = btn.innerHTML;
  btn.disabled = true; btn.style.opacity = '.7'; btn.innerHTML = 'Menyiapkan...';
  try {
    const token = (function(){ try { return localStorage.getItem('token'); } catch(_) { return null; } })();
    const headers = {};
    if (token && token !== 'null') headers['Authorization'] = 'Bearer ' + token;
    const res = await fetch('/api/app-settings/backup', { headers, credentials: 'include' });
    if (!res.ok) throw new Error('HTTP ' + res.status);
    const blob = await res.blob();
    // Ambil nama file dari header bila ada
    let fname = 'skynet-settings-backup.json';
    const cd = res.headers.get('Content-Disposition') || '';
    const m = cd.match(/filename="?([^"]+)"?/);
    if (m) fname = m[1];
    const url = URL.createObjectURL(blob);
    const a = document.createElement('a');
    a.href = url; a.download = fname; document.body.appendChild(a); a.click();
    a.remove(); URL.revokeObjectURL(url);
    App.showToast('Backup berhasil diunduh', 'success');
  } catch (e) {
    App.showToast('Gagal membuat backup: ' + e.message, 'error');
  } finally {
    btn.disabled = false; btn.style.opacity = '1'; btn.innerHTML = orig;
  }
}

function onRestoreFilePicked(input) {
  const nameEl = document.getElementById('restoreFileName');
  const btn = document.getElementById('btnRestore');
  _restorePayload = null;
  const file = input.files && input.files[0];
  if (!file) { nameEl.textContent = ''; btn.style.display = 'none'; btn.disabled = true; return; }
  const reader = new FileReader();
  reader.onload = () => {
    try {
      const json = JSON.parse(reader.result);
      const ok = json && (json.app_settings || json.reminder_settings || json.push_templates || json.wa_templates);
      if (!ok) throw new Error('Struktur tidak dikenali');
      _restorePayload = json;
      nameEl.textContent = file.name;
      nameEl.style.color = '#15803d';
      btn.style.display = 'inline-flex'; btn.disabled = false;
    } catch (e) {
      _restorePayload = null;
      nameEl.textContent = 'File tidak valid (' + e.message + ')';
      nameEl.style.color = '#dc2626';
      btn.style.display = 'none'; btn.disabled = true;
    }
  };
  reader.onerror = () => { App.showToast('Gagal membaca file', 'error'); };
  reader.readAsText(file);
}

async function restoreSettings() {
  if (!_restorePayload) { App.showToast('Pilih file backup dulu', 'error'); return; }
  const meta = _restorePayload._meta || {};
  const when = meta.exported_at ? new Date(meta.exported_at).toLocaleString('id-ID') : 'tidak diketahui';
  if (!confirm('Pulihkan pengaturan dari backup ini?\n\nDibuat: ' + when + '\n\nPengaturan saat ini akan ditimpa. Lanjutkan?')) return;

  const btn = document.getElementById('btnRestore');
  const orig = btn.innerHTML;
  btn.disabled = true; btn.style.opacity = '.7'; btn.innerHTML = 'Memulihkan...';
  try {
    const d = await App.api('/app-settings/restore', { method: 'POST', body: JSON.stringify(_restorePayload) });
    if (d?.success) {
      const s = d.summary || {};
      App.showToast('Pengaturan dipulihkan (' + (s.app_settings||0) + ' setting). Halaman akan dimuat ulang...', 'success');
      setTimeout(() => location.reload(), 1500);
    } else {
      App.showToast(d?.message || 'Gagal memulihkan', 'error');
      btn.disabled = false; btn.style.opacity = '1'; btn.innerHTML = orig;
    }
  } catch (e) {
    App.showToast('Gagal: ' + e.message, 'error');
    btn.disabled = false; btn.style.opacity = '1'; btn.innerHTML = orig;
  }
}

function selectBrand(mode, el) {
  document.querySelectorAll('.brand-option').forEach(o => o.classList.remove('selected'));
  el.classList.add('selected');
  document.getElementById('brandMode').value = mode;
  // Show/hide tagline + logo fields
  document.getElementById('taglineField').style.opacity = mode === 'name_tagline' ? '1' : '.4';
  document.getElementById('logoField').style.display    = mode === 'name_tagline' ? 'none' : 'block';
  updatePreview();
}



async function saveBrandSettings() {
  const data = {
    brand_mode:  document.getElementById('brandMode').value,
    app_name:    document.getElementById('appName')?.value,
    app_tagline: document.getElementById('appTagline')?.value,
    logo_url:    document.getElementById('logoUrl')?.value,
    register_bg_url:     document.getElementById('registerBgUrl')?.value || '',
    register_bg_overlay: document.getElementById('registerBgOverlay')?.value || '0.82',
  };
  const d = await App.api('/app-settings', { method:'POST', body: JSON.stringify(data) });
  if (d?.success) { App.showToast('Pengaturan disimpan! Halaman akan direfresh...', 'success'); setTimeout(() => location.reload(), 1200); }
  else App.showToast(d?.message || 'Gagal menyimpan', 'error');
}

// ══════════════════════════════════════════════════════════════
// PPN / TAX SETTINGS handlers
// ══════════════════════════════════════════════════════════════
function _fmtRpId(n) {
  const v = Math.round(Number(n) || 0);
  return 'Rp ' + v.toLocaleString('id-ID');
}

function _computeTaxPreview(price, rate, mode) {
  const p = Math.max(0, parseFloat(price) || 0);
  const r = Math.max(0, parseFloat(rate) || 0);
  if (r <= 0) return { subtotal: p, tax: 0, total: p };
  if (mode === 'inclusive') {
    const total = p;
    const tax = Math.round(p * r / (100 + r));
    return { subtotal: total - tax, tax, total };
  }
  const subtotal = p;
  const tax = Math.round(p * r / 100);
  return { subtotal, tax, total: subtotal + tax };
}

function updateTaxPreview() {
  const enabled = document.getElementById('taxEnabled')?.checked;
  const rateEl  = document.getElementById('taxRate');
  const modeEl  = document.getElementById('taxMode');
  const labelEl = document.getElementById('taxLabel');
  const baseEl  = document.getElementById('taxPreviewBase');
  if (!rateEl || !modeEl || !baseEl) return;

  const rate  = enabled ? parseFloat(rateEl.value) || 0 : 0;
  const mode  = modeEl.value;
  const label = (labelEl?.value || 'PPN').trim() || 'PPN';
  const base  = parseFloat(baseEl.value) || 0;

  const calc = _computeTaxPreview(base, rate, mode);
  document.getElementById('taxPrevSubtotal').textContent = _fmtRpId(calc.subtotal);
  document.getElementById('taxPrevTax').textContent      = _fmtRpId(calc.tax);
  document.getElementById('taxPrevTotal').textContent    = _fmtRpId(calc.total);
  document.getElementById('taxPrevTaxLbl').textContent   = enabled && rate > 0
    ? (label + ' ' + (Number.isInteger(rate) ? rate : rate.toFixed(2)) + '%')
    : 'Tidak aktif';
}

function setTaxRate(v) {
  const el = document.getElementById('taxRate');
  if (!el) return;
  el.value = v;
  updateTaxPreview();
}

async function onTaxEnabledChange() {
  const cb = document.getElementById('taxEnabled');
  const lbl = document.getElementById('taxEnabledLbl');
  const body = document.getElementById('taxCardBody');
  if (!cb) return;

  // Update UI dulu biar responsif
  if (lbl)  lbl.textContent = cb.checked ? 'Aktif' : 'Nonaktif';
  if (body) body.classList.toggle('tax-disabled', !cb.checked);
  updateTaxPreview();

  // Skip auto-save saat init paint (DOMContentLoaded) — supaya tidak hit endpoint
  // ketika halaman pertama kali render. Kita pakai flag _taxInited yang di-set
  // setelah listener pertama kali siap.
  if (!window._taxInited) return;

  // Auto-save: kirim hanya tax_enabled ke server (rate/mode/label tidak diubah)
  const value = cb.checked ? '1' : '0';
  try {
    const d = await App.api('/app-settings', { method:'POST', body: JSON.stringify({ tax_enabled: value }) });
    if (d?.success) {
      App.showToast(cb.checked ? 'PPN diaktifkan' : 'PPN dinonaktifkan', 'success');
    } else {
      App.showToast(d?.message || 'Gagal menyimpan status PPN', 'error');
      // Rollback toggle ke state sebelumnya
      cb.checked = !cb.checked;
      if (lbl)  lbl.textContent = cb.checked ? 'Aktif' : 'Nonaktif';
      if (body) body.classList.toggle('tax-disabled', !cb.checked);
      updateTaxPreview();
    }
  } catch (err) {
    App.showToast('Gagal menyimpan status PPN: ' + (err?.message || 'network error'), 'error');
    cb.checked = !cb.checked;
    if (lbl)  lbl.textContent = cb.checked ? 'Aktif' : 'Nonaktif';
    if (body) body.classList.toggle('tax-disabled', !cb.checked);
    updateTaxPreview();
  }
}

async function saveTaxSettings() {
  const enabled = document.getElementById('taxEnabled')?.checked ? '1' : '0';
  const rateRaw = document.getElementById('taxRate')?.value;
  const rate    = parseFloat(rateRaw);
  if (rateRaw === '' || isNaN(rate) || rate < 0 || rate > 100) {
    App.showToast('Tarif PPN tidak valid (harus 0–100%)', 'error');
    return;
  }
  const mode  = document.getElementById('taxMode')?.value || 'exclusive';
  const label = (document.getElementById('taxLabel')?.value || 'PPN').trim() || 'PPN';
  const data = {
    tax_enabled: enabled,
    tax_rate:    String(rate),
    tax_mode:    mode,
    tax_label:   label
  };
  const d = await App.api('/app-settings', { method:'POST', body: JSON.stringify(data) });
  if (d?.success) {
    App.showToast('PPN tersimpan. Tagihan baru akan menggunakan tarif ini.', 'success');
  } else {
    App.showToast(d?.message || 'Gagal menyimpan PPN', 'error');
  }
}

// ══════════════════════════════════════════════════════════════
// RECALCULATE PPN — apply current setting ke invoice unpaid/overdue
// Dipakai ketika user mengubah tarif PPN atau menonaktifkan PPN dan
// ingin invoice yang masih outstanding ikut mengikuti setting baru.
// Invoice yang sudah 'paid' atau 'cancelled' TIDAK disentuh (audit safety).
// ══════════════════════════════════════════════════════════════
async function recalculateInvoiceTax() {
  const btn = document.getElementById('recalcTaxBtn');
  const enabled = document.getElementById('taxEnabled')?.checked;
  const rate    = document.getElementById('taxRate')?.value || '0';
  const label   = document.getElementById('taxLabel')?.value || 'PPN';

  // Konfirmasi pakai modal sederhana — pakai pola showBillConfirm kalau ada,
  // fallback ke window.confirm()
  const msg = enabled
    ? `Terapkan tarif <b>${label} ${rate}%</b> ke seluruh invoice yang masih unpaid/overdue?<br><br><span style="font-size:12px;color:#64748b;">Invoice yang sudah lunas tidak akan diubah. Pastikan tarif sudah disimpan dulu.</span>`
    : `Hapus PPN dari seluruh invoice yang masih unpaid/overdue?<br><br><span style="font-size:12px;color:#64748b;">Kolom tax akan di-set 0 dan total = subtotal. Invoice yang sudah lunas tidak akan diubah. Pastikan setting <b>Nonaktif</b> sudah disimpan dulu.</span>`;

  const proceed = (typeof showBillConfirm === 'function')
    ? new Promise(resolve => {
        showBillConfirm('Recalculate PPN', msg, '#0d9488', () => resolve(true));
        // Jika user batal, modal akan di-remove tapi promise tidak resolve.
        // Untuk kasus ini cukup — operasi tidak jalan sampai user klik confirm.
      })
    : Promise.resolve(window.confirm(msg.replace(/<[^>]+>/g, '')));

  const ok = await proceed;
  if (!ok) return;

  if (btn) {
    btn.disabled = true;
    btn.style.opacity = '.6';
    btn.style.cursor = 'wait';
    btn.dataset._origHtml = btn.innerHTML;
    btn.innerHTML = '<svg width="13" height="13" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24" style="animation:spin 1s linear infinite;"><path stroke-linecap="round" stroke-linejoin="round" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9"/></svg> Memproses…';
  }

  try {
    const d = await App.api('/billing/recalculate-tax', { method:'POST', body: JSON.stringify({}) });
    if (d?.success) {
      const upd = d.data?.updated || 0;
      const skp = d.data?.skipped || 0;
      App.showToast(`Recalculate selesai: ${upd} invoice di-update, ${skp} sudah sesuai`, 'success');
    } else {
      App.showToast(d?.message || 'Gagal recalculate PPN', 'error');
    }
  } catch (err) {
    App.showToast('Error: ' + (err?.message || 'network error'), 'error');
  } finally {
    if (btn) {
      btn.disabled = false;
      btn.style.opacity = '1';
      btn.style.cursor = 'pointer';
      if (btn.dataset._origHtml) {
        btn.innerHTML = btn.dataset._origHtml;
        delete btn.dataset._origHtml;
      }
    }
  }
}

// init preview state on first paint (jaga-jaga sebelum user interact)
document.addEventListener('DOMContentLoaded', () => {
  if (document.getElementById('taxRate')) {
    onTaxEnabledChange();
    // Mark sudah init — mulai sekarang setiap perubahan toggle akan auto-save.
    // Flag ini harus di-set SETELAH onTaxEnabledChange() pertama dipanggil supaya
    // render awal tidak men-trigger POST /app-settings yang tidak perlu.
    window._taxInited = true;
  }
});

// ══════════════════════════════════════════════════════════════
// AUTO-GENERATE INVOICE handler — auto-save langsung saat toggle
// ══════════════════════════════════════════════════════════════
async function onAutoGenChange() {
  const cb = document.getElementById('autoGenInvoice');
  const lbl = document.getElementById('autoGenLbl');
  if (!cb) return;
  const value = cb.checked ? '1' : '0';
  if (lbl) lbl.textContent = cb.checked ? 'Aktif' : 'Nonaktif';
  try {
    const d = await App.api('/app-settings', { method:'POST', body: JSON.stringify({ auto_generate_invoice: value }) });
    if (d?.success) {
      App.showToast(cb.checked ? 'Auto generate invoice diaktifkan' : 'Auto generate invoice dinonaktifkan', 'success');
    } else {
      App.showToast(d?.message || 'Gagal menyimpan', 'error');
      // Rollback toggle
      cb.checked = !cb.checked;
      if (lbl) lbl.textContent = cb.checked ? 'Aktif' : 'Nonaktif';
    }
  } catch (e) {
    App.showToast('Error menyimpan', 'error');
    cb.checked = !cb.checked;
    if (lbl) lbl.textContent = cb.checked ? 'Aktif' : 'Nonaktif';
  }
}

// ══════════════════════════════════════════════════════════════
// PORTAL HERO IMAGE handlers
// ══════════════════════════════════════════════════════════════
let _portalHeroUrl = '';

function _renderPortalHeroPreview() {
  const box = document.getElementById('portalHeroPreview');
  if (!box) return;
  if (_portalHeroUrl) {
    const ov = parseFloat(document.getElementById('portalHeroOverlay')?.value || '0.55');
    box.style.border = 'none';
    box.innerHTML =
      '<img src="' + _portalHeroUrl + '" style="position:absolute;inset:0;width:100%;height:100%;object-fit:cover;">' +
      '<div style="position:absolute;inset:0;background:linear-gradient(155deg,rgba(11,30,79,' + Math.min(1, ov+0.3).toFixed(2) + ') 0%,rgba(30,64,175,' + ov.toFixed(2) + ') 45%,rgba(37,99,235,' + Math.max(0, ov-0.15).toFixed(2) + ') 100%);"></div>' +
      '<div style="position:absolute;inset:0;display:flex;flex-direction:column;align-items:center;justify-content:center;color:#fff;text-align:center;padding:10px;">' +
        '<div style="font-size:20px;font-weight:900;letter-spacing:-.3px;text-shadow:0 2px 8px rgba(0,0,0,.3);">Customer Portal</div>' +
        '<div style="font-size:11px;font-weight:500;opacity:.85;margin-top:4px;">Akses layanan internet Anda</div>' +
      '</div>';
  } else {
    box.style.border = '1.5px dashed var(--d-border)';
    box.innerHTML =
      '<div style="text-align:center;color:var(--d-muted);font-size:12.5px;padding:20px">' +
        '<svg width="28" height="28" fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24" style="opacity:.4;margin-bottom:6px"><rect x="3" y="3" width="18" height="18" rx="2"/><circle cx="8.5" cy="8.5" r="1.5"/><polyline points="21 15 16 10 5 21"/></svg>' +
        '<div>Belum ada foto hero</div>' +
        '<div style="font-size:11px;margin-top:4px;opacity:.7;">Portal login akan pakai gradient biru default</div>' +
      '</div>';
  }
}

async function loadPortalHeroSettings() {
  try {
    const d = await App.api('/app-settings');
    if (!d?.success) return;
    const s = d.data || {};
    _portalHeroUrl = s.portal_hero_image || '';
    const overlayEl = document.getElementById('portalHeroOverlay');
    if (overlayEl) {
      overlayEl.value = s.portal_hero_overlay || '0.55';
      document.getElementById('portalHeroOverlayVal').textContent = overlayEl.value;
      overlayEl.addEventListener('input', _renderPortalHeroPreview);
    }
    const tEl = document.getElementById('portalWelcomeTitle');
    const sEl = document.getElementById('portalWelcomeSub');
    if (tEl) tEl.value = s.portal_welcome_title || '';
    if (sEl) sEl.value = s.portal_welcome_sub || '';
    const termsEl = document.getElementById('portalTermsUrl');
    if (termsEl) termsEl.value = s.portal_terms_url || '';
    const termsLabelEl = document.getElementById('portalTermsLabel');
    if (termsLabelEl) termsLabelEl.value = s.portal_terms_label || '';
    _renderPortalHeroPreview();
  } catch(e) { console.error('loadPortalHeroSettings:', e); }
}

async function onPortalHeroPicked(ev) {
  const file = ev.target?.files?.[0];
  if (!file) return;
  if (file.size > 4 * 1024 * 1024) {
    App.showToast('Ukuran file max 4MB', 'error');
    ev.target.value = '';
    return;
  }
  const fd = new FormData();
  fd.append('hero', file);
  App.showToast('Mengupload...', 'info');
  try {
    const token = (window.App && App.token) || localStorage.getItem('token') || '';
    const r = await fetch('/api/upload/portal-hero', {
      method: 'POST',
      headers: token ? { 'Authorization': 'Bearer ' + token } : {},
      body: fd
    });
    const j = await r.json();
    if (!j.success) {
      App.showToast(j.message || 'Upload gagal', 'error');
      return;
    }
    _portalHeroUrl = j.url;
    _renderPortalHeroPreview();
    App.showToast('Foto hero berhasil diupload', 'success');
  } catch(e) {
    App.showToast('Upload error: ' + e.message, 'error');
  } finally {
    ev.target.value = '';
  }
}

async function deletePortalHero() {
  if (!_portalHeroUrl) { App.showToast('Tidak ada foto untuk dihapus', 'info'); return; }
  if (!confirm('Hapus foto hero portal? Portal login akan kembali pakai gradient biru default.')) return;
  try {
    const token = (window.App && App.token) || localStorage.getItem('token') || '';
    const r = await fetch('/api/upload/portal-hero', {
      method: 'DELETE',
      headers: token ? { 'Authorization': 'Bearer ' + token } : {}
    });
    const j = await r.json();
    if (j?.success) {
      _portalHeroUrl = '';
      _renderPortalHeroPreview();
      App.showToast('Foto hero dihapus', 'success');
    } else App.showToast(j?.message || 'Gagal', 'error');
  } catch(e) { App.showToast('Error: ' + e.message, 'error'); }
}

// ══════════════════════════════════════════════════════════════
// BACKGROUND HALAMAN PENDAFTARAN handlers
// ══════════════════════════════════════════════════════════════
function renderRegisterBgPreview() {
  const box = document.getElementById('registerBgPreview');
  if (!box) return;
  const url = document.getElementById('registerBgUrl')?.value || '';
  const ov = parseFloat(document.getElementById('registerBgOverlay')?.value || '0.82');
  if (url) {
    box.style.border = 'none';
    box.innerHTML =
      '<img src="' + url + '" style="position:absolute;inset:0;width:100%;height:100%;object-fit:cover;">' +
      '<div style="position:absolute;inset:0;background:rgba(255,255,255,' + ov.toFixed(2) + ');"></div>' +
      '<div style="position:absolute;inset:0;display:flex;align-items:center;justify-content:center;">' +
        '<div style="background:#fff;border-radius:12px;padding:14px 22px;box-shadow:0 8px 24px rgba(0,0,0,.12);font-size:12.5px;font-weight:700;color:#1e293b;">Form Pendaftaran</div>' +
      '</div>';
  } else {
    box.style.border = '1.5px dashed var(--d-border)';
    box.innerHTML =
      '<div style="text-align:center;color:var(--d-muted);font-size:12.5px;padding:20px">' +
        '<svg width="28" height="28" fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24" style="opacity:.4;margin-bottom:6px"><rect x="3" y="3" width="18" height="18" rx="2"/><circle cx="8.5" cy="8.5" r="1.5"/><polyline points="21 15 16 10 5 21"/></svg>' +
        '<div>Belum ada foto background</div>' +
        '<div style="font-size:11px;margin-top:4px;opacity:.7;">Halaman pendaftaran pakai pola titik default</div>' +
      '</div>';
  }
}

async function onRegisterBgPicked(ev) {
  const file = ev.target?.files?.[0];
  if (!file) return;
  if (file.size > 5 * 1024 * 1024) { App.showToast('Ukuran file max 5MB', 'error'); ev.target.value=''; return; }
  const fd = new FormData();
  fd.append('bg', file);
  App.showToast('Mengupload...', 'info');
  try {
    const token = (window.App && App.token) || localStorage.getItem('token') || '';
    const r = await fetch('/api/upload/register-bg', {
      method: 'POST',
      headers: token ? { 'Authorization': 'Bearer ' + token } : {},
      body: fd
    });
    const j = await r.json();
    if (!j.success) { App.showToast(j.message || 'Upload gagal', 'error'); return; }
    document.getElementById('registerBgUrl').value = j.url;
    renderRegisterBgPreview();
    App.showToast('Background berhasil diupload', 'success');
  } catch(e) {
    App.showToast('Upload error: ' + e.message, 'error');
  } finally { ev.target.value = ''; }
}

async function deleteRegisterBg() {
  const url = document.getElementById('registerBgUrl')?.value || '';
  if (!url) { App.showToast('Tidak ada background untuk dihapus', 'info'); return; }
  if (!confirm('Hapus background halaman pendaftaran? Akan kembali ke pola titik default.')) return;
  try {
    const token = (window.App && App.token) || localStorage.getItem('token') || '';
    const r = await fetch('/api/upload/register-bg', {
      method: 'DELETE',
      headers: token ? { 'Authorization': 'Bearer ' + token } : {}
    });
    const j = await r.json();
    if (j?.success) {
      document.getElementById('registerBgUrl').value = '';
      renderRegisterBgPreview();
      App.showToast('Background dihapus', 'success');
    } else App.showToast(j?.message || 'Gagal', 'error');
  } catch(e) { App.showToast('Error: ' + e.message, 'error'); }
}

async function savePortalHeroSettings() {
  const data = {
    portal_hero_overlay:  document.getElementById('portalHeroOverlay')?.value || '0.55',
    portal_welcome_title: document.getElementById('portalWelcomeTitle')?.value || '',
    portal_welcome_sub:   document.getElementById('portalWelcomeSub')?.value || '',
    portal_terms_url:     document.getElementById('portalTermsUrl')?.value?.trim() || '',
    portal_terms_label:   document.getElementById('portalTermsLabel')?.value?.trim() || '',
  };
  const d = await App.api('/app-settings', { method:'POST', body: JSON.stringify(data) });
  if (d?.success) App.showToast('Pengaturan portal disimpan', 'success');
  else App.showToast(d?.message || 'Gagal menyimpan', 'error');
}

// Auto-load saat masuk tab brand
(function(){
  const origShow = window.showSection;
  if (typeof origShow === 'function') {
    window.showSection = function(id, el) {
      origShow(id, el);
      if (id === 'brand') setTimeout(() => { loadPortalHeroSettings(); renderRegisterBgPreview(); }, 50);
      if (id === 'telegram') setTimeout(() => { loadTelegramSettings(); }, 50);
    };
  }
  // Juga load saat halaman pertama kali (sec-brand default active)
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', () => { loadPortalHeroSettings(); renderRegisterBgPreview(); });
  } else {
    setTimeout(() => { loadPortalHeroSettings(); renderRegisterBgPreview(); }, 100);
  }
})();

async function saveGeneralSettings() {
  const data = {
    company_name:      document.getElementById('companyName')?.value,
    company_whatsapp:  (document.getElementById('companyWhatsapp')?.value || '').replace(/[^0-9]/g,''),
    company_address:   document.getElementById('companyAddress')?.value,
    company_website:   document.getElementById('companyWebsite')?.value,
    snmp_community:    document.getElementById('snmpCommunity')?.value,
    poll_interval:     document.getElementById('pollInterval')?.value,
  };
  const d = await App.api('/app-settings', { method:'POST', body: JSON.stringify(data) });
  if (d?.success) App.showToast('Pengaturan disimpan', 'success');
  else App.showToast(d?.message || 'Gagal', 'error');
}

// ══════════════════════════════════════════════════════════════
// PAYMENT GATEWAY — Midtrans / Xendit / Duitku / Tripay configuration
// ══════════════════════════════════════════════════════════════
var GW_CURRENT_PROVIDER = 'midtrans';

function selectGwProvider(provider) {
  GW_CURRENT_PROVIDER = provider;

  // Toggle radio + selected card class untuk semua provider
  ['midtrans','xendit','duitku','tripay'].forEach(function(p){
    var capName = p.charAt(0).toUpperCase() + p.slice(1);
    var radio = document.getElementById('gwProvRadio' + capName);
    var card  = document.getElementById('gwProvCard-' + p);
    if (radio) radio.checked = (provider === p);
    if (card)  card.classList.toggle('selected', provider === p);
  });

  // Toggle field visibility per-provider
  document.getElementById('gwClientKeyWrap').style.display      = (provider === 'midtrans') ? '' : 'none';
  document.getElementById('gwCallbackTokenWrap').style.display  = (provider === 'xendit')   ? '' : 'none';
  var mcWrap = document.getElementById('gwMerchantCodeWrap');
  // Merchant Code dipakai oleh Duitku & Tripay (label & hint berbeda)
  if (mcWrap) mcWrap.style.display = (provider === 'duitku' || provider === 'tripay') ? '' : 'none';
  var pkWrap = document.getElementById('gwPrivateKeyWrap');
  if (pkWrap) pkWrap.style.display = (provider === 'tripay') ? '' : 'none';

  // Toggle setup guide per-provider
  document.getElementById('setupGuideMidtrans').style.display   = (provider === 'midtrans') ? '' : 'none';
  document.getElementById('setupGuideXendit').style.display     = (provider === 'xendit')   ? '' : 'none';
  var dukGuide = document.getElementById('setupGuideDuitku');
  if (dukGuide) dukGuide.style.display = (provider === 'duitku') ? '' : 'none';
  var trpGuide = document.getElementById('setupGuideTripay');
  if (trpGuide) trpGuide.style.display = (provider === 'tripay') ? '' : 'none';

  // Update Merchant Code label & hint per-provider (Duitku vs Tripay)
  var mcLabel = document.getElementById('gwMerchantCodeLabel');
  var mcHint  = document.getElementById('gwMerchantCodeHint');
  var mcInput = document.getElementById('gwMerchantCode');
  if (provider === 'duitku') {
    if (mcLabel) mcLabel.textContent = 'Merchant Code';
    if (mcHint)  mcHint.innerHTML    = 'Project Code dari Duitku. Lihat di Project Setting → Project Code.';
    if (mcInput) mcInput.placeholder = 'DXXXXX (production) atau DSXXXXX (sandbox)';
  } else if (provider === 'tripay') {
    if (mcLabel) mcLabel.textContent = 'Merchant Code';
    if (mcHint)  mcHint.innerHTML    = 'Kode Merchant dari Tripay. Lihat di Merchant → Detail Merchant → <b>Kode Merchant</b> (mulai "T", contoh T12345).';
    if (mcInput) mcInput.placeholder = 'T12345';
  }

  // Update key label, hint, & placeholder per-provider
  if (provider === 'midtrans') {
    document.getElementById('gwKeyLabel').textContent = 'Server Key';
    document.getElementById('gwKeyHint').innerHTML = 'Ambil dari dashboard Midtrans → Settings → Access Keys';
    document.getElementById('gwServerKey').placeholder = 'SB-Mid-server-XXXXXXXXX (sandbox) atau Mid-server-XXXX (prod)';
  } else if (provider === 'xendit') {
    document.getElementById('gwKeyLabel').textContent = 'Secret API Key';
    document.getElementById('gwKeyHint').innerHTML = 'Ambil dari dashboard Xendit → Settings → Developers → API Keys (perlu permission Money-in)';
    document.getElementById('gwServerKey').placeholder = 'xnd_development_XXXXXXXXX atau xnd_production_XXXXXXXXX';
  } else if (provider === 'duitku') {
    document.getElementById('gwKeyLabel').textContent = 'API Key';
    document.getElementById('gwKeyHint').innerHTML = 'Ambil dari dashboard Duitku → Project → Project Setting → API Key (32 karakter hex)';
    document.getElementById('gwServerKey').placeholder = 'XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX (32 char hex)';
  } else if (provider === 'tripay') {
    document.getElementById('gwKeyLabel').textContent = 'API Key';
    document.getElementById('gwKeyHint').innerHTML = 'Ambil dari dashboard Tripay → Merchant → Detail Merchant → <b>API Key</b>';
    document.getElementById('gwServerKey').placeholder = 'Masukkan API Key dari Tripay';
  }

  updateWebhookUrl();
}

function updateWebhookUrl() {
  var base = window.location.origin;
  var url  = base + '/portal/webhook/' + GW_CURRENT_PROVIDER;
  document.getElementById('webhookUrlText').textContent = url;
}

function copyWebhookUrl() {
  var url = document.getElementById('webhookUrlText').textContent;
  if (navigator.clipboard) {
    navigator.clipboard.writeText(url).then(function(){
      App.showToast('Webhook URL disalin', 'success');
    });
  } else {
    var ta = document.createElement('textarea');
    ta.value = url; document.body.appendChild(ta);
    ta.select(); document.execCommand('copy'); document.body.removeChild(ta);
    App.showToast('Webhook URL disalin', 'success');
  }
}

function updateGwBadge(enabled, env) {
  var b = document.getElementById('gwStatusBadge');
  if (!b) return;
  if (!enabled) {
    b.style.background = '#f1f5f9'; b.style.color = '#64748b';
    b.textContent = 'NONAKTIF';
  } else if (env === 'production') {
    b.style.background = '#d1fae5'; b.style.color = '#047857';
    b.textContent = 'AKTIF · LIVE';
  } else {
    b.style.background = '#fef3c7'; b.style.color = '#b45309';
    b.textContent = 'AKTIF · SANDBOX';
  }
}

async function loadGatewaySettings() {
  try {
    const d = await App.api('/app-settings');
    if (!d?.success) return;
    const s = d.data || {};

    document.getElementById('gwEnabled').checked       = (s.payment_gateway_enabled === 'true' || s.payment_gateway_enabled === '1');
    document.getElementById('gwEnv').value             = s.payment_gateway_env || 'sandbox';
    document.getElementById('gwServerKey').value       = s.payment_gateway_server_key || '';
    document.getElementById('gwClientKey').value       = s.payment_gateway_client_key || '';
    document.getElementById('gwCallbackToken').value   = s.payment_gateway_callback_token || '';
    var mc = document.getElementById('gwMerchantCode');
    if (mc) mc.value = s.payment_gateway_merchant_code || '';
    var pk = document.getElementById('gwPrivateKey');
    if (pk) pk.value = s.payment_gateway_private_key || '';

    selectGwProvider(s.payment_gateway_provider || 'midtrans');
    updateGwBadge(
      document.getElementById('gwEnabled').checked,
      s.payment_gateway_env || 'sandbox'
    );
  } catch (e) {
    console.error('loadGatewaySettings error:', e);
  }
}

async function saveGatewaySettings() {
  const enabled  = document.getElementById('gwEnabled').checked;
  const provider = GW_CURRENT_PROVIDER;
  const env      = document.getElementById('gwEnv').value;
  const srvKey   = (document.getElementById('gwServerKey').value || '').trim();
  const cliKey   = (document.getElementById('gwClientKey').value || '').trim();
  const cbToken  = (document.getElementById('gwCallbackToken').value || '').trim();
  const mcEl     = document.getElementById('gwMerchantCode');
  const merchant = mcEl ? (mcEl.value || '').trim() : '';
  const pkEl     = document.getElementById('gwPrivateKey');
  const privKey  = pkEl ? (pkEl.value || '').trim() : '';

  // Validasi minimal kalau gateway diaktifkan
  if (enabled) {
    if (!srvKey) {
      var keyLabel = (provider === 'midtrans') ? 'Server Key'
                   : (provider === 'duitku')   ? 'API Key'
                   : (provider === 'tripay')   ? 'API Key'
                   : 'Secret API Key';
      App.showToast(keyLabel + ' wajib diisi', 'error');
      return;
    }
    if (provider === 'midtrans' && !cliKey) {
      App.showToast('Client Key wajib diisi untuk Midtrans', 'error');
      return;
    }
    if (provider === 'xendit' && !cbToken) {
      App.showToast('Callback Token wajib diisi untuk Xendit (untuk verifikasi webhook)', 'error');
      return;
    }
    if (provider === 'duitku' && !merchant) {
      App.showToast('Merchant Code wajib diisi untuk Duitku', 'error');
      return;
    }
    if (provider === 'duitku' && !/^[A-Z0-9]{3,15}$/i.test(merchant)) {
      App.showToast('Merchant Code Duitku harus alfanumerik 3-15 karakter (contoh: DXXXXX)', 'error');
      return;
    }
    if (provider === 'tripay' && !merchant) {
      App.showToast('Merchant Code wajib diisi untuk Tripay', 'error');
      return;
    }
    if (provider === 'tripay' && !/^[A-Z0-9]{3,20}$/i.test(merchant)) {
      App.showToast('Merchant Code Tripay harus alfanumerik 3-20 karakter (contoh: T12345)', 'error');
      return;
    }
    if (provider === 'tripay' && !privKey) {
      App.showToast('Private Key wajib diisi untuk Tripay (untuk signature & callback)', 'error');
      return;
    }
    if (provider === 'tripay' && privKey.length < 16) {
      App.showToast('Private Key Tripay terlalu pendek (minimum 16 karakter)', 'error');
      return;
    }
  }

  const data = {
    payment_gateway_enabled:        enabled ? 'true' : 'false',
    payment_gateway_provider:       provider,
    payment_gateway_env:            env,
    payment_gateway_server_key:     srvKey,
    payment_gateway_client_key:     cliKey,
    payment_gateway_callback_token: cbToken,
    payment_gateway_merchant_code:  merchant,
    payment_gateway_private_key:    privKey,
  };

  const d = await App.api('/app-settings', { method:'POST', body: JSON.stringify(data) });
  if (d?.success) {
    App.showToast('Pengaturan payment gateway disimpan', 'success');
    updateGwBadge(enabled, env);
  } else {
    App.showToast(d?.message || 'Gagal menyimpan', 'error');
  }
}

async function testGatewayConfig() {
  const enabled = document.getElementById('gwEnabled').checked;
  const srvKey  = (document.getElementById('gwServerKey').value || '').trim();
  const provider = GW_CURRENT_PROVIDER;
  const pkEl    = document.getElementById('gwPrivateKey');
  const privKey = pkEl ? (pkEl.value || '').trim() : '';
  const mcEl    = document.getElementById('gwMerchantCode');
  const merchant = mcEl ? (mcEl.value || '').trim() : '';

  if (!enabled) {
    App.showToast('Aktifkan dulu payment gateway, lalu simpan, baru test', 'error');
    return;
  }
  if (!srvKey) {
    App.showToast('Isi Server Key/API Key dulu, lalu klik Simpan sebelum test', 'error');
    return;
  }
  if ((provider === 'duitku' || provider === 'tripay') && !merchant) {
    App.showToast('Isi Merchant Code dulu, lalu klik Simpan sebelum test', 'error');
    return;
  }
  if (provider === 'tripay' && !privKey) {
    App.showToast('Isi Private Key Tripay dulu, lalu klik Simpan sebelum test', 'error');
    return;
  }

  // Panggil endpoint test (validasi format + auth ke server provider)
  App.showToast('Sedang verifikasi…', 'info');
  try {
    const d = await App.api('/app-settings/payment-gateway/test', { method: 'POST' });
    _showGatewayTestResult(d);
  } catch (e) {
    App.showToast('Gagal panggil endpoint test: ' + e.message, 'error');
  }
}

function _showGatewayTestResult(d) {
  // Buat modal sederhana untuk tampilkan hasil check
  let modal = document.getElementById('gwTestResultModal');
  if (!modal) {
    modal = document.createElement('div');
    modal.id = 'gwTestResultModal';
    modal.style.cssText = 'display:none;position:fixed;inset:0;background:rgba(0,0,0,.5);z-index:1100;align-items:center;justify-content:center;padding:20px;';
    modal.innerHTML = '<div style="background:#fff;border-radius:16px;max-width:520px;width:100%;max-height:85vh;overflow-y:auto;">' +
      '<div style="padding:18px 22px;border-bottom:1px solid var(--d-border);display:flex;align-items:center;justify-content:space-between;">' +
        '<div id="gwTestTitle" style="font-size:15px;font-weight:700;color:var(--d-text);">Hasil Test Konfigurasi</div>' +
        '<button onclick="document.getElementById(\'gwTestResultModal\').style.display=\'none\'" style="background:none;border:none;cursor:pointer;color:var(--d-muted);padding:4px;">' +
          '<svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>' +
        '</button>' +
      '</div>' +
      '<div id="gwTestBody" style="padding:18px 22px;"></div>' +
    '</div>';
    document.body.appendChild(modal);
  }

  const success = !!d?.success;
  const data = d?.data || {};
  const checks = data.checks || [];

  const headerColor = success ? '#16a34a' : '#dc2626';
  const statusIcon = success
    ? '<svg width="32" height="32" fill="none" stroke="#16a34a" stroke-width="2.5" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>'
    : '<svg width="32" height="32" fill="none" stroke="#dc2626" stroke-width="2.5" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"/></svg>';

  let html = '<div style="display:flex;align-items:center;gap:12px;margin-bottom:14px;padding:14px;background:' + (success ? '#f0fdf4' : '#fef2f2') + ';border-radius:10px;">' +
    statusIcon +
    '<div><div style="font-weight:700;color:' + headerColor + ';font-size:14px;">' + (d?.message || (success ? 'OK' : 'Ada masalah')) + '</div>' +
    '<div style="font-size:11.5px;color:var(--d-muted);margin-top:2px;">Provider: <b>' + (data.provider || '-') + '</b> · Env: <b>' + (data.env || '-') + '</b></div></div></div>';

  if (checks.length) {
    html += '<div style="font-size:12px;font-weight:700;color:var(--d-muted);margin-bottom:8px;letter-spacing:.5px;text-transform:uppercase;">Detail Pemeriksaan</div>';
    checks.forEach(c => {
      const ico = c.pass
        ? '<svg width="16" height="16" fill="none" stroke="#16a34a" stroke-width="3" viewBox="0 0 24 24"><polyline points="20 6 9 17 4 12"/></svg>'
        : '<svg width="16" height="16" fill="none" stroke="#dc2626" stroke-width="3" viewBox="0 0 24 24"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>';
      html += '<div style="display:flex;align-items:flex-start;gap:10px;padding:10px 12px;border-radius:8px;margin-bottom:6px;background:' + (c.pass ? '#f0fdf4' : '#fef2f2') + ';">' +
        '<div style="flex-shrink:0;margin-top:1px;">' + ico + '</div>' +
        '<div style="flex:1;min-width:0;"><div style="font-size:13px;font-weight:600;color:var(--d-text);">' + _esc(c.label) + '</div>' +
        '<div style="font-size:11.5px;color:var(--d-muted);margin-top:2px;word-break:break-all;">' + _esc(c.detail || '') + '</div></div>' +
      '</div>';
    });
  }

  if (!success) {
    html += '<div style="margin-top:14px;padding:12px 14px;background:#fffbeb;border:1px solid #fde68a;border-radius:10px;font-size:12px;color:#78350f;line-height:1.6;">' +
      '<b>Tips:</b><br>' +
      '• Cek key di dashboard provider, copy ulang tanpa spasi<br>' +
      '• Pastikan environment (Sandbox/Production) cocok dengan prefix key<br>' +
      '• Jangan lupa klik <b>Simpan Pengaturan</b> dulu sebelum test' +
    '</div>';
  }

  document.getElementById('gwTestBody').innerHTML = html;
  document.getElementById('gwTestTitle').textContent = success ? '✓ Konfigurasi OK' : '⚠ Ada Masalah';
  modal.style.display = 'flex';
}

function _esc(s) {
  return String(s == null ? '' : s).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;');
}

// ══════════════════════════════════════════════════════════════
// PAYMENT ACCOUNTS — CRUD rekening untuk portal customer
// ══════════════════════════════════════════════════════════════
var PA_BANK_OPTIONS = [
  { v:'bca',      l:'BCA' },
  { v:'bni',      l:'BNI' },
  { v:'bri',      l:'BRI' },
  { v:'mandiri',  l:'Mandiri' },
  { v:'seabank',  l:'SeaBank' },
  { v:'permata',  l:'Permata' },
  { v:'cimb',     l:'CIMB Niaga' },
  { v:'bsi',      l:'BSI' },
  { v:'btpn',     l:'BTPN Jenius' },
  { v:'danamon',  l:'Danamon' }
];
var PA_EWALLET_OPTIONS = [
  { v:'dana',      l:'DANA' },
  { v:'ovo',       l:'OVO' },
  { v:'gopay',     l:'GoPay' },
  { v:'shopeepay', l:'ShopeePay' },
  { v:'linkaja',   l:'LinkAja' }
];

function escapeHtmlSt(s){ return String(s==null?'':s).replace(/[&<>"']/g, function(c){ return {'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]; }); }

async function loadPayAccounts() {
  const container = document.getElementById('payAcctList');
  try {
    const d = await App.api('/payment-accounts');
    const list = (d && d.data) || [];
    if (!list.length) {
      container.innerHTML = '<div style="padding:28px;text-align:center;color:var(--d-muted);font-size:13px">Belum ada metode pembayaran. Klik <b>Tambah</b> untuk menambahkan.</div>';
      window._PA_CACHE = [];
      return;
    }
    const typeLabels = { bank:'Bank', ewallet:'E-Wallet', qris:'QRIS' };
    const typeColors = { bank:'#2563eb', ewallet:'#8b5cf6', qris:'#f59e0b' };
    container.innerHTML = list.map(function(it, idx) {
      const active = it.is_active !== false;
      const label = (it.provider || '').toUpperCase();
      const badge = typeLabels[it.type] || it.type;
      const color = typeColors[it.type] || '#64748b';
      return '<div style="display:flex;align-items:center;gap:12px;padding:12px 14px;border:1px solid var(--d-border);border-radius:12px;margin-bottom:8px;background:#fff">' +
        '<div style="width:36px;height:36px;border-radius:9px;background:'+color+'22;color:'+color+';display:flex;align-items:center;justify-content:center;font-size:10px;font-weight:800;flex-shrink:0">'+(it.type==='qris'?'QR':label.substring(0,3))+'</div>' +
        '<div style="flex:1;min-width:0">' +
          '<div style="font-size:13px;font-weight:700;color:var(--d-text);display:flex;align-items:center;gap:8px;flex-wrap:wrap">' +
            '<span>'+escapeHtmlSt(label||'—')+'</span>' +
            '<span style="padding:1px 7px;background:'+color+'15;color:'+color+';border-radius:20px;font-size:9.5px;font-weight:800;text-transform:uppercase;letter-spacing:.4px">'+badge+'</span>' +
            (active ? '' : '<span style="padding:1px 7px;background:#f1f5f9;color:#64748b;border-radius:20px;font-size:9.5px;font-weight:700">Nonaktif</span>') +
          '</div>' +
          '<div style="font-size:12px;color:var(--d-muted);margin-top:3px;font-family:\'DM Mono\',monospace">'+escapeHtmlSt(it.account_number||'—')+'</div>' +
          (it.account_owner ? '<div style="font-size:11px;color:var(--d-muted);margin-top:1px">a.n. '+escapeHtmlSt(it.account_owner)+'</div>' : '') +
        '</div>' +
        '<button onclick="editPayAcct('+idx+')" style="padding:7px 10px;border-radius:8px;background:#eff6ff;color:var(--d-blue1);border:1px solid #bfdbfe;font-size:11px;font-weight:700;cursor:pointer;font-family:inherit">Edit</button>' +
        '<button onclick="deletePayAcct('+idx+')" style="padding:7px 10px;border-radius:8px;background:#fef2f2;color:#dc2626;border:1px solid #fecaca;font-size:11px;font-weight:700;cursor:pointer;font-family:inherit">Hapus</button>' +
      '</div>';
    }).join('');
    window._PA_CACHE = list;
  } catch(e) {
    container.innerHTML = '<div style="padding:24px;text-align:center;color:#dc2626;font-size:12px">Gagal memuat: '+e.message+'</div>';
  }
}

function openPayAcctModal(preset) {
  document.getElementById('payAcctModalTitle').textContent = preset ? 'Edit Metode Pembayaran' : 'Tambah Metode Pembayaran';
  document.getElementById('paId').value       = preset ? preset._idx : '';
  document.getElementById('paType').value     = preset ? (preset.type||'bank') : 'bank';
  onPaTypeChange();
  if (preset && preset.provider) {
    const providerSel = document.getElementById('paProvider');
    let found = false;
    for (let i = 0; i < providerSel.options.length; i++) {
      if (providerSel.options[i].value === preset.provider) { found = true; break; }
    }
    if (!found && preset.type !== 'qris') {
      const opt = document.createElement('option');
      opt.value = preset.provider; opt.textContent = preset.provider.toUpperCase();
      providerSel.appendChild(opt);
    }
    providerSel.value = preset.provider;
  }
  document.getElementById('paNumber').value   = preset ? (preset.account_number||'') : '';
  document.getElementById('paOwner').value    = preset ? (preset.account_owner||'') : '';
  document.getElementById('paActive').checked = preset ? (preset.is_active !== false) : true;

  // Logo: show preview if preset has logo_url
  const logoUrl = preset ? (preset.logo_url||'') : '';
  document.getElementById('paLogoUrl').value = logoUrl;
  if (logoUrl) {
    document.getElementById('paLogoPreviewImg').src = logoUrl;
    document.getElementById('paLogoFileName').textContent = logoUrl.split('/').pop();
    document.getElementById('paLogoPreview').style.display = 'block';
    document.getElementById('paLogoDropText').style.display = 'none';
  } else {
    clearPayLogoPreview();
  }
  document.getElementById('paLogoFile').value = ''; // reset file input

  document.getElementById('payAcctModal').style.display = 'flex';
}

function clearPayLogoPreview() {
  document.getElementById('paLogoPreview').style.display = 'none';
  document.getElementById('paLogoDropText').style.display = 'block';
  document.getElementById('paLogoPreviewImg').src = '';
  document.getElementById('paLogoFileName').textContent = '—';
}

function clearPayLogo(e) {
  if (e) { e.stopPropagation(); e.preventDefault(); }
  document.getElementById('paLogoUrl').value = '';
  document.getElementById('paLogoFile').value = '';
  clearPayLogoPreview();
}

async function handlePayLogoFile(file) {
  if (!file) return;

  // Validasi client-side
  const allowed = ['image/png','image/jpeg','image/svg+xml','image/webp'];
  if (!allowed.includes(file.type)) {
    App.showToast('Format tidak didukung. Gunakan PNG/JPG/SVG/WebP', 'error');
    document.getElementById('paLogoFile').value = '';
    return;
  }
  if (file.size > 2 * 1024 * 1024) {
    App.showToast('File terlalu besar (max 2MB)', 'error');
    document.getElementById('paLogoFile').value = '';
    return;
  }

  // Tampilkan preview segera (pakai FileReader) supaya responsive
  const reader = new FileReader();
  reader.onload = (e) => {
    document.getElementById('paLogoPreviewImg').src = e.target.result;
    document.getElementById('paLogoFileName').textContent = file.name + ' · uploading...';
    document.getElementById('paLogoPreview').style.display = 'block';
    document.getElementById('paLogoDropText').style.display = 'none';
  };
  reader.readAsDataURL(file);

  // Upload ke server
  try {
    const fd = new FormData();
    fd.append('logo', file);

    // Auth pakai App.token (konsisten dengan App.api pattern)
    const authHeaders = {};
    if (App && App.token && App.token !== 'null') {
      authHeaders['Authorization'] = 'Bearer ' + App.token;
    }

    const res = await fetch('/api/upload/payment-logo', {
      method: 'POST',
      headers: authHeaders,
      credentials: 'include',
      body: fd
    });
    const data = await res.json();

    if (data.success && data.url) {
      document.getElementById('paLogoUrl').value = data.url;
      document.getElementById('paLogoFileName').textContent = file.name;
      App.showToast('Logo terupload', 'success');
    } else {
      App.showToast(data.message || 'Gagal upload', 'error');
      clearPayLogo();
    }
  } catch (err) {
    App.showToast('Gagal upload: ' + err.message, 'error');
    clearPayLogo();
  }
}
function closePayAcctModal(){ document.getElementById('payAcctModal').style.display = 'none'; }

function onPaTypeChange() {
  const type = document.getElementById('paType').value;
  const providerSel   = document.getElementById('paProvider');
  const providerWrap  = document.getElementById('paProviderWrap');
  const providerLabel = document.getElementById('paProviderLabel');
  const numberWrap    = document.getElementById('paNumberWrap');
  const numberLabel   = document.getElementById('paNumberLabel');
  const logoLabel     = document.getElementById('paLogoLabel');
  const logoHint      = document.getElementById('paLogoHint');

  if (type === 'bank') {
    providerWrap.style.display = '';
    providerLabel.textContent  = 'Bank';
    providerSel.innerHTML = PA_BANK_OPTIONS.map(o => '<option value="'+o.v+'">'+o.l+'</option>').join('');
    numberWrap.style.display  = '';
    numberLabel.textContent   = 'Nomor Rekening';
    logoLabel.textContent     = 'Logo Bank (Opsional)';
    logoHint.innerHTML        = 'Opsional. Kalau kosong, akan tampil singkatan nama bank (mis. "BCA").';
  } else if (type === 'ewallet') {
    providerWrap.style.display = '';
    providerLabel.textContent  = 'E-Wallet';
    providerSel.innerHTML = PA_EWALLET_OPTIONS.map(o => '<option value="'+o.v+'">'+o.l+'</option>').join('');
    numberWrap.style.display  = '';
    numberLabel.textContent   = 'Nomor HP / ID E-Wallet';
    logoLabel.textContent     = 'Logo E-Wallet (Opsional)';
    logoHint.innerHTML        = 'Opsional. Kalau kosong, akan tampil nama e-wallet (mis. "DANA").';
  } else if (type === 'qris') {
    providerWrap.style.display = 'none';
    numberWrap.style.display  = 'none';
    logoLabel.textContent     = 'Gambar QR Code *';
    logoHint.innerHTML        = '<b>Wajib diisi.</b> Upload file gambar QR statis Anda (PNG/JPG).';
  }
}

function editPayAcct(idx) {
  const list = window._PA_CACHE || [];
  const item = list[idx];
  if (!item) return;
  item._idx = idx;
  openPayAcctModal(item);
}

async function deletePayAcct(idx) {
  if (!confirm('Hapus metode pembayaran ini?')) return;
  const list = (window._PA_CACHE || []).slice();
  list.splice(idx, 1);
  const d = await App.api('/payment-accounts', { method:'PUT', body: JSON.stringify({ accounts: list }) });
  if (d?.success) { App.showToast('Dihapus', 'success'); loadPayAccounts(); }
  else App.showToast(d?.message || 'Gagal menghapus', 'error');
}

async function savePayAcct() {
  const type     = document.getElementById('paType').value;
  const provider = type === 'qris' ? 'qris' : document.getElementById('paProvider').value;
  const number   = document.getElementById('paNumber').value.trim();
  const owner    = document.getElementById('paOwner').value.trim();
  const logoUrl  = document.getElementById('paLogoUrl').value.trim();
  const active   = document.getElementById('paActive').checked;
  const editIdx  = document.getElementById('paId').value;

  if (type !== 'qris' && !number) return App.showToast('Nomor rekening wajib diisi', 'error');
  if (type !== 'qris' && !owner)  return App.showToast('Atas nama wajib diisi', 'error');
  if (type === 'qris' && !logoUrl) return App.showToast('URL gambar QR wajib diisi', 'error');

  const entry = {
    type: type,
    provider: provider,
    account_number: number,
    account_owner: owner,
    logo_url: logoUrl,
    is_active: active
  };

  const list = (window._PA_CACHE || []).slice();
  if (editIdx !== '' && editIdx !== undefined && editIdx !== null) {
    list[parseInt(editIdx)] = entry;
  } else {
    list.push(entry);
  }

  const d = await App.api('/payment-accounts', { method:'PUT', body: JSON.stringify({ accounts: list }) });
  if (d?.success) {
    App.showToast('Tersimpan', 'success');
    closePayAcctModal();
    loadPayAccounts();
  } else {
    App.showToast(d?.message || 'Gagal menyimpan', 'error');
  }
}

// Logo file handling
let _logoFile = null;

function handleLogoFile(file) {
  if (!file) return;
  _logoFile = file;
  // Show preview thumb
  const reader = new FileReader();
  reader.onload = (e) => {
    document.getElementById('logoThumbImg').src = e.target.result;
    document.getElementById('logoPreviewThumb').style.display = 'block';
    document.getElementById('logoDropText').style.display = 'none';
    document.getElementById('btnUploadLogo').style.display = 'inline-block';
    document.getElementById('logoDropArea').style.borderColor = '#1a6ef5';
      document.getElementById('logoDropArea').style.background = '#eff6ff';
    document.getElementById('logoDropArea').style.background = '#eff6ff';
    updatePreview(e.target.result); // preview pakai data URL dulu
  };
  reader.readAsDataURL(file);
}

function handleLogoDrop(e) {
  e.preventDefault();
  document.getElementById('logoDropArea').style.borderColor = '';
  const file = e.dataTransfer.files[0];
  if (file) handleLogoFile(file);
}

async function uploadLogo() {
  if (!_logoFile) return;
  const btn = document.getElementById('btnUploadLogo');
  btn.textContent = 'Uploading...'; btn.disabled = true;
  const form = new FormData();
  form.append('logo', _logoFile);
  try {
    const token = document.cookie.match(/token=([^;]+)/)?.[1] || localStorage.getItem('token') || '';
    const res = await fetch('/api/upload/logo', {
      method: 'POST',
      headers: token ? { 'Authorization': 'Bearer ' + token } : {},
      body: form
    });
    const d = await res.json();
    if (d.success) {
      // d.url sudah include cache-bust (?v=timestamp) dari server.
      document.getElementById('logoUrl').value = d.url;
      // Refresh thumbnail di settings dengan URL final (bukan dataURL preview).
      document.getElementById('logoThumbImg').src = d.url;
      // Refresh logo di sidebar tanpa reload page penuh.
      document.querySelectorAll('#sidebarBrand img').forEach(img => { img.src = d.url; });

      App.showToast('Logo berhasil diupload!', 'success');
      updatePreview();
      btn.textContent = '✓ Uploaded';
      btn.style.background = 'linear-gradient(135deg,#00c896,#00a07a)';
      // Reset state setelah 1.5 dtk supaya bisa upload ulang
      setTimeout(() => {
        btn.textContent = 'Upload Logo';
        btn.style.background = '';
        btn.disabled = false;
        _logoFile = null;
      }, 1500);
    } else {
      App.showToast(d.message || 'Upload gagal', 'error');
      btn.textContent = 'Upload'; btn.disabled = false;
    }
  } catch(err) {
    App.showToast('Error: ' + err.message, 'error');
    btn.textContent = 'Upload'; btn.disabled = false;
  }
}

// ── Favicon Handlers ──────────────────────────────────────────────
let _faviconFile = null;

function handleFaviconFile(file) {
  if (!file) return;
  // Validasi ukuran
  if (file.size > 1024 * 1024) {
    App.showToast('Ukuran favicon maksimal 1MB', 'error');
    return;
  }
  // Validasi format
  const allowed = ['image/x-icon', 'image/vnd.microsoft.icon', 'image/png', 'image/svg+xml', 'image/jpeg', 'image/jpg'];
  const allowedExt = ['.ico', '.png', '.svg', '.jpg', '.jpeg'];
  const ext = '.' + (file.name.split('.').pop() || '').toLowerCase();
  if (!allowed.includes(file.type) && !allowedExt.includes(ext)) {
    App.showToast('Format tidak didukung. Gunakan ICO/PNG/SVG/JPG', 'error');
    return;
  }
  _faviconFile = file;
  // Preview thumbnail
  const reader = new FileReader();
  reader.onload = e => {
    document.getElementById('faviconThumbImg').src = e.target.result;
    document.getElementById('faviconPreviewThumb').style.display = 'block';
    document.getElementById('faviconDropText').style.display = 'none';
    document.getElementById('btnUploadFavicon').style.display = 'inline-block';
    document.getElementById('btnUploadFavicon').textContent = 'Upload Favicon';
    document.getElementById('btnUploadFavicon').disabled = false;
  };
  reader.readAsDataURL(file);
}

function handleFaviconDrop(e) {
  e.preventDefault();
  e.currentTarget.style.borderColor = '';
  const file = e.dataTransfer.files[0];
  if (file) handleFaviconFile(file);
}

async function uploadFavicon() {
  if (!_faviconFile) return;
  const btn = document.getElementById('btnUploadFavicon');
  btn.textContent = 'Uploading...'; btn.disabled = true;
  const form = new FormData();
  form.append('favicon', _faviconFile);
  try {
    const token = document.cookie.match(/token=([^;]+)/)?.[1] || localStorage.getItem('token') || '';
    const res = await fetch('/api/upload/favicon', {
      method: 'POST',
      headers: token ? { 'Authorization': 'Bearer ' + token } : {},
      body: form
    });
    const d = await res.json();
    if (d.success) {
      App.showToast(d.message || 'Favicon berhasil diupload!', 'success');

      // Refresh thumbnail di settings dengan URL final (bukan dataURL preview).
      document.getElementById('faviconThumbImg').src = d.url;
      document.getElementById('faviconPreviewThumb').style.display = 'block';
      document.getElementById('faviconDropText').style.display = 'none';
      document.getElementById('btnResetFavicon').style.display = 'inline-block';

      btn.textContent = '✓ Uploaded';
      btn.style.background = 'linear-gradient(135deg,#00c896,#00a07a)';

      // Force browser reload favicon di tab.
      // Hapus semua <link rel*="icon"> lama dulu, baru inject yang baru.
      // Pakai URL favicon langsung (d.url sudah include ?v=cache-bust),
      // BUKAN /favicon.ico karena route /favicon.ico di-cache 1 jam oleh server.
      document.querySelectorAll("link[rel*='icon']").forEach(el => el.remove());
      const link = document.createElement('link');
      link.rel  = 'icon';
      link.href = d.url;
      // Set type berdasarkan ekstensi
      const ext = (d.url.split('?')[0].split('.').pop() || '').toLowerCase();
      const mimeMap = { ico:'image/x-icon', png:'image/png', svg:'image/svg+xml', jpg:'image/jpeg', jpeg:'image/jpeg' };
      if (mimeMap[ext]) link.type = mimeMap[ext];
      document.head.appendChild(link);

      // Reset state setelah 1.5 dtk supaya bisa upload ulang
      setTimeout(() => {
        btn.textContent = 'Upload Favicon';
        btn.style.background = '';
        btn.disabled = false;
        _faviconFile = null;
      }, 1500);
    } else {
      App.showToast(d.message || 'Upload gagal', 'error');
      btn.textContent = 'Upload Favicon'; btn.disabled = false;
    }
  } catch(err) {
    App.showToast('Error: ' + err.message, 'error');
    btn.textContent = 'Upload Favicon'; btn.disabled = false;
  }
}

async function resetFavicon() {
  if (!confirm('Reset favicon ke default? File yang sudah diupload akan dihapus.')) return;
  try {
    const token = document.cookie.match(/token=([^;]+)/)?.[1] || localStorage.getItem('token') || '';
    const res = await fetch('/api/upload/favicon', {
      method: 'DELETE',
      headers: token ? { 'Authorization': 'Bearer ' + token } : {}
    });
    const d = await res.json();
    if (d.success) {
      App.showToast(d.message || 'Favicon dikembalikan ke default', 'success');
      // Reset UI
      document.getElementById('faviconPreviewThumb').style.display = 'none';
      document.getElementById('faviconDropText').style.display = 'block';
      document.getElementById('btnResetFavicon').style.display = 'none';
      document.getElementById('btnUploadFavicon').style.display = 'none';
      _faviconFile = null;
      // Force browser reload favicon
      setTimeout(() => location.reload(), 800);
    } else {
      App.showToast(d.message || 'Gagal reset', 'error');
    }
  } catch(err) {
    App.showToast('Error: ' + err.message, 'error');
  }
}

function imgError(el) { el.style.display='none'; var n=el.nextSibling; if(n) n.style.display='block'; }

function updatePreview(dataUrl) {
  const mode    = document.getElementById('brandMode').value;
  const name    = document.getElementById('appName')?.value || 'Skynet';
  const tagline = document.getElementById('appTagline')?.value || '';
  const logo    = dataUrl || document.getElementById('logoUrl')?.value || '';
  const prev    = document.getElementById('brandPreview');

  // Sinkronkan 3 kartu Mode Tampilan supaya ikut nama/tagline yang sedang diketik
  updateBrandOptionsPreview(name, tagline);

  if (!prev) return;

  if (mode === 'logo_only' && logo) {
    prev.innerHTML = '<img src="' + logo + '" alt="logo" style="height:36px;object-fit:contain;" onerror="imgError(this)"><span style="display:none;font-size:12px;color:#dc2626;">&#10060; Logo tidak terbaca</span>';
  } else if (mode === 'name_logo' && logo) {
    prev.innerHTML = '<img src="' + logo + '" alt="logo" style="height:30px;object-fit:contain;flex-shrink:0;" onerror="imgError(this)"> <span style="font-size:14px;font-weight:800;color:#0d1b3e;">' + name + '</span>';
  } else {
    prev.innerHTML = '<div style="width:32px;height:32px;border-radius:9px;background:linear-gradient(135deg,#1a6ef5,#0047cc);display:flex;align-items:center;justify-content:center;flex-shrink:0;"><svg width=\"14\" height=\"14\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"white\" stroke-width=\"2\"><circle cx=\"12\" cy=\"12\" r=\"10\"/><path d=\"M12 6v6l4 2\"/></svg></div>' +
      '<div style="display:flex;flex-direction:column;line-height:1.2;">' +
        '<span style="font-size:15px;font-weight:800;color:#0d1b3e;">' + name + '</span>' +
        (mode === 'name_tagline' && tagline ? '<span style="font-size:9.5px;color:#94a3b8;">' + tagline + '</span>' : '') +
      '</div>';
  }
}

// Refresh teks di 3 kartu Mode Tampilan (Nama+Tagline / Logo+Nama / Logo Only)
// Dipanggil dari updatePreview() saat user mengetik di field nama atau tagline.
function updateBrandOptionsPreview(name, tagline) {
  const n = (name || '').trim() || 'Skynet';
  const t = (tagline || '').trim() || 'ISP Management';
  const n1 = document.getElementById('bopt1Name');
  const t1 = document.getElementById('bopt1Tag');
  const n2 = document.getElementById('bopt2Name');
  if (n1) n1.textContent = n;
  if (t1) t1.textContent = t;
  if (n2) n2.textContent = n;
}
// Init
document.addEventListener('DOMContentLoaded', () => {
  const mode = document.getElementById('brandMode')?.value || 'name_tagline';
  document.getElementById('taglineField').style.opacity = mode === 'name_tagline' ? '1' : '.4';
  document.getElementById('logoField').style.display    = mode === 'name_tagline' ? 'none' : 'block';
  // Show existing logo thumb if URL exists
  const existingLogo = document.getElementById('logoUrl')?.value;
  if (existingLogo) {
    const thumb = document.getElementById('logoThumbImg');
    const wrap  = document.getElementById('logoPreviewThumb');
    const text  = document.getElementById('logoDropText');
    if (thumb) { thumb.src = existingLogo; wrap.style.display = 'block'; text.style.display = 'none'; }
  }
  updatePreview();
});

// ── TELEGRAM MONITORING ───────────────────────────────────────
// Sisipkan placeholder ke textarea template yang terakhir aktif
let _tgLastTpl = null;
document.addEventListener('focusin', (e) => {
  if (e.target && e.target.id && e.target.id.startsWith('tg-tpl-')) _tgLastTpl = e.target;
});
function tgInsertPlaceholder(ph){
  const el = _tgLastTpl || document.getElementById('tg-tpl-device_down');
  if (!el) return;
  const s = el.selectionStart ?? el.value.length;
  const e = el.selectionEnd ?? el.value.length;
  el.value = el.value.slice(0, s) + ph + el.value.slice(e);
  const pos = s + ph.length;
  el.focus();
  try { el.setSelectionRange(pos, pos); } catch(_){}
  _tgLastTpl = el;
}

async function loadTelegramSettings() {
  try {
    const d = await App.api('/app-settings');
    if (!d?.success) return;
    const s = d.data || {};
    const set = (id, val) => { const e = document.getElementById(id); if (e) e.value = val; };
    const chk = (id, val) => { const e = document.getElementById(id); if (e) e.checked = String(val) === '1'; };
    chk('tg-enabled',       s.telegram_enabled);
    set('tg-bot-token',     s.telegram_bot_token || '');
    set('tg-chat-id',       s.telegram_chat_id || '');
    chk('tg-notif-device',  s.telegram_notif_device);
    chk('tg-notif-ont',     s.telegram_notif_ont);
    chk('tg-notif-ip',      s.telegram_notif_ip);
    chk('tg-notif-pppoe',   s.telegram_notif_pppoe);
    set('tg-interval',      s.telegram_interval || '3');
    // Template pesan + ambil default untuk placeholder
    const tplKeys = ['device_down','device_recover','ont_down','ont_recover','ip_down','ip_recover','pppoe_down','pppoe_recover'];
    let defaults = {};
    try { const dd = await App.api('/app-settings/telegram/templates-default'); if (dd?.success) defaults = dd.data || {}; } catch(_){}
    tplKeys.forEach(k => {
      const el = document.getElementById('tg-tpl-' + k);
      if (el) { el.value = s['telegram_tpl_' + k] || ''; el.placeholder = defaults[k] || ''; }
    });
  } catch (e) { console.error('loadTelegramSettings:', e); }
}

function _telegramPayload() {
  return {
    telegram_enabled:      document.getElementById('tg-enabled')?.checked ? '1' : '0',
    telegram_bot_token:    document.getElementById('tg-bot-token')?.value.trim() || '',
    telegram_chat_id:      document.getElementById('tg-chat-id')?.value.trim() || '',
    telegram_notif_device: document.getElementById('tg-notif-device')?.checked ? '1' : '0',
    telegram_notif_ont:    document.getElementById('tg-notif-ont')?.checked ? '1' : '0',
    telegram_notif_ip:     document.getElementById('tg-notif-ip')?.checked ? '1' : '0',
    telegram_notif_pppoe:  document.getElementById('tg-notif-pppoe')?.checked ? '1' : '0',
    telegram_interval:     document.getElementById('tg-interval')?.value || '3',
    telegram_tpl_device_down:    document.getElementById('tg-tpl-device_down')?.value || '',
    telegram_tpl_device_recover: document.getElementById('tg-tpl-device_recover')?.value || '',
    telegram_tpl_ont_down:       document.getElementById('tg-tpl-ont_down')?.value || '',
    telegram_tpl_ont_recover:    document.getElementById('tg-tpl-ont_recover')?.value || '',
    telegram_tpl_ip_down:        document.getElementById('tg-tpl-ip_down')?.value || '',
    telegram_tpl_ip_recover:     document.getElementById('tg-tpl-ip_recover')?.value || '',
    telegram_tpl_pppoe_down:     document.getElementById('tg-tpl-pppoe_down')?.value || '',
    telegram_tpl_pppoe_recover:  document.getElementById('tg-tpl-pppoe_recover')?.value || '',
  };
}

async function saveTelegramSettings() {
  try {
    const d = await App.api('/app-settings', { method:'POST', body: JSON.stringify(_telegramPayload()) });
    if (d?.success) App.showToast('Pengaturan Telegram disimpan', 'success');
    else App.showToast(d?.message || 'Gagal menyimpan', 'error');
  } catch (e) { App.showToast('Gagal: ' + e.message, 'error'); }
}

async function testTelegram() {
  const btn = document.getElementById('tg-test-btn');
  const out = document.getElementById('tg-test-result');
  if (out) { out.textContent = 'Menguji…'; out.style.color = '#6b7fa8'; }
  if (btn) btn.disabled = true;
  try {
    // Simpan dulu config terbaru, lalu tes
    const d = await App.api('/app-settings/telegram/test', { method:'POST', body: JSON.stringify(_telegramPayload()) });
    if (out) {
      out.textContent = d?.success ? ('✓ ' + (d.message || 'Berhasil')) : ('✗ ' + (d?.message || 'Gagal'));
      out.style.color = d?.success ? '#16a34a' : '#ef4444';
    }
  } catch (e) {
    if (out) { out.textContent = '✗ ' + e.message; out.style.color = '#ef4444'; }
  } finally {
    if (btn) btn.disabled = false;
  }
}
window.saveTelegramSettings = saveTelegramSettings;
window.testTelegram = testTelegram;
window.loadTelegramSettings = loadTelegramSettings;

// Paksa jalankan pengecekan monitoring sekarang (debug / verifikasi).
async function runTelegramChecks() {
  const btn = document.getElementById('tg-run-btn');
  const out = document.getElementById('tg-test-result');
  if (out) { out.textContent = 'Menjalankan pengecekan…'; out.style.color = '#6b7fa8'; }
  if (btn) btn.disabled = true;
  try {
    // Simpan config terbaru dulu supaya pakai toggle/template terkini
    await saveTelegramSettings();
    const d = await App.api('/app-settings/telegram/run-checks', { method:'POST', body: '{}' });
    if (out) {
      out.textContent = d?.success ? ('✓ ' + (d.message || 'Selesai')) : ('✗ ' + (d?.message || 'Gagal'));
      out.style.color = d?.success ? '#16a34a' : '#ef4444';
    }
  } catch (e) {
    if (out) { out.textContent = '✗ ' + e.message; out.style.color = '#ef4444'; }
  } finally {
    if (btn) btn.disabled = false;
  }
}
window.runTelegramChecks = runTelegramChecks;

// Kosongkan semua textarea template → pakai format bawaan (placeholder tampil)
async function resetTelegramTemplates() {
  if (!confirm('Kembalikan semua template ke format bawaan? Teks kustom akan dihapus.')) return;
  ['device_down','device_recover','ont_down','ont_recover','ip_down','ip_recover','pppoe_down','pppoe_recover'].forEach(k => {
    const el = document.getElementById('tg-tpl-' + k); if (el) el.value = '';
  });
  // Simpan langsung supaya backend pakai default
  await saveTelegramSettings();
}
window.resetTelegramTemplates = resetTelegramTemplates;
</script>

<!-- ═══ MODAL: Announcement ═══ -->
<!-- MODAL: Kirim Email Test -->
<div id="testEmailModal" style="display:none;position:fixed;inset:0;background:rgba(13,27,62,.55);backdrop-filter:blur(3px);z-index:10001;align-items:center;justify-content:center;padding:24px;" onclick="if(event.target===this)closeTestEmailModal()">
  <div style="background:#fff;border-radius:18px;width:min(440px,94vw);box-shadow:0 26px 70px rgba(13,27,62,.4);overflow:hidden;">
    <div style="display:flex;align-items:center;gap:11px;padding:18px 22px;border-bottom:1px solid #eef2f9;background:linear-gradient(135deg,#f8faff,#eef4ff);">
      <div style="width:38px;height:38px;border-radius:11px;background:#eff6ff;color:#1d4ed8;display:flex;align-items:center;justify-content:center;flex-shrink:0;">
        <svg width="19" height="19" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path d="M4 4h16a2 2 0 0 1 2 2v12a2 2 0 0 1-2 2H4a2 2 0 0 1-2-2V6a2 2 0 0 1 2-2z"/><polyline points="22,6 12,13 2,6"/></svg>
      </div>
      <div style="flex:1;">
        <div style="font-size:16px;font-weight:800;color:#0d1b3e;letter-spacing:-.3px;">Kirim Email Test</div>
        <div style="font-size:12px;color:#64748b;margin-top:1px;">Verifikasi konfigurasi SMTP Anda</div>
      </div>
      <button onclick="closeTestEmailModal()" style="width:34px;height:34px;border-radius:10px;border:1px solid #e2e8f0;background:#fff;color:#64748b;cursor:pointer;display:flex;align-items:center;justify-content:center;flex-shrink:0;">
        <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2.2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12"/></svg>
      </button>
    </div>
    <div style="padding:22px;">
      <label class="pf-label">Alamat Email Tujuan</label>
      <input type="email" class="pf-input" id="testEmailTo" placeholder="nama@domain.com" autocomplete="off" onkeydown="if(event.key==='Enter')sendTestEmailNow()">
      <div class="op-hint">Email percobaan akan dikirim ke alamat ini memakai konfigurasi SMTP yang sedang diisi.</div>
      <div id="testEmailStatus" style="display:none;margin-top:14px;padding:11px 13px;border-radius:10px;font-size:12.5px;font-weight:600;"></div>
      <div style="display:flex;gap:10px;margin-top:20px;">
        <button onclick="closeTestEmailModal()" style="flex:1;padding:11px;border-radius:11px;border:none;background:#f1f5f9;color:#475569;font-family:inherit;font-size:13.5px;font-weight:700;cursor:pointer;">Batal</button>
        <button id="testEmailSendBtn" onclick="sendTestEmailNow()" style="flex:1;padding:11px;border-radius:11px;border:none;background:#1d4ed8;color:#fff;font-family:inherit;font-size:13.5px;font-weight:700;cursor:pointer;display:inline-flex;align-items:center;justify-content:center;gap:7px;">
          <svg width="15" height="15" fill="none" stroke="currentColor" stroke-width="2.2" viewBox="0 0 24 24"><line x1="22" y1="2" x2="11" y2="13"/><polygon points="22 2 15 22 11 13 2 9 22 2"/></svg>
          Kirim Test
        </button>
      </div>
    </div>
  </div>
</div>

<div id="annModal" style="display:none;position:fixed;inset:0;background:rgba(0,0,0,.45);z-index:9999;display:none;align-items:center;justify-content:center;padding:20px">
  <div style="background:#fff;border-radius:20px;padding:28px;width:100%;max-width:480px;max-height:90vh;overflow-y:auto;box-shadow:0 20px 60px rgba(0,0,0,.2)">
    <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:20px">
      <div style="font-size:16px;font-weight:800;color:#0d1b3e" id="annModalTitle">Tambah Pengumuman</div>
      <button onclick="closeAnnModal()" style="background:none;border:none;font-size:18px;cursor:pointer;color:#6b7fa8;padding:4px">✕</button>
    </div>
    <div style="margin-bottom:14px">
      <label class="pf-label">Jenis</label>
      <select class="pf-select" id="annType">
        <option value="gangguan">⚠️ Gangguan Jaringan</option>
        <option value="maintenance">🔧 Maintenance</option>
        <option value="info" selected>ℹ️ Informasi</option>
        <option value="promo">🎁 Promo</option>
      </select>
    </div>
    <div style="margin-bottom:14px">
      <label class="pf-label">Judul *</label>
      <input type="text" class="pf-input" id="annTitle" placeholder="Contoh: Gangguan Jaringan Area Depok" maxlength="200">
    </div>
    <div style="margin-bottom:14px">
      <label class="pf-label">Isi / Detail (opsional)</label>
      <textarea class="pf-input" id="annContent" rows="3" style="resize:vertical" placeholder="Penjelasan lebih lengkap..."></textarea>
    </div>
    <div style="display:grid;grid-template-columns:1fr 1fr;gap:12px;margin-bottom:14px">
      <div>
        <label class="pf-label">Tampil Mulai (kosongkan = sekarang)</label>
        <input type="datetime-local" class="pf-input" id="annFrom">
      </div>
      <div>
        <label class="pf-label">Tampil Sampai (kosongkan = selamanya)</label>
        <input type="datetime-local" class="pf-input" id="annUntil">
      </div>
    </div>
    <div style="display:flex;align-items:center;gap:10px;margin-bottom:20px">
      <input type="checkbox" id="annActive" checked style="width:16px;height:16px;accent-color:#1a6ef5">
      <label for="annActive" style="font-size:13px;font-weight:600;color:#0d1b3e;cursor:pointer">Aktif (tampil di portal pelanggan)</label>
    </div>
    <div style="display:flex;gap:10px">
      <button onclick="saveAnn()" id="btnSaveAnn" style="flex:1;padding:11px;border:none;border-radius:10px;background:linear-gradient(135deg,#1a6ef5,#0047cc);color:#fff;font-size:13px;font-weight:700;cursor:pointer;font-family:inherit">Simpan</button>
      <button onclick="closeAnnModal()" style="padding:11px 18px;border:1.5px solid #e0e7f3;border-radius:10px;background:#fff;font-size:13px;font-weight:600;cursor:pointer;font-family:inherit;color:#6b7fa8">Batal</button>
    </div>
  </div>
</div>

<script>
// ── Announcement CRUD ──────────────────────────────────────────
var _annEditId = null;
var _typeColors = { gangguan:'#fee2e2', maintenance:'#fef3c7', info:'#eff6ff', promo:'#f0fdf4' };
var _typeBorder = { gangguan:'#fca5a5', maintenance:'#fde68a', info:'#bfdbfe', promo:'#86efac' };
var _typeLabel  = { gangguan:'⚠️ Gangguan', maintenance:'🔧 Maintenance', info:'ℹ️ Info', promo:'🎁 Promo' };

async function loadAnnList() {
  try {
    var r = await fetch('/api/announcements', { headers: { 'Content-Type': 'application/json' } });
    var j = await r.json();
    var el = document.getElementById('annList');
    if (!j.data || !j.data.length) {
      el.innerHTML = '<div style="padding:32px;text-align:center;color:#6b7fa8;font-size:13px">Belum ada pengumuman</div>';
      return;
    }
    var html = '<table style="width:100%;border-collapse:collapse">' +
      '<thead><tr style="background:#f8faff;font-size:11px;font-weight:700;color:#6b7fa8;text-transform:uppercase">' +
      '<th style="padding:10px 16px;text-align:left">Judul</th>' +
      '<th style="padding:10px 8px;text-align:center">Jenis</th>' +
      '<th style="padding:10px 8px;text-align:center">Status</th>' +
      '<th style="padding:10px 8px;text-align:center">Sampai</th>' +
      '<th style="padding:10px 12px;text-align:right">Aksi</th>' +
      '</tr></thead><tbody>';
    j.data.forEach(function(a) {
      var now = new Date();
      var expired = a.show_until && new Date(a.show_until) < now;
      var statusBg = a.is_active && !expired ? '#dcfce7' : '#f1f5f9';
      var statusColor = a.is_active && !expired ? '#16a34a' : '#6b7fa8';
      var statusText = expired ? 'Expired' : (a.is_active ? 'Aktif' : 'Nonaktif');
      var untilText = a.show_until ? new Date(a.show_until).toLocaleDateString('id-ID',{day:'2-digit',month:'short',year:'numeric'}) : '—';
      html += '<tr style="border-top:1px solid #f0f4ff">' +
        '<td style="padding:11px 16px;font-size:13px;font-weight:600;color:#0d1b3e;max-width:200px">' +
          '<div style="white-space:nowrap;overflow:hidden;text-overflow:ellipsis">' + escHtml(a.title) + '</div>' +
          (a.content ? '<div style="font-size:11px;color:#6b7fa8;margin-top:2px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis">' + escHtml(a.content.substring(0,60)) + '</div>' : '') +
        '</td>' +
        '<td style="padding:11px 8px;text-align:center"><span style="font-size:10px;font-weight:700;padding:3px 8px;border-radius:20px;background:' + (_typeColors[a.type]||'#f1f5f9') + ';border:1px solid ' + (_typeBorder[a.type]||'#e2e8f0') + '">' + (_typeLabel[a.type]||a.type) + '</span></td>' +
        '<td style="padding:11px 8px;text-align:center">' +
          '<button onclick="toggleAnn(' + a.id + ')" style="font-size:10px;font-weight:700;padding:3px 10px;border-radius:20px;border:none;cursor:pointer;background:' + statusBg + ';color:' + statusColor + '">' + statusText + '</button>' +
        '</td>' +
        '<td style="padding:11px 8px;text-align:center;font-size:11px;color:#6b7fa8">' + untilText + '</td>' +
        '<td style="padding:11px 12px;text-align:right;white-space:nowrap">' +
          '<button onclick="editAnn(' + JSON.stringify(a).replace(/"/g,'&quot;') + ')" style="padding:5px 10px;border-radius:8px;border:1.5px solid #e0e7f3;background:#fff;font-size:11px;font-weight:700;cursor:pointer;margin-right:4px;color:#1a6ef5">Edit</button>' +
          '<button onclick="deleteAnn(' + a.id + ')" style="padding:5px 10px;border-radius:8px;border:1.5px solid #fca5a5;background:#fff;font-size:11px;font-weight:700;cursor:pointer;color:#dc2626">Hapus</button>' +
        '</td>' +
        '</tr>';
    });
    html += '</tbody></table>';
    el.innerHTML = html;
  } catch(e) { document.getElementById('annList').innerHTML = '<div style="padding:24px;text-align:center;color:#dc2626;font-size:13px">Gagal memuat</div>'; }
}

function escHtml(s) { return (s||'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;'); }

function openAnnModal(data) {
  _annEditId = null;
  document.getElementById('annModalTitle').textContent = 'Tambah Pengumuman';
  document.getElementById('annType').value = 'info';
  document.getElementById('annTitle').value = '';
  document.getElementById('annContent').value = '';
  document.getElementById('annFrom').value = '';
  document.getElementById('annUntil').value = '';
  document.getElementById('annActive').checked = true;
  document.getElementById('annModal').style.display = 'flex';
}

function editAnn(a) {
  _annEditId = a.id;
  document.getElementById('annModalTitle').textContent = 'Edit Pengumuman';
  document.getElementById('annType').value = a.type || 'info';
  document.getElementById('annTitle').value = a.title || '';
  document.getElementById('annContent').value = a.content || '';
  document.getElementById('annFrom').value = a.show_from ? a.show_from.substring(0,16) : '';
  document.getElementById('annUntil').value = a.show_until ? a.show_until.substring(0,16) : '';
  document.getElementById('annActive').checked = !!a.is_active;
  document.getElementById('annModal').style.display = 'flex';
}

function closeAnnModal() { document.getElementById('annModal').style.display = 'none'; }

async function saveAnn() {
  var title = document.getElementById('annTitle').value.trim();
  if (!title) { alert('Judul wajib diisi'); return; }
  var btn = document.getElementById('btnSaveAnn');
  btn.disabled = true; btn.textContent = 'Menyimpan...';
  try {
    var body = {
      title, type: document.getElementById('annType').value,
      content: document.getElementById('annContent').value.trim() || null,
      show_from: document.getElementById('annFrom').value || null,
      show_until: document.getElementById('annUntil').value || null,
      is_active: document.getElementById('annActive').checked
    };
    var url = _annEditId ? '/api/announcements/' + _annEditId : '/api/announcements';
    var method = _annEditId ? 'PUT' : 'POST';
    var r = await fetch(url, { method, headers: {'Content-Type':'application/json'}, body: JSON.stringify(body) });
    var j = await r.json();
    if (j.success) { closeAnnModal(); loadAnnList(); } else { alert(j.message || 'Gagal'); }
  } catch(e) { alert('Error: ' + e.message); }
  finally { btn.disabled = false; btn.textContent = 'Simpan'; }
}

async function toggleAnn(id) {
  try {
    var r = await fetch('/api/announcements/' + id + '/toggle', { method: 'PATCH', headers: {'Content-Type':'application/json'} });
    var j = await r.json();
    if (j.success) loadAnnList();
  } catch(e) {}
}

async function deleteAnn(id) {
  if (!confirm('Hapus pengumuman ini?')) return;
  try {
    await fetch('/api/announcements/' + id, { method: 'DELETE', headers: {'Content-Type':'application/json'} });
    loadAnnList();
  } catch(e) {}
}

// Load saat section announcement / payment / push dibuka
var _origShowSection = showSection;
showSection = function(id, el) {
  _origShowSection(id, el);
  if (id === 'announcements') loadAnnList();
  if (id === 'payment' && !window._PA_LOADED) {
    window._PA_LOADED = true;
    loadPayAccounts();
  }
  if (id === 'push' && !window._PUSH_LOADED) {
    window._PUSH_LOADED = true;
    initPushNotifSection();
  }
};

// ══════════════════════════════════════════════════════════════
// PUSH NOTIFICATION — Kirim, Template, Riwayat
// ══════════════════════════════════════════════════════════════
var _PUSH_PICKED = new Map();   // id → { id, name, customer_id, phone }
var _CAT_META = {
  promo:      { icon:'🎉', color:'#f59e0b', label:'PROMO' },
  info:       { icon:'ℹ️', color:'#3b82f6', label:'INFO' },
  maintenance:{ icon:'🔧', color:'#64748b', label:'MAINTENANCE' },
  warning:    { icon:'⚠️', color:'#ef4444', label:'WARNING' },
  greeting:   { icon:'🙏', color:'#8b5cf6', label:'GREETING' },
  other:      { icon:'📌', color:'#475569', label:'LAINNYA' }
};

async function initPushNotifSection() {
  loadPushStats();
  loadPackageOptions();
  updatePushPreview();
  updateManualPickCount(); // tampilkan hint initial
  loadPushReminderSettings(); // muat pengaturan reminder tagihan otomatis
  loadPushIsolirSettings();   // muat pengaturan notif isolir & restore
  loadPushTestSubscribers();  // muat daftar subscriber untuk dropdown test
  // Default tab: Kirim — sudah active
}

// ── Pengaturan Reminder Tagihan Otomatis ──────────────────────
const PR_DEFAULTS = {
  push_reminder_enabled:'1', push_reminder_send_hour:'8',
  push_reminder_h3_enabled:'1', push_reminder_h1_enabled:'1', push_reminder_h0_enabled:'1',
  push_reminder_overdue_enabled:'1', push_reminder_overdue_hour:'9',
  push_reminder_h3_title:'⏰ Tagihan 3 Hari Lagi',
  push_reminder_h3_body:'Tagihan Anda sebesar {jumlah} jatuh tempo {tgl}. Bayar sekarang untuk menghindari isolir.',
  push_reminder_h1_title:'⚠️ Tagihan Besok Jatuh Tempo',
  push_reminder_h1_body:'Tagihan {jumlah} jatuh tempo besok ({tgl}). Segera bayar!',
  push_reminder_h0_title:'🚨 Tagihan Jatuh Tempo Hari Ini!',
  push_reminder_h0_body:'Tagihan {jumlah} harus dibayar hari ini untuk menjaga koneksi internet Anda.',
  push_reminder_overdue_title:'🔴 Tagihan Melewati Jatuh Tempo',
  push_reminder_overdue_body:'Tagihan {jumlah} melewati tanggal jatuh tempo. Bayar sekarang untuk menghindari pemutusan layanan.'
};

function _prFillHourSelect(id, val) {
  const sel = document.getElementById(id);
  if (!sel) return;
  sel.innerHTML = '';
  for (let h = 0; h < 24; h++) {
    const o = document.createElement('option');
    o.value = String(h);
    o.textContent = (h < 10 ? '0' + h : h) + ':00';
    sel.appendChild(o);
  }
  sel.value = String(parseInt(val) || 0);
}

function _prToggleBody() {
  const on = document.getElementById('prEnabled')?.checked;
  const body = document.getElementById('prBody');
  if (body) { body.style.opacity = on ? '1' : '.45'; body.style.pointerEvents = on ? '' : 'none'; }
}

async function loadPushReminderSettings() {
  try {
    const d = await App.api('/app-settings');
    const cfg = (d?.success && d.data) ? d.data : {};
    const g = k => (cfg[k] !== undefined && cfg[k] !== '') ? cfg[k] : PR_DEFAULTS[k];
    const setChk = (id, k) => { const e = document.getElementById(id); if (e) e.checked = g(k) === '1'; };
    const setVal = (id, k) => { const e = document.getElementById(id); if (e) e.value = g(k); };
    setChk('prEnabled','push_reminder_enabled');
    setChk('prH3','push_reminder_h3_enabled');
    setChk('prH1','push_reminder_h1_enabled');
    setChk('prH0','push_reminder_h0_enabled');
    setChk('prOverdue','push_reminder_overdue_enabled');
    _prFillHourSelect('prSendHour', g('push_reminder_send_hour'));
    _prFillHourSelect('prOverdueHour', g('push_reminder_overdue_hour'));
    setVal('prH3Title','push_reminder_h3_title');   setVal('prH3Body','push_reminder_h3_body');
    setVal('prH1Title','push_reminder_h1_title');   setVal('prH1Body','push_reminder_h1_body');
    setVal('prH0Title','push_reminder_h0_title');   setVal('prH0Body','push_reminder_h0_body');
    setVal('prOverdueTitle','push_reminder_overdue_title'); setVal('prOverdueBody','push_reminder_overdue_body');
    const me = document.getElementById('prEnabled');
    if (me && !me.__bound) { me.addEventListener('change', _prToggleBody); me.__bound = true; }
    _prToggleBody();
  } catch (e) { console.error('loadPushReminderSettings:', e); }
}

async function savePushReminderSettings() {
  const btn = document.getElementById('prSaveBtn');
  const msg = document.getElementById('prSaveMsg');
  const v   = id => document.getElementById(id);
  const body = {
    push_reminder_enabled:         v('prEnabled')?.checked ? '1' : '0',
    push_reminder_send_hour:       v('prSendHour')?.value || '8',
    push_reminder_overdue_hour:    v('prOverdueHour')?.value || '9',
    push_reminder_h3_enabled:      v('prH3')?.checked ? '1' : '0',
    push_reminder_h1_enabled:      v('prH1')?.checked ? '1' : '0',
    push_reminder_h0_enabled:      v('prH0')?.checked ? '1' : '0',
    push_reminder_overdue_enabled: v('prOverdue')?.checked ? '1' : '0',
    push_reminder_h3_title: v('prH3Title')?.value || '',  push_reminder_h3_body: v('prH3Body')?.value || '',
    push_reminder_h1_title: v('prH1Title')?.value || '',  push_reminder_h1_body: v('prH1Body')?.value || '',
    push_reminder_h0_title: v('prH0Title')?.value || '',  push_reminder_h0_body: v('prH0Body')?.value || '',
    push_reminder_overdue_title: v('prOverdueTitle')?.value || '', push_reminder_overdue_body: v('prOverdueBody')?.value || ''
  };
  if (btn) btn.disabled = true;
  try {
    const d = await App.api('/app-settings', { method:'POST', body: JSON.stringify(body) });
    if (d?.success) { App.showToast('Pengaturan reminder disimpan', 'success'); if (msg) { msg.textContent = '✓ Tersimpan'; setTimeout(()=>{ if(msg) msg.textContent=''; }, 3000); } }
    else App.showToast(d?.message || 'Gagal menyimpan', 'error');
  } catch (e) { App.showToast('Error: ' + e.message, 'error'); }
  finally { if (btn) btn.disabled = false; }
}

// ── Verifikasi sistem: muat daftar subscriber untuk dropdown test ──
async function loadPushTestSubscribers() {
  const sel = document.getElementById('prTestCustomer');
  if (!sel) return;
  try {
    const d = await App.api('/push-notif/customers?subscribed_only=true&limit=200');
    if (!d?.success || !Array.isArray(d.data)) return;
    while (sel.options.length > 1) sel.remove(1);
    d.data.forEach(c => {
      const opt = document.createElement('option');
      opt.value = c.id;
      opt.textContent = `${c.name} (${c.customer_id})`;
      sel.appendChild(opt);
    });
    if (!d.data.length) sel.options[0].textContent = 'Belum ada subscriber';
  } catch (e) { console.error('loadPushTestSubscribers:', e); }
}

// ── Test kirim 1 notifikasi ──
async function testPushSend() {
  const btn = document.getElementById('prTestSendBtn');
  const sel = document.getElementById('prTestCustomer');
  const msg = document.getElementById('prTestMsg');
  const body = {};
  if (sel && sel.value) body.customer_id = parseInt(sel.value);
  if (btn) btn.disabled = true;
  if (msg) { msg.style.color = 'var(--d-muted)'; msg.textContent = 'Mengirim notifikasi uji…'; }
  try {
    const d = await App.api('/push-notif/test', { method:'POST', body: JSON.stringify(body) });
    if (d?.success) {
      App.showToast('Notifikasi uji terkirim', 'success');
      if (msg) { msg.style.color = '#16a34a'; msg.textContent = '✓ ' + d.message; }
    } else {
      App.showToast(d?.message || 'Gagal mengirim', 'error');
      if (msg) { msg.style.color = '#dc2626'; msg.textContent = '✕ ' + (d?.message || 'Gagal mengirim'); }
    }
  } catch (e) {
    App.showToast('Error: ' + e.message, 'error');
    if (msg) { msg.style.color = '#dc2626'; msg.textContent = '✕ ' + e.message; }
  } finally { if (btn) btn.disabled = false; }
}

// ── Test trigger cron reminder otomatis ──
async function testPushCron(type) {
  const btnId = type === 'overdue' ? 'prTestOverdueBtn' : 'prTestDueBtn';
  const btn = document.getElementById(btnId);
  const msg = document.getElementById('prTestMsg');
  if (btn) btn.disabled = true;
  if (msg) { msg.style.color = 'var(--d-muted)'; msg.textContent = 'Menjalankan cron ' + type + '…'; }
  try {
    const d = await App.api('/push-notif/test-cron/' + encodeURIComponent(type), { method:'POST', body: '{}' });
    if (d?.success) {
      App.showToast('Cron dijalankan', 'success');
      if (msg) { msg.style.color = '#16a34a'; msg.textContent = '✓ ' + d.message; }
    } else {
      App.showToast(d?.message || 'Gagal', 'error');
      if (msg) { msg.style.color = '#dc2626'; msg.textContent = '✕ ' + (d?.message || 'Gagal'); }
    }
  } catch (e) {
    App.showToast('Error: ' + e.message, 'error');
    if (msg) { msg.style.color = '#dc2626'; msg.textContent = '✕ ' + e.message; }
  } finally { if (btn) btn.disabled = false; }
}

// ── Pengaturan Notifikasi Isolir & Restore ────────────────────
const PI_DEFAULTS = {
  push_isolir_enabled:'1',
  push_isolir_title:'🚫 Layanan Internet Terisolir',
  push_isolir_body:'Halo {nama}, layanan internet Anda dinonaktifkan sementara. Silakan lakukan pembayaran melalui portal untuk mengaktifkan kembali.',
  push_restore_title:'✅ Layanan Internet Aktif Kembali',
  push_restore_body:'Halo {nama}, layanan internet Anda telah aktif kembali. Terima kasih.'
};

function _piToggleBody() {
  const on = document.getElementById('piEnabled')?.checked;
  const body = document.getElementById('piBody');
  if (body) { body.style.opacity = on ? '1' : '.45'; body.style.pointerEvents = on ? '' : 'none'; }
}

async function loadPushIsolirSettings() {
  try {
    const d = await App.api('/app-settings');
    const cfg = (d?.success && d.data) ? d.data : {};
    const g = k => (cfg[k] !== undefined && cfg[k] !== '') ? cfg[k] : PI_DEFAULTS[k];
    const setVal = (id, k) => { const e = document.getElementById(id); if (e) e.value = g(k); };
    const en = document.getElementById('piEnabled'); if (en) en.checked = g('push_isolir_enabled') === '1';
    setVal('piIsolirTitle','push_isolir_title');   setVal('piIsolirBody','push_isolir_body');
    setVal('piRestoreTitle','push_restore_title'); setVal('piRestoreBody','push_restore_body');
    if (en && !en.__bound) { en.addEventListener('change', _piToggleBody); en.__bound = true; }
    _piToggleBody();
  } catch (e) { console.error('loadPushIsolirSettings:', e); }
}

async function savePushIsolirSettings() {
  const btn = document.getElementById('piSaveBtn');
  const msg = document.getElementById('piSaveMsg');
  const v = id => document.getElementById(id);
  const body = {
    push_isolir_enabled:  v('piEnabled')?.checked ? '1' : '0',
    push_isolir_title:    v('piIsolirTitle')?.value || '',
    push_isolir_body:     v('piIsolirBody')?.value || '',
    push_restore_title:   v('piRestoreTitle')?.value || '',
    push_restore_body:    v('piRestoreBody')?.value || ''
  };
  if (btn) btn.disabled = true;
  try {
    const d = await App.api('/app-settings', { method:'POST', body: JSON.stringify(body) });
    if (d?.success) { App.showToast('Pengaturan notifikasi isolir disimpan', 'success'); if (msg) { msg.textContent = '✓ Tersimpan'; setTimeout(()=>{ if(msg) msg.textContent=''; }, 3000); } }
    else App.showToast(d?.message || 'Gagal menyimpan', 'error');
  } catch (e) { App.showToast('Error: ' + e.message, 'error'); }
  finally { if (btn) btn.disabled = false; }
}
window.savePushIsolirSettings = savePushIsolirSettings;

async function loadPushStats() {
  try {
    const d = await App.api('/push-notif/stats');
    if (!d?.success) return;
    const s = d.data;
    document.getElementById('pushStatSubs').textContent  = s.total_subscribers;
    document.getElementById('pushStatToday').textContent = s.total_today;
    document.getElementById('pushStatMonth').textContent = s.total_month;
    document.getElementById('pushStatSched').textContent = s.total_scheduled;

    // Badge di tab Schedule
    const tabBadge = document.getElementById('pushSchedBadge');
    if (tabBadge) {
      if (s.total_scheduled > 0) {
        tabBadge.style.display = 'inline-flex';
        tabBadge.innerHTML = '<span style="padding:1px 7px;background:#dbeafe;color:#1e40af;border-radius:10px;font-size:10px;font-weight:800;margin-left:2px">' + s.total_scheduled + '</span>';
      } else {
        tabBadge.style.display = 'none';
      }
    }

    // Badge status VAPID
    const badge = document.getElementById('pushReadyBadge');
    if (!s.push_ready) {
      badge.style.display = 'block';
      badge.innerHTML = '<span style="padding:4px 10px;background:#fee2e2;color:#991b1b;border-radius:20px;font-size:10.5px;font-weight:700;display:inline-flex;align-items:center;gap:5px">' +
        '<svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M10.29 3.86L1.82 18a2 2 0 001.71 3h16.94a2 2 0 001.71-3L13.71 3.86a2 2 0 00-3.42 0z"/><line x1="12" y1="9" x2="12" y2="13"/><line x1="12" y1="17" x2="12.01" y2="17"/></svg>' +
        'VAPID belum dikonfigurasi</span>';
    } else {
      badge.style.display = 'block';
      badge.innerHTML = '<span style="padding:4px 10px;background:#dcfce7;color:#166534;border-radius:20px;font-size:10.5px;font-weight:700;display:inline-flex;align-items:center;gap:5px">' +
        '<span style="width:7px;height:7px;border-radius:50%;background:#10b981;animation:pushPulse 1.5s infinite"></span>' +
        'Aktif</span>';
    }
  } catch(e) { console.error('loadPushStats:', e); }
}

async function loadPackageOptions() {
  try {
    const d = await App.api('/packages');
    const list = d?.data || d?.packages || (Array.isArray(d) ? d : []);
    const container = document.getElementById('pushFilterPackages');
    if (!list.length) {
      container.innerHTML = '<div style="color:var(--d-muted);font-size:12px;padding:4px">Belum ada paket.</div>';
      return;
    }
    container.innerHTML = list.map(p =>
      '<label style="display:flex;align-items:center;gap:8px;padding:6px 6px;cursor:pointer;border-radius:6px" onmouseover="this.style.background=\'#f8faff\'" onmouseout="this.style.background=\'\'">' +
        '<input type="checkbox" class="push-fpkg" value="'+p.id+'" style="width:14px;height:14px;cursor:pointer;margin:0">' +
        '<span style="font-size:12.5px;color:var(--d-text);font-weight:600;flex:1">'+escapeHtmlSt(p.name||'?')+'</span>' +
        '<span style="font-size:11px;color:var(--d-muted);font-family:\'DM Mono\',monospace">'+(p.speed_down||0)+'Mbps</span>' +
      '</label>'
    ).join('');
  } catch(e) {
    document.getElementById('pushFilterPackages').innerHTML = '<div style="color:#dc2626;font-size:12px;padding:4px">Gagal memuat paket: '+e.message+'</div>';
  }
}

function switchPushTab(tab, el) {
  document.querySelectorAll('.push-tab').forEach(t => t.classList.remove('active'));
  document.querySelectorAll('.push-tab-content').forEach(c => c.classList.remove('active'));
  el.classList.add('active');
  document.getElementById('push-tab-'+tab).classList.add('active');
  if (tab === 'scheduled')     loadPushScheduled();
  else if (tab === 'template') loadPushTemplates();
  else if (tab === 'history')  loadPushHistory();
}

function collectPushFilters() {
  const packages = Array.from(document.querySelectorAll('.push-fpkg:checked')).map(i => parseInt(i.value));
  const bill_status = Array.from(document.querySelectorAll('.push-fbill:checked')).map(i => i.value);
  const customer_status = Array.from(document.querySelectorAll('.push-fcust:checked')).map(i => i.value);
  const isolir_checked = document.querySelectorAll('.push-fisol:checked');
  const isolir_status = Array.from(isolir_checked).map(i => i.value);
  const area = document.getElementById('pushFilterArea').value.trim();

  const filters = {};
  if (packages.length)        filters.packages = packages;
  if (bill_status.length)     filters.bill_status = bill_status;
  if (customer_status.length) filters.customer_status = customer_status;
  if (isolir_status.length)   filters.isolir_status = isolir_status;
  if (area)                   filters.area = area;
  if (_PUSH_PICKED.size)      filters.customer_ids = Array.from(_PUSH_PICKED.keys());

  return filters;
}

async function calcPushTargets() {
  const filters = collectPushFilters();
  const el = document.getElementById('pushPreviewText');
  el.innerHTML = '<span style="color:var(--d-muted)">Menghitung...</span>';
  try {
    const d = await App.api('/push-notif/preview-targets', {
      method: 'POST', body: JSON.stringify({ filters })
    });
    if (!d?.success) { el.innerHTML = '<span style="color:#dc2626">Gagal: '+(d?.message||'error')+'</span>'; return; }
    const s = d.data;
    el.innerHTML =
      '<div style="font-size:18px;font-weight:800;color:var(--d-blue1);margin-bottom:2px">' + s.total_subscribed + ' pelanggan</div>' +
      '<div style="font-size:11.5px;color:var(--d-muted)">' +
        'Match filter: <b>' + s.total_matched + '</b> · ' +
        'Subscribe push: <b>' + s.total_subscribed + '</b>' +
        (s.total_unsubscribed > 0 ? ' · <span style="color:#b45309">Belum subscribe: '+s.total_unsubscribed+'</span>' : '') +
      '</div>';
  } catch(e) {
    el.innerHTML = '<span style="color:#dc2626">Error: '+e.message+'</span>';
  }
}

let _custSearchTimer = null;
window._PUSH_CUST_CACHE = {}; // id → customer data (cache hasil search)

function debouncedCustSearch() {
  clearTimeout(_custSearchTimer);
  _custSearchTimer = setTimeout(loadCustomerPicker, 300);
}

async function loadCustomerPicker() {
  const search = document.getElementById('pushCustSearch').value.trim();
  const container = document.getElementById('pushCustList');
  if (!search) {
    container.style.display = 'none';
    container.innerHTML = '';
    return;
  }
  container.style.display = 'block';
  container.innerHTML = '<div style="padding:10px;color:var(--d-muted);text-align:center">Mencari...</div>';
  try {
    const d = await App.api('/push-notif/customers?search='+encodeURIComponent(search)+'&limit=30');
    const list = d?.data || [];
    if (!list.length) {
      container.innerHTML = '<div style="padding:10px;color:var(--d-muted);text-align:center">Tidak ada pelanggan yang cocok.</div>';
      return;
    }

    // Cache data tiap customer supaya bisa diambil by ID saat diklik
    list.forEach(c => {
      window._PUSH_CUST_CACHE[c.id] = {
        id: c.id,
        name: c.name || '',
        customer_id: c.customer_id || '',
        phone: c.phone || ''
      };
    });

    container.innerHTML = list.map(c => {
      const picked = _PUSH_PICKED.has(c.id);
      // Pakai data-id attribute saja — tidak ada JSON di inline onclick
      return '<div class="push-cust-row'+(picked?' picked':'')+'" data-cust-id="'+c.id+'">' +
        '<input type="checkbox" '+(picked?'checked':'')+' tabindex="-1" style="pointer-events:none">' +
        '<span class="push-cust-sub'+(c.subscribed?'':' off')+'" title="'+(c.subscribed?'Subscribe aktif':'Belum subscribe push')+'"></span>' +
        '<span class="push-cust-name">'+escapeHtmlSt(c.name||'—')+'</span>' +
        '<span class="push-cust-meta">'+escapeHtmlSt(c.customer_id||'')+(c.phone?' · '+escapeHtmlSt(c.phone):'')+'</span>' +
      '</div>';
    }).join('');

    // Pasang event delegation SEKALI saja (idempotent)
    _attachCustPickerListener();
  } catch(e) {
    container.innerHTML = '<div style="padding:10px;color:#dc2626;text-align:center">Error: '+e.message+'</div>';
  }
}

// Attach event listener ke container (event delegation, idempotent)
function _attachCustPickerListener() {
  const container = document.getElementById('pushCustList');
  if (!container || container._pushListenerAttached) return;
  container._pushListenerAttached = true;
  container.addEventListener('click', function(e) {
    const row = e.target.closest('.push-cust-row[data-cust-id]');
    if (!row) return;
    e.preventDefault();
    e.stopPropagation();
    const id = parseInt(row.getAttribute('data-cust-id'));
    if (!id) return;
    togglePickCustomer(id);
  });
}

function togglePickCustomer(id) {
  if (_PUSH_PICKED.has(id)) {
    _PUSH_PICKED.delete(id);
  } else {
    const data = window._PUSH_CUST_CACHE[id] || { id, name: 'ID '+id };
    _PUSH_PICKED.set(id, data);
  }
  // Update visual state semua row yang visible
  document.querySelectorAll('#pushCustList .push-cust-row').forEach(r => {
    const rowId = parseInt(r.getAttribute('data-cust-id'));
    const isChecked = _PUSH_PICKED.has(rowId);
    const cb = r.querySelector('input[type=checkbox]');
    if (cb) cb.checked = isChecked;
    r.classList.toggle('picked', isChecked);
  });
  renderPickedChips();
  updateManualPickCount();
}

function renderPickedChips() {
  const wrap = document.getElementById('pushPickedChips');
  if (!_PUSH_PICKED.size) {
    wrap.style.display = 'none';
    wrap.innerHTML = '';
    return;
  }
  wrap.style.display = 'flex';
  const chips = [];
  for (const [id, data] of _PUSH_PICKED) {
    const name = escapeHtmlSt(data.name || ('ID '+id));
    const meta = data.customer_id ? ' · ' + escapeHtmlSt(data.customer_id) : '';
    chips.push(
      '<div style="display:inline-flex;align-items:center;gap:6px;padding:5px 4px 5px 10px;background:#fff;border:1px solid #bfdbfe;border-radius:20px;font-size:11.5px;font-weight:600;color:var(--d-text)">' +
        '<span style="display:inline-flex;align-items:center;gap:4px"><svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"><path d="M20 21v-2a4 4 0 00-4-4H8a4 4 0 00-4 4v2"/><circle cx="12" cy="7" r="4"/></svg>'+name+'<span style="color:var(--d-muted);font-weight:400">'+meta+'</span></span>' +
        '<button type="button" data-remove-id="'+id+'" style="width:18px;height:18px;border-radius:50%;background:#ef4444;color:#fff;border:none;cursor:pointer;display:flex;align-items:center;justify-content:center;font-size:12px;line-height:1;padding:0" title="Hapus dari daftar">×</button>' +
      '</div>'
    );
  }
  wrap.innerHTML = chips.join('');
  // Pasang listener chip remove (event delegation, idempotent)
  if (!wrap._pushChipListenerAttached) {
    wrap._pushChipListenerAttached = true;
    wrap.addEventListener('click', function(e) {
      const btn = e.target.closest('[data-remove-id]');
      if (!btn) return;
      e.preventDefault();
      const id = parseInt(btn.getAttribute('data-remove-id'));
      if (id) removePickedCustomer(id);
    });
  }
}

function removePickedCustomer(id) {
  _PUSH_PICKED.delete(id);
  renderPickedChips();
  updateManualPickCount();
  // Kalau search result masih tampil, sync visual state row-nya
  document.querySelectorAll('#pushCustList .push-cust-row').forEach(r => {
    const rowId = parseInt(r.getAttribute('data-cust-id'));
    if (rowId === id) {
      const cb = r.querySelector('input[type=checkbox]');
      if (cb) cb.checked = false;
      r.classList.remove('picked');
    }
  });
}

function updateManualPickCount() {
  const el = document.getElementById('pushManualCount');
  if (_PUSH_PICKED.size) {
    el.innerHTML = '<b style="color:var(--d-blue1)">' + _PUSH_PICKED.size + ' pelanggan</b> dipilih manual (override filter di atas)';
  } else {
    el.innerHTML = 'Ketik nama/ID/telepon lalu klik pada pelanggan untuk menambahkan ke daftar.';
  }
}

function clearManualPick() {
  _PUSH_PICKED.clear();
  document.getElementById('pushCustSearch').value = '';
  document.getElementById('pushCustList').innerHTML = '';
  document.getElementById('pushCustList').style.display = 'none';
  renderPickedChips();
  updateManualPickCount();
}

function pickEmoji(e) {
  document.getElementById('pushIcon').value = e;
  updatePushPreview();
}

function updatePushPreview() {
  const icon  = document.getElementById('pushIcon').value || '🔔';
  const title = document.getElementById('pushTitle').value || '(judul notifikasi)';
  const body  = document.getElementById('pushBody').value  || '(isi pesan)';
  document.querySelector('#pushPreview .pp-icon').textContent = icon;
  document.getElementById('ppTitle').textContent = title;
  document.getElementById('ppBody').textContent  = body;
  document.getElementById('pushTitleCount').textContent = document.getElementById('pushTitle').value.length;
  document.getElementById('pushBodyCount').textContent  = document.getElementById('pushBody').value.length;
}

function toggleSchedule() {
  const on = document.getElementById('pushScheduleToggle').checked;
  document.getElementById('pushScheduleWrap').style.display = on ? 'block' : 'none';
  document.getElementById('btnSendPushLabel').textContent = on ? 'Jadwalkan' : 'Kirim Sekarang';
  if (on) {
    // default +5 menit (aman dari edge case: user butuh waktu isi form, gap cron 1 menit)
    const d = new Date(Date.now() + 5*60*1000);
    const pad = n => String(n).padStart(2,'0');
    const v = d.getFullYear() + '-' + pad(d.getMonth()+1) + '-' + pad(d.getDate()) + 'T' + pad(d.getHours()) + ':' + pad(d.getMinutes());
    document.getElementById('pushScheduledAt').value = v;
  }
}

async function sendPushNotif() {
  const title = document.getElementById('pushTitle').value.trim();
  const body  = document.getElementById('pushBody').value.trim();
  if (!title || !body) return App.showToast('Judul dan isi pesan wajib diisi', 'error');

  const payload = {
    title,
    body,
    icon: document.getElementById('pushIcon').value.trim() || null,
    url: document.getElementById('pushUrl').value.trim() || '/portal',
    filters: collectPushFilters()
  };

  const scheduleOn = document.getElementById('pushScheduleToggle').checked;
  if (scheduleOn) {
    const sched = document.getElementById('pushScheduledAt').value;
    if (!sched) return App.showToast('Pilih tanggal & jam jadwal', 'error');

    // Validasi client: minimal 2 menit dari sekarang
    // (buffer untuk: request latency + cron granularity 1 menit + clock skew)
    const schedMs = new Date(sched).getTime();
    const nowMs = Date.now();
    const minScheduleMs = nowMs + 2 * 60 * 1000;
    if (schedMs < minScheduleMs) {
      const minTime = new Date(minScheduleMs);
      const hh = String(minTime.getHours()).padStart(2,'0');
      const mm = String(minTime.getMinutes()).padStart(2,'0');
      return App.showToast('Jadwal terlalu dekat. Minimal 2 menit dari sekarang (paling cepat jam ' + hh + ':' + mm + ')', 'error');
    }

    payload.scheduled_at = new Date(sched).toISOString();
  }

  const btn = document.getElementById('btnSendPush');
  btn.disabled = true;
  const lbl = document.getElementById('btnSendPushLabel');
  const origLabel = lbl.textContent;
  lbl.textContent = scheduleOn ? 'Menjadwalkan...' : 'Mengirim...';

  try {
    const d = await App.api('/push-notif/send', { method:'POST', body: JSON.stringify(payload) });
    if (d?.success) {
      App.showToast(d.message || 'Berhasil', 'success');
      resetPushForm();
      loadPushStats();
      // Refresh tab terkait
      if (d.mode === 'scheduled') loadPushScheduled();
      else if (d.mode === 'sent') loadPushHistory();
    } else {
      App.showToast(d?.message || 'Gagal', 'error');
    }
  } catch(e) {
    App.showToast('Error: '+e.message, 'error');
  } finally {
    btn.disabled = false;
    lbl.textContent = origLabel;
  }
}

function resetPushForm() {
  document.getElementById('pushTitle').value = '';
  document.getElementById('pushBody').value  = '';
  document.getElementById('pushIcon').value  = '';
  document.getElementById('pushUrl').value   = '/portal';
  document.getElementById('pushFilterArea').value = '';
  document.getElementById('pushScheduleToggle').checked = false;
  toggleSchedule();
  document.querySelectorAll('.push-fpkg, .push-fbill, .push-fcust, .push-fisol').forEach(i => i.checked = false);
  clearManualPick();
  updatePushPreview();
  document.getElementById('pushPreviewText').innerHTML = 'Klik <b>Hitung Target</b> untuk melihat jumlah pelanggan.';
}

// ── Template picker (dari tab Kirim) ──
async function openTemplatePicker() {
  try {
    const d = await App.api('/push-notif/templates');
    const list = d?.data || [];
    if (!list.length) {
      return App.showToast('Belum ada template. Buat dulu di tab Template.', 'error');
    }
    // Simple picker pakai prompt list
    const choice = prompt('Pilih template (ketik nomor):\n\n' +
      list.map((t,i) => (i+1)+'. '+(t.icon||'')+' '+t.name+' — '+t.title.substring(0,40)).join('\n'));
    if (!choice) return;
    const idx = parseInt(choice) - 1;
    const tpl = list[idx];
    if (!tpl) return App.showToast('Nomor tidak valid', 'error');
    applyTemplate(tpl);
  } catch(e) { App.showToast('Error: '+e.message, 'error'); }
}

function applyTemplate(tpl) {
  document.getElementById('pushIcon').value  = tpl.icon || '';
  document.getElementById('pushTitle').value = tpl.title || '';
  document.getElementById('pushBody').value  = tpl.body || '';
  if (tpl.url) document.getElementById('pushUrl').value = tpl.url;
  updatePushPreview();
  App.showToast('Template "'+tpl.name+'" dipakai', 'success');
}

// ── TAB: TEMPLATE CRUD ──
async function loadPushTemplates() {
  const container = document.getElementById('pushTemplateList');
  container.innerHTML = '<div style="padding:28px;text-align:center;color:var(--d-muted);font-size:13px">Memuat...</div>';
  try {
    const d = await App.api('/push-notif/templates');
    const list = d?.data || [];
    if (!list.length) {
      container.innerHTML = '<div style="padding:28px;text-align:center;color:var(--d-muted);font-size:13px">Belum ada template. Klik <b>Tambah Template</b>.</div>';
      return;
    }
    window._PUSH_TPL_CACHE = list;
    container.innerHTML = list.map((t, idx) => {
      const meta = _CAT_META[t.category] || _CAT_META.other;
      return '<div class="push-tpl-card">' +
        '<div class="push-tpl-icon" style="background:'+meta.color+'22;color:'+meta.color+'">'+(t.icon || meta.icon)+'</div>' +
        '<div class="push-tpl-info">' +
          '<div class="push-tpl-name">' +
            '<span>'+escapeHtmlSt(t.name)+'</span>' +
            '<span class="push-tpl-badge" style="background:'+meta.color+'22;color:'+meta.color+'">'+meta.label+'</span>' +
          '</div>' +
          '<div class="push-tpl-title">'+escapeHtmlSt(t.title)+'</div>' +
          '<div class="push-tpl-body">'+escapeHtmlSt(t.body)+'</div>' +
        '</div>' +
        '<div style="display:flex;flex-direction:column;gap:5px">' +
          '<button class="push-mini-btn" onclick="useTemplateFromList('+idx+')" style="background:#dcfce7;color:#166534;border-color:#bbf7d0">Pakai</button>' +
          '<button class="push-mini-btn" onclick="editTemplateFromList('+idx+')">Edit</button>' +
          '<button class="push-mini-btn" onclick="deleteTemplateFromList('+idx+')" style="background:#fee2e2;color:#991b1b;border-color:#fecaca">Hapus</button>' +
        '</div>' +
      '</div>';
    }).join('');
  } catch(e) {
    container.innerHTML = '<div style="padding:28px;text-align:center;color:#dc2626;font-size:13px">Gagal: '+e.message+'</div>';
  }
}

function useTemplateFromList(idx) {
  const tpl = (window._PUSH_TPL_CACHE || [])[idx];
  if (!tpl) return;
  applyTemplate(tpl);
  // Pindah ke tab Kirim
  const tabs = document.querySelectorAll('.push-tab');
  if (tabs[0]) tabs[0].click();
}

function editTemplateFromList(idx) {
  const tpl = (window._PUSH_TPL_CACHE || [])[idx];
  if (!tpl) return;
  openTemplateModal(tpl);
}

async function deleteTemplateFromList(idx) {
  const tpl = (window._PUSH_TPL_CACHE || [])[idx];
  if (!tpl) return;
  if (!confirm('Hapus template "'+tpl.name+'"?')) return;
  const d = await App.api('/push-notif/templates/'+tpl.id, { method:'DELETE' });
  if (d?.success) { App.showToast('Dihapus', 'success'); loadPushTemplates(); }
  else App.showToast(d?.message || 'Gagal', 'error');
}

function openTemplateModal(tpl) {
  document.getElementById('pushTplModalTitle').textContent = tpl ? 'Edit Template' : 'Tambah Template';
  document.getElementById('tplId').value       = tpl ? tpl.id : '';
  document.getElementById('tplName').value     = tpl ? (tpl.name||'') : '';
  document.getElementById('tplCategory').value = tpl ? (tpl.category||'info') : 'info';
  document.getElementById('tplIcon').value     = tpl ? (tpl.icon||'') : '';
  document.getElementById('tplTitle').value    = tpl ? (tpl.title||'') : '';
  document.getElementById('tplBody').value     = tpl ? (tpl.body||'') : '';
  document.getElementById('tplUrl').value      = tpl ? (tpl.url||'') : '';
  document.getElementById('pushTplModal').style.display = 'flex';
}
function closeTemplateModal() { document.getElementById('pushTplModal').style.display = 'none'; }

async function saveTemplate() {
  const id = document.getElementById('tplId').value;
  const payload = {
    name:     document.getElementById('tplName').value.trim(),
    category: document.getElementById('tplCategory').value,
    icon:     document.getElementById('tplIcon').value.trim(),
    title:    document.getElementById('tplTitle').value.trim(),
    body:     document.getElementById('tplBody').value.trim(),
    url:      document.getElementById('tplUrl').value.trim()
  };
  if (!payload.name || !payload.title || !payload.body) {
    return App.showToast('Nama, judul, isi wajib diisi', 'error');
  }
  const url = id ? '/push-notif/templates/'+id : '/push-notif/templates';
  const method = id ? 'PUT' : 'POST';
  const d = await App.api(url, { method, body: JSON.stringify(payload) });
  if (d?.success) {
    App.showToast('Tersimpan', 'success');
    closeTemplateModal();
    loadPushTemplates();
  } else {
    App.showToast(d?.message || 'Gagal', 'error');
  }
}

// ── TAB: RIWAYAT ──
let _histSearchTimer = null;
function debouncedHistSearch() {
  clearTimeout(_histSearchTimer);
  _histSearchTimer = setTimeout(loadPushHistory, 400);
}

// SVG icon helpers (inline)
var SVG_ICON = {
  retry:     '<svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" style="vertical-align:-2px;margin-right:3px"><polyline points="23 4 23 10 17 10"/><polyline points="1 20 1 14 7 14"/><path d="M3.51 9a9 9 0 0114.85-3.36L23 10M1 14l4.64 4.36A9 9 0 0020.49 15"/></svg>',
  cancel:    '<svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" style="vertical-align:-2px;margin-right:3px"><circle cx="12" cy="12" r="10"/><line x1="4.93" y1="4.93" x2="19.07" y2="19.07"/></svg>',
  trash:     '<svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" style="vertical-align:-2px;margin-right:3px"><polyline points="3 6 5 6 21 6"/><path d="M19 6l-1 14a2 2 0 01-2 2H8a2 2 0 01-2-2L5 6m3 0V4a2 2 0 012-2h4a2 2 0 012 2v2"/></svg>',
  check:     '<svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>',
  x:         '<svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>',
  clock:     '<svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>',
  warning:   '<svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M10.29 3.86L1.82 18a2 2 0 001.71 3h16.94a2 2 0 001.71-3L13.71 3.86a2 2 0 00-3.42 0z"/><line x1="12" y1="9" x2="12" y2="13"/><line x1="12" y1="17" x2="12.01" y2="17"/></svg>',
  target:    '<svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" style="vertical-align:-1px;margin-right:3px"><circle cx="12" cy="12" r="10"/><circle cx="12" cy="12" r="6"/><circle cx="12" cy="12" r="2"/></svg>',
  sent:      '<svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" style="vertical-align:-1px;margin-right:3px"><path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/><polyline points="22 6 12 13 2 6"/></svg>',
  calendar:  '<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="vertical-align:-3px;margin-right:5px"><rect x="3" y="4" width="18" height="18" rx="2" ry="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg>',
  bell:      '<svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="#93c5fd" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M18 8A6 6 0 006 8c0 7-3 9-3 9h18s-3-2-3-9"/><path d="M13.73 21a2 2 0 01-3.46 0"/></svg>'
};

function _pushStatusLabel(status) {
  // Status label tanpa emoji — hanya text, warna dihandle CSS badge
  return {
    sent:      'Terkirim',
    scheduled: 'Terjadwal',
    failed:    'Gagal',
    cancelled: 'Dibatalkan',
    pending:   'Pending'
  }[status] || status;
}

async function loadPushHistory() {
  const status = document.getElementById('pushHistFilter').value;
  const search = document.getElementById('pushHistSearch').value.trim();
  const qs = [];
  if (status) qs.push('status='+encodeURIComponent(status));
  if (search) qs.push('search='+encodeURIComponent(search));
  qs.push('limit=50');
  const container = document.getElementById('pushHistoryList');
  container.innerHTML = '<div style="padding:28px;text-align:center;color:var(--d-muted);font-size:13px">Memuat...</div>';

  try {
    const d = await App.api('/push-notif/list?'+qs.join('&'));
    let list = d?.data || [];
    // Tab Riwayat hanya tampilkan notif yang BUKAN scheduled (itu ada di tab khusus)
    list = list.filter(n => n.status !== 'scheduled' && n.status !== 'pending');

    if (!list.length) {
      container.innerHTML = '<div style="padding:28px;text-align:center;color:var(--d-muted);font-size:13px">Belum ada riwayat pengiriman.</div>';
      return;
    }
    container.innerHTML = list.map(n => {
      const statusLabel = _pushStatusLabel(n.status);
      const when = n.sent_at ? 'Dikirim: ' + new Date(n.sent_at).toLocaleString('id-ID')
                 : n.scheduled_at ? 'Dijadwalkan: ' + new Date(n.scheduled_at).toLocaleString('id-ID')
                 : 'Dibuat: ' + new Date(n.created_at || n.createdAt).toLocaleString('id-ID');

      let actions = '';
      if (n.status === 'sent' || n.status === 'failed') {
        actions += '<button class="push-mini-btn" onclick="retryPush('+n.id+')" style="background:#dbeafe;color:#1e40af;border-color:#bfdbfe">'+SVG_ICON.retry+'Kirim Ulang</button>';
      }
      actions += '<button class="push-mini-btn" onclick="deletePush('+n.id+')" style="background:#fee2e2;color:#991b1b;border-color:#fecaca">'+SVG_ICON.trash+'Hapus</button>';

      return '<div class="push-hist-card">' +
        '<div class="push-hist-head">' +
          '<div style="font-size:22px">'+(n.icon||'🔔')+'</div>' +
          '<div class="push-hist-title">'+escapeHtmlSt(n.title)+'</div>' +
          '<span class="push-status-badge push-status-'+n.status+'">'+statusLabel+'</span>' +
        '</div>' +
        '<div class="push-hist-body">'+escapeHtmlSt(n.body)+'</div>' +
        '<div class="push-hist-meta">' +
          '<span>'+SVG_ICON.target+'Target: <b>'+n.target_count+'</b></span>' +
          (n.status === 'sent' ? '<span>'+SVG_ICON.sent+'Terkirim: <b style="color:#059669">'+n.sent_count+'</b></span>' : '') +
          (n.failed_count > 0 ? '<span style="color:#dc2626">'+SVG_ICON.warning+'Gagal: <b>'+n.failed_count+'</b></span>' : '') +
          '<span>'+SVG_ICON.clock+when+'</span>' +
          (n.error_message ? '<span style="color:#dc2626;flex-basis:100%">'+SVG_ICON.warning+escapeHtmlSt(n.error_message.substring(0,120))+'</span>' : '') +
        '</div>' +
        (actions ? '<div style="display:flex;gap:8px;margin-top:10px;flex-wrap:wrap">'+actions+'</div>' : '') +
      '</div>';
    }).join('');
  } catch(e) {
    container.innerHTML = '<div style="padding:28px;text-align:center;color:#dc2626;font-size:13px">Gagal: '+e.message+'</div>';
  }
}

// ── TAB: SCHEDULED — notifikasi yang masih menunggu waktu kirim ──
async function loadPushScheduled() {
  const container = document.getElementById('pushScheduledList');
  container.innerHTML = '<div style="padding:28px;text-align:center;color:var(--d-muted);font-size:13px">Memuat...</div>';

  try {
    const d = await App.api('/push-notif/list?status=scheduled&limit=100');
    const list = d?.data || [];

    if (!list.length) {
      container.innerHTML =
        '<div style="padding:40px 20px;text-align:center;color:var(--d-muted);font-size:13px;background:#f8faff;border:1.5px dashed var(--d-border);border-radius:12px">' +
          '<svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" style="display:block;margin:0 auto 10px;opacity:.4"><rect x="3" y="4" width="18" height="18" rx="2" ry="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg>' +
          'Tidak ada notifikasi terjadwal.<br>' +
          '<span style="font-size:11.5px">Buat jadwal dari tab <b>Kirim Notifikasi</b> &rarr; centang "Jadwalkan kirim nanti".</span>' +
        '</div>';
      return;
    }

    // Sort by scheduled_at ASC (yang paling dekat di atas)
    list.sort((a, b) => new Date(a.scheduled_at) - new Date(b.scheduled_at));

    const now = Date.now();
    container.innerHTML = list.map(n => {
      const schedTime = n.scheduled_at ? new Date(n.scheduled_at) : null;
      const ms = schedTime ? schedTime.getTime() - now : 0;
      const schedStr = schedTime ? schedTime.toLocaleString('id-ID', {
        weekday: 'short', day: '2-digit', month: 'short', year: 'numeric',
        hour: '2-digit', minute: '2-digit'
      }) : '—';

      // Countdown badge
      let countdown = '';
      let countdownColor = '#1e40af';
      let countdownBg = '#dbeafe';
      if (ms < 0) {
        countdown = 'Terlewat';
        countdownColor = '#991b1b';
        countdownBg = '#fee2e2';
      } else if (ms < 60*1000) {
        countdown = 'Sebentar lagi';
        countdownColor = '#854d0e';
        countdownBg = '#fef3c7';
      } else if (ms < 60*60*1000) {
        const mins = Math.round(ms / 60000);
        countdown = mins + ' menit lagi';
      } else if (ms < 24*60*60*1000) {
        const hrs = Math.round(ms / 3600000);
        countdown = hrs + ' jam lagi';
      } else {
        const days = Math.round(ms / 86400000);
        countdown = days + ' hari lagi';
      }

      return '<div class="push-hist-card" style="border-left:1px solid #e0e7f3">' +
        '<div class="push-hist-head">' +
          '<div style="font-size:22px">'+(n.icon||'🔔')+'</div>' +
          '<div class="push-hist-title">'+escapeHtmlSt(n.title)+'</div>' +
          '<span style="padding:3px 10px;border-radius:20px;font-size:10.5px;font-weight:800;background:'+countdownBg+';color:'+countdownColor+'">'+countdown+'</span>' +
        '</div>' +
        '<div class="push-hist-body">'+escapeHtmlSt(n.body)+'</div>' +
        '<div class="push-hist-meta">' +
          '<span style="font-weight:700;color:#1e40af">'+SVG_ICON.calendar+schedStr+'</span>' +
          '<span>'+SVG_ICON.target+'Target: <b>'+n.target_count+'</b></span>' +
        '</div>' +
        '<div style="display:flex;gap:8px;margin-top:10px;flex-wrap:wrap">' +
          '<button class="push-mini-btn" onclick="cancelPush('+n.id+')" style="background:#fef3c7;color:#854d0e;border-color:#fde68a">'+SVG_ICON.cancel+'Batalkan</button>' +
        '</div>' +
      '</div>';
    }).join('');
  } catch(e) {
    container.innerHTML = '<div style="padding:28px;text-align:center;color:#dc2626;font-size:13px">Gagal: '+e.message+'</div>';
  }
}

async function retryPush(id) {
  if (!confirm('Kirim ulang notifikasi ini ke target yang sama?')) return;
  const d = await App.api('/push-notif/'+id+'/retry', { method:'POST' });
  if (d?.success) { App.showToast(d.message || 'Berhasil', 'success'); loadPushHistory(); loadPushStats(); }
  else App.showToast(d?.message || 'Gagal', 'error');
}

async function cancelPush(id) {
  if (!confirm('Batalkan notifikasi yang dijadwalkan?')) return;
  const d = await App.api('/push-notif/'+id+'/cancel', { method:'POST' });
  if (d?.success) {
    App.showToast('Dibatalkan', 'success');
    loadPushScheduled();
    loadPushHistory();
    loadPushStats();
  } else App.showToast(d?.message || 'Gagal', 'error');
}

async function deletePush(id) {
  if (!confirm('Hapus log notifikasi ini secara permanen?')) return;
  const d = await App.api('/push-notif/'+id, { method:'DELETE' });
  if (d?.success) { App.showToast('Dihapus', 'success'); loadPushHistory(); }
  else App.showToast(d?.message || 'Gagal', 'error');
}

// Close modal on backdrop click
document.getElementById('annModal').addEventListener('click', function(e) {
  if (e.target === this) closeAnnModal();
});
</script>
</body></html>
